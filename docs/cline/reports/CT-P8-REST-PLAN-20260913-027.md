# CT-P8-REST-PLAN-20260913-027

**Task ID:** `CT-P8-REST-PLAN-20260913-027`
**Title:** CarbonTally Phase 8 / Phase 8-X — Remaining-Work Plan, Readiness Assessment and Prepared Implementation Instructions
**Date:** 2026-09-13
**Type:** PLANNING / FORENSICS ONLY — **nothing implemented, no migration applied, no database modified, no commit, no push**
**Role:** Implementation lead / governance planner
**Status of this document:** planning material. It authorises nothing.

**Evidence discipline:** every material statement is tagged **[R]** repository fact · **[V]** verified · **[D]** PO/ratified decision · **[U]** unresolved · **[I]** interpretation · **[REC]** recommendation.

---

## 1. Executive summary

1. **B2 is complete on the evidence and closed by the PO, but its closure record does not exist in the repository.** `CT-P8-B2-IMPLEMENTATION-20260913-024` (implementation) and `CT-P8-B2-INDEPENDENT-VERIFICATION-20260913-025` (`B2 INDEPENDENT VERIFICATION PASS — READY FOR PO CLOSURE`) exist. A repository-wide search finds **no** `CT-P8-B2-PO-CLOSURE-20260913-026` file and no other closure artefact **[R]**. The closure is therefore a PO statement carried into this task's prompt, **not** a repository record. This is reported, not silently reconciled (see §24 C-1).
2. **Nothing in Phase 8 or Phase 8-X is deployed.** B1, B2 and the Phase 8 lifecycle migration are absent from every persistent environment; the dev database and `carbontally_test` hold neither B1 nor B2 schema **[R]/[V]** from `-019` §17, `-023` §8, the B2 contract F-B2-16. Outside disposable clones, every B1/B2 runtime suite reports **SKIPPED**, which is **not** a PASS.
3. **B2 does not unblock B3 by itself.** B3 needs B2's *schema present in the environment B3 is built and tested against*, not merely B2's code in the worktree. The next engineering action is therefore a **controlled, snapshot-protected application of B1+B2 to a non-production (dev/QA) environment**, which **PQ-5 already ratified in principle** (`dev/QA allowed; production gated`) **[D]**.
4. **B3 is NOT READY for PO authorization.** There is no B3 contract, and `-023` §11 records that the B3/B4 internal decisions are **"NOT RECOVERABLE FROM CURRENT REPOSITORY EVIDENCE"**. Only the batch's *content envelope* is ratified (read-only projections; framework mappings; applicability engine; purpose/template projections; SECR intensity model — `PQ-1`, roadmap §20) **[D]**. A **contract-and-decision task** must precede any B3 implementation instruction.
5. **B4 is NOT READY** (depends on B3; narrative/finalisation policy inputs partially ratified through the Disclosure Model decisions `A1`, `A3`, `P3`, `DM-5`, but the batch has no contract).
6. **Two separate remediation workstreams are designed but unauthorised:** **P1** (PDF/IMAGE extraction fidelity; 8 open PO decisions `P1-D1…P1-D8`) and **P2** (EF-E picker defect; forensic complete, live-catalogue census blocked on an authorised read-only DB session). Neither blocks B3/B4 **[D]/[R]**.
7. **RLS remediation remains a held security workstream** with 13 open decisions and an unresolved internal sequence discrepancy; it must not be bundled with product work **[D]**.
8. **Phase 8-X and Phase 9 name the same domain and the conflict is still open.** The RLS register (09-12) states the Phase 9 identifier was never PO-ratified; the Phase 9 baseline document was nevertheless committed the same day (`b471286`) and itself flags three MATERIAL conflicts requiring PO adjudication **[R]/[D]**. **X8 remains undefined.** This requires a PO reconciliation before any X implementation instruction is used.
9. **Insight I1 authorisation is ambiguous:** commit `d91ace5`'s subject says *"authorize I1"* while the ratified D2 document says **"READY FOR I1 IMPLEMENTATION AUTHORISATION"** and requires a separate prompt; no `carbontally_insight_*` object exists anywhere **[R]**. **PO clarification required.**
10. **This plan is non-authorising.** **Prepared, copy/paste Cline instructions** for every identified future batch are contained in §6 (§6.1 contract, §6.2 implementation), §7, §9, §10, §11.4, §12, §13, §14, §15, §16, §17, §18.2 and §23.1–§23.4 — each opening with an explicit authorization gate. Only a later PO instruction naming one task ID authorises execution of that one instruction. **None of them can be copied into Cline execution today** (see §26).

### Verdict summary

| Item | Verdict |
|---|---|
| B2 | **IMPLEMENTED + INDEPENDENTLY VERIFIED (PASS); PO CLOSED per prompt — repository closure record ABSENT** |
| B3 | **B3 NOT READY — PO DECISIONS REQUIRED** |
| B4 | **B4 NOT READY — PO DECISIONS REQUIRED** (and B3-dependent) |
| B1/B2 application to non-production | **READY FOR A BOUNDED AUTHORISED OPERATIONAL STEP** (PQ-5 ratified dev/QA) |
| Production application | **PROHIBITED** (G0-D open) |
| P1 | DESIGNED — NOT AUTHORISED (7 open decisions) |
| P2 | FORENSIC COMPLETE — NOT AUTHORISED (census blocked) |
| RLS | HELD — SEPARATE WORKSTREAM |
| Phase 8-X / Phase 9 | **UNRECONCILED — PO DECISION REQUIRED** |
| Insight I1 | **AUTHORISATION AMBIGUOUS — PO CLARIFICATION REQUIRED** |

---

## 2. Current-state baseline

### 2.1 Worktree record (captured before analysis; nothing was written to the repository)

| Item | Value |
|---|---|
| Branch | `main` |
| HEAD | `37b19d13723b0b1eabceeade86ce1615a98ab400` — `feat(admin): admin-configurable Google Analytics 4 (Analytics & Integrations)` (2026-09-13T13:34:20+06:00) |
| Staged | **0** |
| Tracked modifications | **218** (pre-existing, preserved) |
| Untracked (porcelain entries) | **89** |
| Untracked (`-uall`) | **839** |
| Migrations on disk | **59** |
| Just before HEAD | `19e4f01` (Phase 8 report lifecycle foundation), `b471286` (Phase 9 baseline), `c86b83c` (S1 correctness), `d204fda`/`d91ace5` (Insight D2) |

**No reset, clean, stash, checkout, stage, commit or push was performed by this task.** The only file written anywhere is this document.

### 2.2 B2 artefacts present on disk (uncommitted, as designed)

| Artefact | Evidence |
|---|---|
| `supabase/migrations/20260916000000_p8_b2_evidence_line_items.sql` | **[R]** present |
| `supabase/migrations/20260916010000_p8_b2_provenance_line_links.sql` | **[R]** present |
| `backend/domain/line_items.py`, `backend/data/evidence_line_items.py`, `backend/tools/backfill_evidence_line_items.py` | **[R]** present |
| `docs/cline/reports/CT-P8-B2-IMPLEMENTATION-20260913-024.md` (406 lines) | **[R]** |
| `docs/cline/reports/CT-P8-B2-INDEPENDENT-VERIFICATION-20260913-025.md` (415 lines) | **[R]** |
| **B3/B4 contract, decision or authorisation document** | **NONE — searched `docs/architecture/` and `docs/cline/reports/`** **[R]** |
| **`CT-P8-B2-PO-CLOSURE-…-026`** | **NONE — searched the whole tree** **[R]** |

### 2.3 Environment reality (unchanged from `-019` §17 / `-023` §8)

| Database | B1 schema | B2 schema | Consequence |
|---|---|---|---|
| dev application DB (`DATABASE_URL`) | absent | absent | B1/B2 runtime suites SKIP |
| `carbontally_test` (integration DB) | absent | absent | B1/B2 runtime suites SKIP |
| production | absent (21 applied / 34 outstanding + B2's 2) | absent | production prohibited (G0-D) |
| disposable clones (`ct_b2_iv_20260913`, `carbontally_b2_clone_20260913`) | applied | applied | where V2's PASS was obtained; retained at PO discretion |


---

## 3. Closed work

| Item | Record | State |
|---|---|---|
| Phase 8 reporting discovery / D1–D17 architecture | discovery `-001`; ratification `CARBONTALLY_PHASE8_REPORTING_ARCHITECTURE_PO_RATIFICATION_D1-D17` | **PO-RATIFIED** **[D]** |
| Report lifecycle specification | `CARBONTALLY_PHASE8_REPORTING_LIFECYCLE_SPEC_20260912.md` | **PO-RATIFIED (spec)**; its §33/§34 policy inputs remain **PO DECISION REQUIRED** **[D]** |
| Disclosure Model design + 20 decisions | design + Decision Record | **19 RATIFIED + `E1-COV` CONDITIONAL** **[D]** |
| S0 open-decision closure package | `…OPEN_DECISION_CLOSURE_AND_IMPLEMENTATION_AUTHORISATION…` | **PO-RATIFIED** (A1–A15 classification) **[D]** |
| S1 report correctness (`is_current`) | commit `c86b83c` | **IMPLEMENTED — INDEPENDENT VERIFICATION NOT EVIDENCED** **[R]** |
| S3 report lifecycle states | commit `19e4f01`, migration `20260913000000`, applied locally | **IMPLEMENTED — INDEPENDENT VERIFICATION NOT EVIDENCED** **[R]** |
| B1 disclosure foundation (11 tables, RLS helper, seeds) | `-009`, `-010/011/012`, `-013`, `-015`, `-016` | **IMPLEMENTED + V1R PASS WITH NONBLOCKING FINDINGS (P3 ×6); NOT APPLIED TO ANY ENVIRONMENT** **[D]/[V]** |
| B2 evidence / line-item addressability | `-017`, `-018`, `-019`, `-021`, `-024`, `-025` | **IMPLEMENTED + INDEPENDENT VERIFICATION PASS; PO CLOSED per prompt — repository closure record absent** **[R]/[V]** |
| B1 PO decisions PQ-1…PQ-8 | B1 ratification report | **ALL CLOSED — RATIFIED** (incl. **PQ-5 dev/QA allowed, production gated**; **PQ-8 `B1 → B2 → B3 → B4` sequence**) **[D]** |
| G0-B / G0-F / G0-I | `-008` §6–§8 | **CLOSED / RECORDED** **[D]** |
| GA4 admin configuration (unrelated side work) | commit `37b19d1` (14 files: `v3_settings.py`, `settings.py`, frontend analytics) | **COMMITTED — outside Phase 8 scope; coordination consideration only** **[R]** |

**Not closed and frequently mistaken for closed:** B1/B2 *application* to any persistent environment; S1/S3 independent verification; G0-C; G0-D; A-RLS; Phase 8-X; I1; B3; B4.

---

## 4. Remaining Phase 8 scope (complete inventory)

Legend for “Ready?” columns: **Y** = ready now · **N** = not ready · **N/A**.

| ID | Item | Purpose | Current state | Authoritative record | Impl | Verified | PO auth | Ready PO? | Ready impl? | Blockers | Next gate |
|---|---|---|---|---|---|---|---|---|---|---|---|
| R1 | B2 PO closure record (+ F-025 dispositions) | Close B2 on the record; dispose of F-025-1/F-025-2 | IV PASS; closure asserted in prompt; **no repo record** | `-024`, `-025`; this task’s prompt | n/a | n/a | **CLOSED (per prompt)** | Y | N/A | missing closure artefact | **write closure record** (`…028`) |
| R2 | B1/B2 controlled application to non-production | Make B1/B2 real in dev/QA so B3 can build on it and suites stop skipping | Not applied anywhere | B2 contract §18.4/§18.5; **PQ-5 (dev/QA allowed)** | blocked | blocked | **not yet (needs bounded authorisation)** | Y | **Y (operational step)** | restorable snapshot; B1 index dedupe precondition (V1R R4) | `…029` |
| R3 | B2 worktree reconciliation (track/commit) | Make B2 artefacts traceable | Untracked; staged 0 | repo discipline (AGENTS.md §70) | n/a | n/a | PO decision | Y | N/A | GA4 commit history | PO decision (fold into `…028`) |
| R4 | S1/S3 independent verification | Close the verification asymmetry | Implemented; no IV report | lifecycle spec; `-023` §10.6 | done | **none** | n/a | Y | N/A | S3 applied only locally | `…030` |
| R5 | S2 lifecycle schema **+ A-RLS** | Extend `report_versions`; decide report-table RLS posture | A-RLS **OPEN**; S3 overtook S2 | closure package L1165; `-023` §10.5 | none | none | **decision required** | Y | N | A-RLS | PO decision → `…046` |
| R6 | **B3** Disclosure Model integration | Projections, mappings, applicability, purpose, intensity | **No contract; internal decisions not recoverable** | roadmap §20; `PQ-1`; `-023` §11 | none | none | **not authorised** | Y (contract) | **N** | contract absent; `E1-COV`; B1/B2 not applied | `…031` → `…032` |
| R7 | **B4** narrative + frozen artefact + finalisation | Narrative bound to requirements; frozen export; `DM-5` gate | **No contract** | roadmap §20; `A1`/`A3`/`P3`/`DM-5`/`DM-6`; S4 report | none | none | **not authorised** | N (needs B3) | **N** | B3; batch allocation of S5/S6/S7 unrecorded | `…034` → `…035` |
| R8 | S5+ frozen final PDF | Immutable artefact + hash | Designed only (`final_report_url` dormant) | lifecycle spec §19; roadmap B4 | none | none | not authorised | N | N | folded into B4? **unrecorded** | PO allocation decision |
| R9 | S6 frontend lifecycle UI / S7 regression+security tests | UI + negative tests | Designed only | lifecycle spec §24/§32 | none | none | not authorised | N | N | **allocation into B4/B4-IV unrecorded** | PO allocation decision |
| R10 | S8 AI-assisted narrative | Post-safety AI narrative | Deferred | lifecycle spec §29/§35 | none | none | not authorised | N | N | AI safety preconditions NOT MET | separate authorisation |
| R11 | E1 evidence closure (**G0-A**) | Unblock E1 seed rows for B3 | `E1-COV` CONDITIONAL; identifiers unresolved | Decision Record §14; roadmap §29 | none | none | evidence/PO | Y | N | authoritative ESRS sources | `…048` |
| R12 | **P1** PDF/IMAGE extraction fidelity | Make extraction faithful (N lines → N lines) | Contract complete; **7 open decisions** | P1 contract §19/§20; `-020` | none | none | **not authorised** | Y | N | `P1-D1…P1-D8` | `…037` |
| R13 | **P2** EF-E picker correctness | Fix exact-unit exclusion | Forensic formalised; census blocked | EF forensic §11/§13 | none | none | **not authorised** | Y | N | read-only DB session | `…038` → PO authorisation |
| R14 | RLS production remediation | Close the security hold | **HELD**; 13 decisions; sequence discrepancy | hold register §6.1/§8/§10; RLS spec | none | none | **held** | Y | N | PO reopen | `…039` |
| R15 | Phase 8-X X1 (stages X1–X3) | Worker/queue visibility, health correctness, heartbeat | Designed, unauthorised | P8-X discovery §13/§15/§19 | none | none | not authorised | Y | N | `PX-2/PX-4/PX-5`; naming conflict | `…040` → `…043` |
| R16 | Phase 8-X X2 (stages X4–X7) | Aggregation, console, thresholds, API metrics | Designed, unauthorised | P8-X discovery §15 | none | none | not authorised | N | N | X1; `PX-6/PX-7` | `…045` |
| R17 | Phase 8-X **X8** | Verification stage | **UNDEFINED** | none | none | none | n/a | Y | N | must be defined | `…040` |
| R18 | Insight **I1** | Persistent Insight foundation | **Authorisation ambiguous**; nothing implemented | D2 ratification §24; commit `d91ace5` | none | none | **ambiguous** | Y | N | PO clarification | `…041` |
| R19 | G0 gate reconciliation | Close/adjudicate remaining gates | G0-A/C/D/G open; G0-E closed by B2 | `-008` §18; roadmap §29 | n/a | n/a | PO | Y | N/A | — | `…042` |
| R20 | D16 legacy report disposition (**LEG**) | Retire/retain legacy route | Ratified as **separate** workstream; unexecuted | Decision Record §6 `LEG`; `-008` | none | none | separate | Y | N | separate authorisation | separate task |


---

## 5. B3 readiness assessment

### 5.1 What B3 is actually defined to include

| Source | Definition of B3 |
|---|---|
| Roadmap §20 (Option B batch plan) **[D]** | *B3 — Disclosure Model integration*: read-only calculation **projections**; **framework mappings** (GHG Protocol + SECR + E1 **concept set**); **applicability engine**; **purpose/template projections**; **SECR intensity model**. Prerequisite: B1, B2. Must **not** include: calculation-engine change; E1 **identifiers** (until G0); per-gas; base-year. Gate **V3**. |
| B2 contract §14.1/§14.2 **[D]** | B3 owns the deterministic **value→line enumeration read model**; B2 only made it a one-hop join. B3 must not be absorbed into B2 (and B2 must not open B3's schema). |
| `PQ-1` (B1 ratification) **[D]** | Intensity catalogue placement → **B3**; B1 keeps 11 tables; `INTENSITY_RATIO` reserved. |
| Decision Record (20 decisions) **[D]** | Supplies the ratified semantics B3 must consume: `DM-1` (requirement codes), `DM-2` (purpose vs `report_type`), `DM-3` (period), `DM-4` (benchmarking), `DM-5` (finalisation classes), `DM-6` (drill-down), `DM-7` (line-item population), `APPL` (applicability, `UNDETERMINED`), `GP-CONS`, `GP-S2M`, `GP-S3`, `GP-GAS` (deferred), `GP-BY` (deferred), `D11-CAT` (intensity denominator catalogue), `E1-VER`, `E1-COV` (**CONDITIONAL**), `D15` (version binding). |

### 5.2 What exists / what does not

| Element | State |
|---|---|
| B3 implementation contract | **DOES NOT EXIST** (no B3 file in `docs/architecture/` or `docs/cline/reports/`) **[R]** |
| B3 decision record | **DOES NOT EXIST**; `-023` §11 item 2: *“B3/B4 internal decisions are NOT RECOVERABLE — beyond the readiness batch table and the design doc's MVP list”* **[R]** |
| B3 authorisation record | **DOES NOT EXIST** **[R]** |
| B3 implementation | **NONE** **[R]** |
| B3 seeds / mapping content | **NONE**; E1 content gated by `E1-COV = CONDITIONAL` and **G0-A** **[D]** |
| B3 prerequisite B1/B2 in an environment | **ABSENT everywhere except disposable clones** **[V]** |

### 5.3 PO decisions required before B3 implementation (topics, to be formalised as `B3-Dn` by the contract task)

| # | Decision topic | Why it is a PO decision |
|---|---|---|
| **B3-PO-1** | Projection semantics: what a disclosure value is derived **from** (snapshots, `emissions_logs`, aggregation over scopes/categories/period), and how multiple snapshots/lines aggregate | determines the authoritative numbers the product shows |
| **B3-PO-2** | Which framework **versions** and **requirement sets** are in the initial mapping (GHG Protocol, SECR, ESRS E1 concept set) and who approves that content | regulated vocabulary; **no invented identifiers** (`D17`) |
| **B3-PO-3** | Applicability engine inputs and lifecycle (which customer facts, who supplies them, when re-evaluation occurs, `UNDETERMINED` retention) | `APPL` sets the rule but not the operational inputs |
| **B3-PO-4** | Purpose projections: the `purpose_code` set and its binding to `report_type` and `template_structure` | `DM-2`/`D6-R` constrain but do not enumerate |
| **B3-PO-5** | SECR intensity model: denominators in the `D11-CAT` catalogue and their evidence basis | catalogue content is evidence-gated; numeric values must not be invented |
| **B3-PO-6** | Value status semantics in the projection layer (`effective_class` authoritative; `value_status` = `PENDING`/`RESOLVED`/`UNRESOLVED` per `PQ-3`) and their transition triggers | affects workflow behaviour |
| **B3-PO-7** | Drill-down boundary (`DM-6`): does B3 ship the read model + API only, with UI in B4? | scope boundary between batches |
| **B3-PO-8** | E1 emission policy while `E1-COV` is CONDITIONAL (what is emitted, and how `NOT_SUPPORTED` vs `CUSTOMER_INPUT_REQUIRED` is chosen) | honesty rule (`D12`, `DM-5`, §17 acceptance principles) |
| **B3-PO-9** | Baseline environment for B3 build/test (does B3 require R2 to have happened first?) | deployment/verification ordering |
| **B3-PO-10** | Confirmation that B3 is **read-only** w.r.t. the calculation engine, factor data, evidence classification and legacy reporting | preserves the ratified root boundaries |
| **B3-PO-11** | Whether B3 exposes the deterministic value→line enumeration as an API/endpoint now or defers it to the drill-down UI | API surface decision |
| **B3-PO-12** | Historical-report behaviour: what a pre-B1/B2 report version shows when projected (and whether G0-F/`DM-7` boundaries are restated verbatim) | `DM-7`/`D15` non-destructive rules |

### 5.4 Verdict

### `B3 NOT READY — PO DECISIONS REQUIRED`

**Basis [R]/[D]:** the batch's content envelope is ratified, but there is **no contract**, the internal decisions are recorded as **not recoverable**, `E1-COV` is CONDITIONAL (blocking E1 seed content), and the prerequisite schema (B1+B2) exists in **no** persistent environment. Only after `…031` produces the contract and the PO ratifies `B3-PO-1…B3-PO-12` can an implementation instruction be authorised. **This plan does not authorise B3.**

### 5.5 Does B2 closure change anything for B3?

* The ratified dependency is unchanged: `B1 → B2 → B3 → B4` (`PQ-8`) **[D]**. B2's verification PASS removes the *architecture* risk that B2's line-links would not exist; it does **not** remove the **environment** gap: B3 cannot be built or runtime-tested against a schema that is absent (R2). **[I]**
* B2 added **no** B3 objects, and B2's contract §16/IV §6.4 confirm no B3/B4 objects exist **[D]/[V]**.
* B2 accepts **no** unresolved PO decision (`B2-D1…B2-D12` all closed), so B2 contributes nothing open to B3 **[D]**.

---

## 6. B3 — COPY/PASTE CLINE INSTRUCTION (STAGE 1: CONTRACT + PO DECISIONS)

```text
============================================================
COPY/PASTE CLINE IMPLEMENTATION INSTRUCTION
TASK: CT-P8-B3-CONTRACT-AND-PO-DECISIONS-20260913-031
STATUS: PLANNED — NOT YET AUTHORIZED
TYPE: CONTRACT / DECISION PREPARATION ONLY (no implementation)
============================================================

IMPLEMENTATION AUTHORIZATION GATE

This instruction is a prepared instruction only.
Cline MUST NOT execute it unless the PO has separately and explicitly
authorized task CT-P8-B3-CONTRACT-AND-PO-DECISIONS-20260913-031 in a
later instruction.
Until that authorization exists, this section is planning material only.

READ FIRST (authoritative records)
- docs/architecture/CARBONTALLY_PHASE8_P8X_COMPREHENSIVE_IMPLEMENTATION_READINESS_20260912.md (§20 batch plan B3 row; §8 gap matrix G-1..G-6, G-13, G-16, G-19; §29 G0-A/G0-E)
- docs/architecture/CARBONTALLY_PHASE8_DISCLOSURE_MODEL_DESIGN_20260912.md (§6–§14, §17–§23)
- docs/architecture/CARBONTALLY_PHASE8_DISCLOSURE_MODEL_DECISION_RECORD_20260912.md (all 20 decisions; §16 boundaries)
- docs/architecture/CARBONTALLY_PHASE8_B2_IMPLEMENTATION_CONTRACT_20260913.md (§14.1/§14.2/§14.3)
- docs/architecture/CARBONTALLY_PHASE8_B1_PO_DECISION_RATIFICATION_20260912.md (PQ-1 intensity→B3; PQ-6 seeds; PQ-8 sequence)
- docs/cline/reports/CT-P8-CURRENT-STATE-RECOVERY-20260913-023.md (§7, §11, §12 D/E)
- docs/cline/reports/CT-P8-B2-IMPLEMENTATION-20260913-024.md and CT-P8-B2-INDEPENDENT-VERIFICATION-20260913-025.md
- AGENTS.md

CURRENT BASELINE (verified at planning time)
- B1 implemented + V1R PASS; B2 implemented + IV PASS; B1/B2 applied in NO persistent environment.
- Repo: branch main, HEAD 37b19d13…, staged 0, 218 tracked modifications, 59 migrations.
- No B3 contract, decision record or authorisation exists.

OBJECTIVE
Produce the complete B3 implementation contract and the PO decision register
required to authorise a bounded B3 implementation task, without implementing any
part of B3.

IN SCOPE
1. Verify and state the exact current state of the disclosure layer (B1 tables) and the
   line-linkage (B2) as they exist in the repository.
2. Define B3's normative deliverables and acceptance criteria (gate V3) as testable statements.
3. Define the read-model shape for projections (queries only; no calculated values
   re-derived outside the existing snapshot/emissions data).
4. Define the framework/requirement/mapping **loading** mechanism and the seed governance
   (what may be seeded, by whom, with what evidence) — no invented identifiers.
5. Define the applicability engine contract (inputs, outputs, `UNDETERMINED`, period/version binding).
6. Define purpose/template projections and the `purpose_code` model.
7. Define the SECR intensity model contract and the `D11-CAT` denominator catalogue governance.
8. Define the value-status/`effective_class` projection semantics.
9. Define the B3→B4 interface and the B3→drill-down boundary (`DM-6`).
10. Define the test/verification contract (static, pure, runtime; ALLOW and DENY; the environment
    declaration that states whether B1+B2 must be provisioned).
11. Enumerate every open decision as a formal `B3-Dn` entry with a recommendation and rationale
    (from §5.3 B3-PO-1…B3-PO-12) for PO ratification.
12. Record the E1 handling rule while `E1-COV` is CONDITIONAL (G0-A).

OUT OF SCOPE
- Any implementation, migration, seed row, API, frontend, RLS or data change.
- Any change to the calculation engine, factor data, evidence classification, B1/B2 objects,
  P1/P2, RLS remediation, Phase 8-X, Insight or legacy reporting.
- Any invented regulatory identifier, threshold or numeric value.

DEPENDENCIES
- B2 closure (PO, per prompt) — for contract lineage only.
- G0-A (E1 evidence) — blocks only the E1 seed content, not the contract.
- R2 (B1/B2 environment application) — the contract must state the environment requirement
  that B3's runtime verification will inherit.

IMPLEMENTATION REQUIREMENTS (documentation-only task)
- Produce exactly two documents:
  (1) docs/architecture/CARBONTALLY_PHASE8_B3_IMPLEMENTATION_CONTRACT_20260913.md,
  (2) the mandatory report docs/cline/reports/CT-P8-B3-CONTRACT-AND-PO-DECISIONS-20260913-031.md.
- Follow the structure and rigour of the B1/B2 contracts (purpose, boundary, exact schema/queries,
  acceptance criteria, test contract, migration strategy, security posture, PO decisions, verdict).
- Every normative statement must be traceable to a ratified decision or repository evidence.
- Mark anything undecidable as [DECISION REQUIRED] — never invent.

TEST REQUIREMENTS
- None (no code). Instead: a completeness self-check table proving every §5.3 topic became a
  `B3-Dn` entry and every §20 B3 scope element is covered.

INDEPENDENT VERIFICATION
This task is not implementation; its output (the contract) becomes the subject of PO
ratification, and the later implementation is separately independently verified (`…033`).
Cline must not self-certify the contract as "ratified".

REPORTING
Create: docs/cline/reports/CT-P8-B3-CONTRACT-AND-PO-DECISIONS-20260913-031.md
The report must include: task identity; sources reviewed; verified current state; contract
summary; the complete `B3-Dn` decision register with statuses; gaps/conflicts found; files
created; git/worktree before-and-after; confirmation that no implementation occurred; verdict.

STOP CONDITIONS
Stop immediately if:
- the authoritative design/decision records contradict each other on B3 scope;
- a required B3 element has no ratified basis (report it, do not design around it);
- producing the contract would require inventing regulatory identifiers or numeric values;
- the worktree contains unrelated uncommitted work that would be altered.

DO NOT
- expand scope; modify unrelated work; alter unrelated migrations; resolve unrelated findings;
- commit; push; deploy;
unless explicitly authorized by the applicable governance decision.

FINAL STATUS
Return: CONTRACT COMPLETE — READY FOR PO DECISIONS  (or)  BLOCKED — <reason>
============================================================
```


---

## 6.2 B3 — COPY/PASTE CLINE INSTRUCTION (STAGE 2: IMPLEMENTATION)

> **NOTE:** this block is complete in form but deliberately **not completable in substance** until `…031` has produced a ratified B3 contract. Its `IN SCOPE` section instructs Cline to take the scope **verbatim from the ratified contract** and to **STOP** if that contract is absent. Do not use it before the contract exists.

```text
============================================================
COPY/PASTE CLINE IMPLEMENTATION INSTRUCTION
TASK: CT-P8-B3-IMPLEMENTATION-20260913-032
STATUS: PLANNED — NOT YET AUTHORIZED
============================================================

You are implementing CT-P8-B3-IMPLEMENTATION-20260913-032.

IMPLEMENTATION AUTHORIZATION
This task is authorized only if the PO has explicitly authorized
CT-P8-B3-IMPLEMENTATION-20260913-032 in the current conversation.
If explicit authorization is absent:
STOP immediately and report that implementation authorization has not been provided.

READ FIRST
- docs/architecture/CARBONTALLY_PHASE8_B3_IMPLEMENTATION_CONTRACT_20260913.md (the ratified contract)
- docs/cline/reports/CT-P8-B3-CONTRACT-AND-PO-DECISIONS-20260913-031.md
- Every authoritative record the contract cites (B1/B2 contracts, Decision Record, readiness §20/§29)
- AGENTS.md

CURRENT BASELINE
- B1 + B2 implemented and independently verified; applied in the environment named by the
  contract's environment declaration (see R2).
- No B3 code, migration or seed exists.
- Worktree state must be captured before any change (branch, HEAD, staged, tracked mods, untracked).

OBJECTIVE
Implement exactly the B3 deliverables authorised by the ratified contract and nothing else.

IN SCOPE
<take verbatim from the ratified contract's implementation-boundary section; if the contract
 does not exist, STOP — do not infer scope from this plan>

OUT OF SCOPE
- Calculation-engine change; factor-data change; evidence-classification change.
- Any B4 object (narrative store, frozen artefact, finalisation UI).
- E1 identifiers while E1-COV is CONDITIONAL and G0-A is open.
- Per-gas (GP-GAS) and base-year (GP-BY) functionality.
- P1, P2, RLS remediation, Phase 8-X, Insight, legacy D16 reporting.
- Any modification of B1 or B2 migration files, or of any unrelated migration.

DEPENDENCIES
- Ratified B3 contract (`…031`) and PO ratification of `B3-D1…B3-Dn`.
- R2 environment application (B1+B2 present in the target non-production environment).
- G0-A only for E1 seed content.

IMPLEMENTATION REQUIREMENTS
- Additive migrations only; no destructive statement; no silent rewrite of historical data.
- Re-use the existing calculation/snapshot/emissions data as the only authoritative numeric source.
- Preserve tenant isolation and enforce authorization server-side (UI is never the boundary).
- Preserve provenance: every projected value must resolve through the B1/B2 chain.
- Seeded catalogue content only where evidence-backed and PO-ratified; no invented identifiers.
- Record durable audit events through the existing audit substrate where the contract requires it.

TEST REQUIREMENTS
- Implement exactly the contract's static, pure, runtime, migration and security tests,
  including ALLOW and DENY cases (cross-tenant, PE, Viewer).
- Runtime tests must fail loudly, not silently skip, when the contract's declared environment
  is not provisioned — a SKIPPED suite is NOT a PASS.
- Amend, never delete, existing tests (the B2-D3 / §20.6 analogue applies).

INDEPENDENT VERIFICATION
Cline must not self-certify acceptance. The implementation report states
"implemented, not independently verified"; verification is a separate task (`…033`).

REPORTING
Create: docs/cline/reports/CT-P8-B3-IMPLEMENTATION-20260913-032.md
The report must include: task identity; scope; files created/changed; migrations; seeds; API
changes; frontend changes; tests and their results; the environment and its provisioned state;
runtime verification (or an explicit statement that it was not performed); security verification;
worktree state before/after; remaining limitations; verdict.

STOP CONDITIONS
Stop immediately if:
- the B3 contract does not exist, is not ratified, or its scope is ambiguous;
- a required decision is not ratified;
- implementation would require changing the calculation engine, factor data, evidence
  classification, or a B1/B2 migration;
- the target environment does not contain B1+B2 schema;
- the worktree contains unrelated changes that would be affected.

DO NOT
- expand scope; modify unrelated work; alter unrelated migrations; resolve unrelated findings;
- commit; push; deploy;
unless explicitly authorized by the applicable governance decision.

FINAL STATUS
Return: B3 IMPLEMENTATION COMPLETE — READY FOR INDEPENDENT VERIFICATION  (or) BLOCKED — <reason>
============================================================
```

---

## 7. B3 — COPY/PASTE CLINE INSTRUCTION (INDEPENDENT VERIFICATION)

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-B3-INDEPENDENT-VERIFICATION-20260913-033
STATUS: PLANNED — NOT YET AUTHORIZED
============================================================

IMPLEMENTATION AUTHORIZATION GATE
This instruction is a prepared instruction only. Cline MUST NOT execute it unless the PO has
separately and explicitly authorized CT-P8-B3-INDEPENDENT-VERIFICATION-20260913-033.

READ FIRST
- docs/architecture/CARBONTALLY_PHASE8_B3_IMPLEMENTATION_CONTRACT_20260913.md
- docs/cline/reports/CT-P8-B3-IMPLEMENTATION-20260913-032.md  (the report, not the code)
- The actual implementation code, migrations and tests (the report is not evidence)
- CT-P8-B2-INDEPENDENT-VERIFICATION-20260913-025.md as the methodological precedent
- AGENTS.md §44–§45, §72–§75

CURRENT BASELINE
- B3 implemented under `…032`, declared complete, not independently verified.
- Environment and git state must be captured before verification begins.

OBJECTIVE
Independently establish whether B3 satisfies its ratified contract, using the verifier's own
probes, clone(s) and queries — not the implementer's narrative.

IN SCOPE
- Contract/ratification reconciliation (every clause mapped to evidence).
- Independent inspection of the implementation artefacts.
- Independent database verification on a disposable privileges-inclusive clone:
  migration apply + re-apply (rc=0, zero drift), declared delta only, parity on pre-existing objects.
- Independent runtime probes for each acceptance criterion and every ALLOW/DENY security case.
- Provenance verification: projected values resolve to real snapshots/evidence lines; no fabrication.
- Scope-boundary verification: no B4/P1/P2/RLS/Phase 8-X/Insight object or behaviour introduced.
- Migration and historical-data safety (non-destructive, idempotent, no silent rewrite).

OUT OF SCOPE
- Fixing anything found; changing code, migrations, tests or data; committing; pushing; deploying.
- Re-litigating ratified decisions.

DEPENDENCIES
- `…032` complete; B1+B2 available (clone or environment) for the prerequisite schema.

IMPLEMENTATION REQUIREMENTS
- Author your own probes; do not reuse the implementer's test file as the sole evidence.
- Record exact commands, outputs, hashes, Git SHA and timestamps.
- Where a probe fails for a probe-design reason, correct the probe and re-record both attempts.

TEST REQUIREMENTS
- State, per criterion, PASS / FAIL / SKIPPED / BLOCKED / UNVERIFIED. Never report a
  SKIPPED suite as PASS.

INDEPENDENT VERIFICATION
This task IS the independent verification. The verifier must not implement, correct or improve
anything, and must not accept the implementation report's claims without executing evidence.

REPORTING
Create: docs/cline/reports/CT-P8-B3-INDEPENDENT-VERIFICATION-20260913-033.md
The report must include: environment; Git SHA; exact probes and results; database evidence;
security ALLOW/DENY results; findings with severity and reproduction; non-blocking observations;
scope-boundary evidence; remaining unknowns; verdict.

STOP CONDITIONS
Stop immediately if:
- the environment cannot be safely provisioned or is shared/non-disposable;
- verification would require mutating production or the investor-demo database;
- a finding would need a fix to be verified (record it, stop, report).

FINAL STATUS
Return: B3 INDEPENDENT VERIFICATION PASS — READY FOR PO CLOSURE  (or)
        B3 INDEPENDENT VERIFICATION FAIL — <findings>  (or) BLOCKED — <reason>
============================================================
```


---

## 8. B4 readiness assessment

### 8.1 Authoritative scope

| Source | Definition of B4 |
|---|---|
| Roadmap §20 **[D]** | *B4 — Narrative + frozen artefact + finalisation*: narrative overlay bound to requirements; DRAFT-only; validation; role enforcement; frozen export artefact (hash/metadata); `DM-5` finalisation gate. Prerequisite: **B1, B3**. Must **not** include: new report engine; report-wide editing; comments table; generic rules. Gate **V4**. |
| B2 contract §14.1 **[D]** | B4 owns narrative / finalisation / frozen artefact; B2 must not touch it. |
| Decision Record **[D]** | `A1` (narrative allowlist — requirement-bound only; Owner/Admin authoring), `A3` (no arbitrary character limits; typed fields; evidence-backed limits only), `P3` (customer-editable boundary — never calculations/provenance/factors), `DM-5` (finalisation; `NOT_SUPPORTED` vs `CUSTOMER_INPUT_REQUIRED`), `DM-6` (drill-down), `D15` (historical reproducibility). |
| Lifecycle spec §33/§34 **[D]/[U]** | A further set of narrative/finalisation/approval policy inputs (`A1/A2/A3/A4…A15`, `P3`–`P8`) marked PO DECISION REQUIRED **in that document**; several of these were subsequently ratified in the Disclosure Model Decision Record (see §24 C-4). |

### 8.2 State

| Element | State |
|---|---|
| B4 contract / decision record / authorisation | **NONE EXIST** **[R]** |
| B4 implementation | **NONE**; no `narrative_overlay` column, no narrative store, no frozen artefact table, no `final_report_url` producer **[R]** (S4 report §2) |
| S4 narrative overlay | **BLOCKED — HARD STOP** (`S4 IMPLEMENTATION BLOCKED — PO/ARCHITECTURE REVIEW REQUIRED`), on the stated grounds that the allowlist was `PO DECISION REQUIRED` **[D]** |
| S5 frozen PDF | Designed only; storage/hashing/retention plan **NOT DEFINED** (lifecycle spec §37.2) **[D]** |
| Batch allocation of S5/S6/S7 and of the unchanged S-series stages | **UNRECORDED** — the B-series §20 table covers narrative + frozen artefact + finalisation, but not the S6 UI or S7 regression/security tests **[R]** |

### 8.3 Decisions required before B4 implementation (topics)

| # | Decision topic |
|---|---|
| **B4-PO-1** | Confirm that B4 **absorbs S4 (narrative) and S5 (frozen artefact)** and state their acceptance criteria under gate V4 |
| **B4-PO-2** | Confirm the allocation of **S6 (frontend lifecycle UI)** and **S7 (regression + negative security tests)** — inside B4, inside B4's verification, or a separate batch |
| **B4-PO-3** | Frozen artefact model: storage location/bucket, naming, hashing algorithm(s), immutability enforcement, and whether the legacy live-render PDF route is retained for drafts (`A15`) |
| **B4-PO-4** | Approval model for finalisation: who approves (`P4`: Owner only vs Owner+Admin), post-approval edit invalidation (`P5`), revocation (`A6`), staff/consultant approval (`A2`), review gating (`A4`/`A5`), comment visibility (`A7`) |
| **B4-PO-5** | Retention/deletion rules for report versions and frozen artefacts (`A10`) — must be consistent with N3 and never weaken auditability |
| **B4-PO-6** | Download audit (`A11`), staleness flagging and “data as of” (`A12`), drill-down exposure (`A13`) |
| **B4-PO-7** | Entitlement/billing gating of finalisation (`A14`) if any |
| **B4-PO-8** | Narrative strategy: deterministic/template-first only (`P8`) — AI narrative remains S8 and out of B4 |
| **B4-PO-9** | Confirm whether `report_comments` (dormant) is activated in B4 or deferred |

### 8.4 Verdict

### `B4 NOT READY — PO DECISIONS REQUIRED`

**Basis:** B4 has no contract, depends on B3 (which is itself not ready), and its policy inputs are a mixture of ratified Disclosure Model decisions and still-open lifecycle-spec decisions. **This plan does not authorise B4.**

---

## 9. B4 — COPY/PASTE CLINE INSTRUCTION (STAGE 1: CONTRACT + PO DECISIONS)

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-B4-CONTRACT-AND-PO-DECISIONS-20260913-034
STATUS: PLANNED — NOT YET AUTHORIZED
TYPE: CONTRACT / DECISION PREPARATION ONLY (no implementation)
============================================================

IMPLEMENTATION AUTHORIZATION GATE
This instruction is a prepared instruction only. Cline MUST NOT execute it unless the PO has
separately and explicitly authorized CT-P8-B4-CONTRACT-AND-PO-DECISIONS-20260913-034.

READ FIRST
- docs/architecture/CARBONTALLY_PHASE8_REPORTING_LIFECYCLE_SPEC_20260912.md (§10, §15, §17–§20, §21–§28, §33–§35, §37)
- docs/architecture/CARBONTALLY_PHASE8_DISCLOSURE_MODEL_DECISION_RECORD_20260912.md (A1, A3, P3, DM-5, DM-6)
- docs/architecture/CARBONTALLY_PHASE8_P8X_COMPREHENSIVE_IMPLEMENTATION_READINESS_20260912.md (§20 B4 row; §8 gaps G-10..G-12)
- docs/cline/reports/CT-P8-REPORTING-S4-NARRATIVE-OVERLAY-20260912-001.md (the hard stop and its exact reasons)
- docs/architecture/CARBONTALLY_PHASE8_B3_IMPLEMENTATION_CONTRACT_20260913.md (once it exists)
- AGENTS.md

CURRENT BASELINE
- Report lifecycle S1/S3 implemented (S3 applied locally); narrative absent; frozen artefact absent.
- No B4 contract, decision record or authorisation exists.

OBJECTIVE
Produce the complete B4 implementation contract and the `B4-Dn` decision register, resolving the
S4 hard stop explicitly (state whether each hard-stop condition is now satisfied by a ratified
decision, and which decision satisfies it).

IN SCOPE
1. Verified current state of the report lifecycle, narrative absence and frozen-artefact absence.
2. Exact B4 deliverables and gate-V4 acceptance criteria.
3. The narrative data model (requirement-bound; DRAFT-only), its API boundary and role enforcement.
4. The frozen-artefact model (storage, hash, metadata, immutability) — or an explicit
   [DECISION REQUIRED] where policy is absent.
5. The `DM-5` finalisation gate and its interaction with report version states.
6. The B4↔B3 interface and the drill-down boundary (`DM-6`).
7. Retention/deletion rules consistent with N3 and auditability.
8. The test/verification contract for gate V4 (including ALLOW/DENY and immutability tests).
9. Formal `B4-Dn` entries for every topic in §8.3, each with a recommendation and rationale.

OUT OF SCOPE
- Any implementation, migration, storage change, API, frontend, AI narrative, RLS or data change.
- Activation of `report_comments` unless the PO decides it.
- S8 (AI-assisted narrative) in any form.

DEPENDENCIES
- B3 closed (V3 PASS + PO closure), because B4 binds narrative to B3 requirements.
- Ratified B3 contract for the requirement interface.

IMPLEMENTATION REQUIREMENTS
- Two documents only: the B4 contract under docs/architecture/ and the mandatory report under
  docs/cline/reports/.
- Every normative statement traceable to a ratified decision or repository evidence.

TEST REQUIREMENTS
- None (no code); instead a completeness table mapping every §8.3 topic to a `B4-Dn` entry and
every S4 hard-stop condition to its resolution.

INDEPENDENT VERIFICATION
Contract task; the later implementation is verified separately (`…036`). Do not self-certify.

REPORTING
Create: docs/cline/reports/CT-P8-B4-CONTRACT-AND-PO-DECISIONS-20260913-034.md
Required contents: identity; sources; verified state; contract summary; `B4-Dn` register;
S4 hard-stop resolution table; conflicts; files created; worktree before/after; no-implementation statement; verdict.

STOP CONDITIONS
Stop immediately if: a required policy input has no ratified basis and cannot be expressed as
[DECISION REQUIRED]; the contract would require inventing retention/storage policy; the B3
contract is absent when the B4↔B3 interface must be specified.

FINAL STATUS
Return: B4 CONTRACT COMPLETE — READY FOR PO DECISIONS (or) BLOCKED — <reason>
============================================================
```


---

## 10. B4 — COPY/PASTE CLINE INSTRUCTION (INDEPENDENT VERIFICATION)

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-B4-INDEPENDENT-VERIFICATION-20260913-036
STATUS: PLANNED — NOT YET AUTHORIZED
============================================================

IMPLEMENTATION AUTHORIZATION GATE
Prepared instruction only. Cline MUST NOT execute it unless the PO has separately and
explicitly authorized CT-P8-B4-INDEPENDENT-VERIFICATION-20260913-036.

READ FIRST
- docs/architecture/CARBONTALLY_PHASE8_B4_IMPLEMENTATION_CONTRACT_20260913.md (ratified)
- docs/cline/reports/CT-P8-B4-IMPLEMENTATION-20260913-035.md (report, not code)
- The implementation code, migrations and tests themselves
- CT-P8-B2-INDEPENDENT-VERIFICATION-20260913-025.md (methodological precedent)
- AGENTS.md §44–§45, §72–§75

CURRENT BASELINE
- B4 implemented under `…035`; environment and Git state captured before verification.

OBJECTIVE
Independently establish whether B4 satisfies its ratified contract, with particular emphasis on
immutability, authorization and the honesty of narrative/finalisation behaviour.

IN SCOPE
- Clause-by-clause contract reconciliation against executed evidence.
- Disposable privileges-inclusive clone: migration apply/re-apply, declared delta only, parity.
- Runtime probes for every acceptance criterion, including: narrative write denied to non-permitted
  roles; narrative never writable outside a requirement binding; frozen artefact immutability and
  hash reproducibility; no post-approval silent rewrite; `NOT_SUPPORTED` vs `CUSTOMER_INPUT_REQUIRED`
  preserved; cross-tenant and PE denial.
- Provenance: every rendered figure resolves to persisted snapshots/evidence; no fabricated values.
- Scope-boundary verification: no B3/P1/P2/RLS/Phase 8-X/Insight/legacy change.

OUT OF SCOPE
- Fixing findings; changing anything; committing; pushing; deploying; re-litigating decisions.

TEST REQUIREMENTS
- PASS / FAIL / SKIPPED / BLOCKED / UNVERIFIED per criterion; a SKIPPED suite is never a PASS.

REPORTING
Create: docs/cline/reports/CT-P8-B4-INDEPENDENT-VERIFICATION-20260913-036.md
Required contents: environment; Git SHA; probes and results; database and storage evidence;
security ALLOW/DENY results; findings with severity, reproduction and impact; non-blocking
observations; scope-boundary evidence; remaining unknowns; verdict.

STOP CONDITIONS
Stop if the environment is not disposable; if verification would mutate production or the
investor-demo database; or if a finding requires a fix before it can be verified.

FINAL STATUS
Return: B4 INDEPENDENT VERIFICATION PASS — READY FOR PO CLOSURE (or) FAIL — <findings> (or) BLOCKED — <reason>
============================================================
```

---

## 11. B1/B2 controlled application to non-production (and the production boundary)

### 11.1 What the records already settle **[D]**

| Question | Ratified answer | Source |
|---|---|---|
| Which environment first? | A **dev/QA** environment (application on a non-production database). | `PQ-5` — *“Deployment gate; dev/QA allowed; production gated”* |
| Is B1 required before B2? | **Yes** — B2-1 reuses B1's helper `p8_disclosure_is_org_member`; B2-2 depends on B1's `disclosure_value_evidence`; filename order `20260914/15` → `20260916`. | B2 contract §18.2 |
| Migration order | `20260914000000` → `20260915000000` → `20260916000000` → `20260916010000`, then **re-apply both B2** for idempotency. | B2 contract §18.4 |
| Preflight checks | Restorable snapshot exists; environment is disposable/QA; `conftest.py` refuses the main app DB (D31); the B1 tie-break index `uq_dve_reference_nullsafe` needs a documented dedupe if the target has duplicate historical rows (V1R R4). | §18.4 item 1; B2 contract §3; V1R §36/R4 |
| Verification after application | B1 runtime suite + **B2 runtime suite (V2)** must actually execute (not skip); evidence line rows/columns present; parity on pre-existing objects. | §18.4 item 5; §20.7 |
| Backfill | **Only if separately authorised** — dry-run, review the run report, then execute (never as part of application). | §18.4 item 6 |
| Production | **Not authorised.** 34 pre-existing outstanding migrations + B2's 2; production stays prohibited while **G0-D** is open. | §18.4 item 7; `-019` §18; roadmap §25 rows 10–11 |
| Rollback | Drop the two additive columns (+FKs/indexes) then `DROP TABLE evidence_line_items`; nothing pre-existing was rewritten. | B2 contract §19 |

### 11.2 Questions the records do **not** settle (flag for the PO/engineering)

1. **Which specific QA environment** is the target (a new dedicated QA database vs the existing `carbontally_test`, which has only 114 tables and **no** B1/B2 schema and was explicitly judged unsuitable as the clone in `-019` O-2)? **[U]** — recommend a **new dedicated QA database** restored production-shaped, leaving `carbontally_test` as-is for integration tests.
2. **Should the QA environment be persistent** (so B3/B4 development and runtime suites have a stable target), or a disposable clone re-created per task? **[U]** — recommend **persistent QA**, with disposable clones retained for independent verification.
3. **Ordering relative to B3**: should application happen **before** B3 implementation? **[REC]** — **yes**: B3 needs the schema to build and runtime-test against, and until then every B3/B1/B2 runtime suite would skip. This is a sequencing recommendation, not a ratified order; it needs PO acknowledgement (R2 precedes `…032`).
4. **Whether the B2 backfill runs in QA at all** (it is separately authorised per §18.4 item 6) — **[REC]** run the **dry-run** in QA only, and treat execution as a separate decision.
5. **Whether B2's worktree artefacts are committed** before or after QA application (R3) — a repository-discipline decision (AGENTS.md §70) **[U]**.

### 11.3 Production boundary (unchanged)

Production application of B1/B2 (and later B3/B4) remains **PROHIBITED** until **G0-D** is resolved and a migration strategy is independently verified (`PQ-5`, `-019` §18, AGENTS.md §66). The RLS security hold is a **separate** gate and is not satisfied by applying B1/B2.


### 11.4 COPY/PASTE CLINE INSTRUCTION — controlled non-production application

```text
============================================================
COPY/PASTE CLINE OPERATIONAL INSTRUCTION
TASK: CT-P8-B1B2-ENV-APPLICATION-20260913-029
STATUS: PLANNED — NOT YET AUTHORIZED
============================================================

IMPLEMENTATION AUTHORIZATION GATE
This instruction is a prepared instruction only. Cline MUST NOT execute it unless the PO has
separately and explicitly authorized CT-P8-B1B2-ENV-APPLICATION-20260913-029 **and** named the
target non-production environment.

READ FIRST
- docs/architecture/CARBONTALLY_PHASE8_B2_IMPLEMENTATION_CONTRACT_20260913.md §18.2–§18.5, §19
- docs/architecture/CARBONTALLY_PHASE8_B1_IMPLEMENTATION_CONTRACT_20260912.md §22, §26
- docs/cline/reports/CT-P8-B1-V1R-INDEPENDENT-RE-VERIFICATION-20260913-016.md §36, §37 (R4 dedupe precondition), §38
- docs/cline/reports/CT-P8-B2-INDEPENDENT-VERIFICATION-20260913-025.md (§13.1 skipped suites; §15)
- docs/cline/reports/CT-P8-B2-PO-IMPLEMENTATION-AUTHORISATION-20260913-019.md §17, §18
- AGENTS.md §55 (demo safety), §66 (database change policy), §70

CURRENT BASELINE
- B1 and B2 are implemented and verified but applied in **no** persistent environment.
- Targeted: the PO-named non-production (dev/QA) database only. Production is explicitly out of scope.
  The investor-demo database must not be used.

OBJECTIVE
Apply B1 then B2 to the named non-production environment, prove idempotency and parity, and make the
B1/B2 runtime suites execute for the first time in a persistent environment.

IN SCOPE
1. Record the environment(s) inspected and the target database identity.
2. Prove a restorable snapshot/backup exists and is usable before any DDL.
3. Preflight: confirm the target is not production, not the investor-demo DB, and that `conftest.py`
   guards are intact; confirm the B1 unique-index precondition (duplicate historical rows) or record a dedupe plan.
4. Apply `20260914000000_p8_b1_disclosure_model_foundation.sql` → rc=0.
5. Apply `20260915000000_p8_b1_correction_privileges_and_evidence_idempotency.sql` → rc=0.
6. Apply `20260916000000_p8_b2_evidence_line_items.sql` → rc=0.
7. Apply `20260916010000_p8_b2_provenance_line_links.sql` → rc=0.
8. Re-apply steps 4–7 → rc=0 with zero schema/policy diff (idempotency proof).
9. Run the B1 runtime suite and the B2 runtime suite (V2) and report their real results
   (executed, not skipped).
10. Record the post-application inventory (new relations/columns/indexes/policies) and the
    before/after parity of pre-existing objects.

OUT OF SCOPE
- Production in any form.
- The Class-1 backfill: **dry-run only if the PO authorises it**; executing the backfill is a
  separate authorisation (B2 contract §18.4 item 6).
- Any code, migration-text, test or RLS-policy change; any new migration.
- Any change to B3/B4/P1/P2/Phase 8-X/Insight surface.

DEPENDENCIES
- PO authorisation + named environment; `PQ-5` already permits dev/QA.
- Restorable snapshot; the B2 closure record (`…028`) for lineage.

IMPLEMENTATION REQUIREMENTS
- Use the repository's existing migration mechanism; do not hand-edit schema.
- Never disable or bypass RLS to make an application succeed.
- Capture exact commands and outputs; capture the Git SHA of the tree used.
- If any step fails or diverges from the declared delta, stop and report rather than improvising.

TEST REQUIREMENTS
- B1 runtime suite: executed result with counts.
- B2 runtime suite (V2): executed result with counts.
- Idempotency: two apply passes, rc=0, zero diff.
- Parity: pre-existing objects unchanged (grants/policies/RLS flags).

INDEPENDENT VERIFICATION
This task performs an operational step; the resulting environment state is subject to a separate
independent verification if the PO requires fresh evidence (`…033`/clone-based re-verification).
Cline must not claim acceptance of B1/B2 on the basis of this task alone.

REPORTING
Create: docs/cline/reports/CT-P8-B1B2-ENV-APPLICATION-20260913-029.md
Required contents: target environment identity; snapshot evidence; preflight results; exact
migrations applied with rc; idempotency evidence; post-application inventory; before/after parity;
runtime suite results (executed vs skipped) with counts; Git SHA; worktree state; rollback plan;
remaining limitations; verdict.

STOP CONDITIONS
Stop immediately if:
- the PO has not named the target environment or the target is production/investor-demo;
- no restorable snapshot is available;
- a migration returns a non-zero rc, or produces a delta beyond the declared objects;
- the B1 unique index fails on duplicate historical data and no authorised dedupe exists;
- any step would require disabling RLS, bypassing authorization, or rewriting historical rows.

DO NOT
- touch production; touch the investor-demo database; run the backfill; alter migrations; commit; push;
deploy to production.

FINAL STATUS
Return: B1/B2 APPLIED TO <ENV> — RUNTIME SUITES EXECUTED (or) BLOCKED — <reason>
============================================================
```

---

## 12. P1 status and next action

### 12.1 State **[R]/[D]**

| Element | State |
|---|---|
| Contract | **COMPLETE** — `CARBONTALLY_PHASE8_P1_PDF_IMAGE_EXTRACTION_REMEDIATION_CONTRACT_20260913.md` + report `-020`; verdict `P1 FORENSIC + REMEDIATION CONTRACT COMPLETE — READY FOR PO REVIEW` |
| Root boundary | P1 = **extraction fidelity** only; **not** evidence addressability (B2), **not** factor matching (P2), **not** calculation/reporting/UI |
| PO authorisation | **NOT AUTHORISED**; **7 decisions open** (`P1-D1…P1-D8`) |
| Implementation | none; `automatic_extraction.py`, `extraction_suggestions.py`, `ai_document_extraction.py`, `v3_processing_workflow.py` unmodified |
| Dependency on B2 | **None** — B2 declared the optional per-line interface; flat PDF/IMAGE records simply yield no line rows; P1 is recorded as **not a blocker** for B2. Conversely **B2 must not be used to fix extraction** (`B2-D4`) |
| Ownership of `source_page` semantics | **Not P1** (P1 contract §9.3); a separate evidence/provenance workstream (`P1-D5`) |
| Critical-path status | **Parallel remediation track**, not a Phase 8 critical-path blocker for B3/B4 |

### 12.2 Next action

**PO decision package for `P1-D1…P1-D8`** (prepared by `…037`), then — only if authorised — a separate P1 implementation batch with its own contract-fixed scope, followed by its own independent verification (`VP`). **P1 must not be bundled with P2** **[D]**.

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-P1-AUTHORISATION-PREPARATION-20260913-037
STATUS: PLANNED — NOT YET AUTHORIZED
TYPE: DECISION PREPARATION (no implementation)
============================================================

IMPLEMENTATION AUTHORIZATION GATE
Prepared instruction only. Cline MUST NOT execute it unless the PO has separately and explicitly
authorized CT-P8-P1-AUTHORISATION-PREPARATION-20260913-037.

READ FIRST
- docs/architecture/CARBONTALLY_PHASE8_P1_PDF_IMAGE_EXTRACTION_REMEDIATION_CONTRACT_20260913.md (§7, §13, §16, §18, §19, §20)
- docs/cline/reports/CT-P8-P1-PDF-IMAGE-EXTRACTION-FORENSIC-ARCHITECTURE-20260913-020.md
- docs/cline/reports/CT-P8-LINE-ITEM-TRACEABILITY-FORENSIC-20260912.md
- docs/cline/reports/CT-P8-B2-IMPLEMENTATION-20260913-024.md and ...-025.md (interface only)
- AGENTS.md

CURRENT BASELINE
- P1 contract complete and unauthorised; `P1-D1…P1-D8` open; no extraction code modified.
- B2 is implemented and closed (per prompt); B2 does not implement P1 and must not be used for it.

OBJECTIVE
Produce a PO decision package that turns `P1-D1…P1-D8` into formal, decision-ready items (recommendation,
rationale, consequence of deferral, affected roles/workflows, cost implications), and a bounded
implementation scope statement that can become a P1 authorisation.

IN SCOPE
- Restate each open decision verbatim with its contract context; add the recommendation and the
  consequence of *not* deciding (including which failure modes persist, e.g. FM-4).
- State the exact bounded implementation boundary (contract §19) and the test/verification contract (§16).
- State explicitly how P1 remains separate from P2, RLS, B3/B4 and Phase 8-X.

OUT OF SCOPE
- Any implementation, code, migration, OCR/AI prompt, frontend, data or configuration change.
- Any historical re-extraction (prohibited without separate authorisation).

TEST REQUIREMENTS
- None; instead a decision-completeness table (every contract `P1-Dn` → decision entry).

INDEPENDENT VERIFICATION
Not applicable to a decision package; the later implementation is verified separately (gate VP).

REPORTING
Create: docs/cline/reports/CT-P8-P1-AUTHORISATION-PREPARATION-20260913-037.md
Required contents: identity; sources; verified state; the eight decision entries; scope statement;
boundaries; worktree state; no-implementation statement; verdict.

STOP CONDITIONS
Stop if the contract contradicts itself on scope or if a decision would require a PO policy choice
that cannot be expressed as an option set.

FINAL STATUS
Return: P1 DECISION PACKAGE READY FOR PO (or) BLOCKED — <reason>
============================================================
```


---

## 13. P2 / EF-E status and next action

### 13.1 State **[R]/[D]**

| Element | State |
|---|---|
| Forensic | **FORMALIZED** — `CT-P8-EMISSION-FACTOR-MATCHING-FORENSIC-20260912.md`: the mapping picker uses **direct SQL**, not the `FactorMatchingEngine`; **EF-E is a confirmed, independent picker defect** (exact-unit equality on one surface excludes eligible qualified-unit factors); three mapping-options surfaces have inconsistent unit semantics |
| Implementation contract | **NONE** (the forensic recommends, but does not authorise, passing `unit_substring=True` at `v3_processing_workflow.py:1113`) |
| PO authorisation | **NOT AUTHORISED**; no factor-data change authorised |
| Blocking evidence gap | The **factor-catalogue census** needs an **authorised read-only DB session** (`EF-A`/`EF-D` remain undecidable without it) |
| Dependency on B3/B4 | **None** — different root cause, different blast radius; must remain a **separate** batch and must not be bundled with P1 |

### 13.2 Next action

Authorise a **read-only** factor-catalogue census (`…038`) so the defect's scope (how many picker pairs are affected) is quantified before any PO decision on remediation; the remediation then becomes its own bounded batch with its own independent verification.

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-P2-FACTOR-CATALOGUE-CENSUS-20260913-038
STATUS: PLANNED — NOT YET AUTHORIZED
TYPE: FORENSIC FOLLOW-UP (read-only)
============================================================

IMPLEMENTATION AUTHORIZATION GATE
Prepared instruction only. Cline MUST NOT execute it unless the PO has separately and explicitly
authorized CT-P8-P2-FACTOR-CATALOGUE-CENSUS-20260913-038 **and** authorised the read-only database
session it requires.

READ FIRST
- docs/cline/reports/CT-P8-EMISSION-FACTOR-MATCHING-FORENSIC-20260912.md (§4, §9–§13)
- backend/data/emission_factors.py (find_by_activity), backend/api/v3_operations.py, backend/api/v3_processing_workflow.py
- backend/core/units.py (UNIT_ALIASES, normalize_unit, units_equivalent)
- AGENTS.md §68 (storage), §78 (logging), §55 (demo safety)

CURRENT BASELINE
- EF-E confirmed by static evidence; live catalogue vocabulary **UNVERIFIED**; no factor data may change.

OBJECTIVE
Perform a read-only census of the factor catalogue and picker behaviour to quantify EF-E and separate it
from genuine coverage gaps (EF-A) and unit-normalisation gaps (EF-D).

IN SCOPE
- Read-only `SELECT` queries against the factor catalogue and the mapping-option query paths.
- Counts: factors per activity/unit; exact vs substring matching differences; qualified-unit factors
  excluded by exact-unit equality; coverage of the extraction label vocabulary.
- A structured findings table with the exact queries used.

OUT OF SCOPE
- Any write of any kind; any factor-data change; any code change; any new migration.
- Remediation (that is a separate authorised batch).
- Use of production credentials if a read-only replica/role is not available.

DEPENDENCIES
- PO authorisation of the read-only session (RLS decision D-10 relates to the mechanism).

IMPLEMENTATION REQUIREMENTS
- Read-only only. No `INSERT`/`UPDATE`/`DELETE`/DDL. No schema change.
- Do not print or store credentials, connection strings or signed URLs.
- Record the exact database and Git SHA used.

TEST REQUIREMENTS
- Not applicable; the output is evidence, not code.

INDEPENDENT VERIFICATION
A second reviewer (or a separate verification task) should re-run a sample of the queries.

REPORTING
Create: docs/cline/reports/CT-P8-P2-FACTOR-CATALOGUE-CENSUS-20260913-038.md
Required contents: environment; read-only statement; queries; results; EF-E quantification;
EF-A/EF-D separation; coverage findings; limitations; recommended bounded remediation options; verdict.

STOP CONDITIONS
Stop if no authorised read-only access exists, or if the session would require write privileges.

FINAL STATUS
Return: P2 CENSUS COMPLETE — READY FOR PO DECISION (or) BLOCKED — NO AUTHORISED READ-ONLY ACCESS
============================================================
```

---

## 14. RLS security remediation — status and next action

### 14.1 Exact current hold **[D]/[R]**

* The remediation gate is **held** and requires **explicit PO authorisation** to reopen (hold register §10). It is **not** authorised by the passage of time, by Phase 8 completion, or by Phase 8-X completion.
* Verified production baseline: **97 of 104 tables with RLS disabled**, `anon` holds `GRANT ALL`, 7 functions with `EXECUTE` questions, 12 production-missing tables, 34 outstanding migrations.
* Remediation sequence recorded: `RLS-4A → RLS-5 → RLS-3 → RLS-2 → RLS-1 → verification`, with an **unresolved internal discrepancy** (`§6.1`) and a **batching prohibition** (`§7.4`).
* **13 decisions still required** (`Q1/D-1 … Q12/D-12`, plus `D-13` sequence confirmation) — see hold register §8.
* RLS remediation is **not** a Phase 8-X deliverable, and Phase 8-X is not part of RLS remediation **[D]**.

### 14.2 Report-table RLS (`A-RLS`) is a **separate but related** open decision

`A-RLS` (complete policy coverage **vs** documented app-layer-only posture for the two un-policied report tables) **blocks S2 only** and remains **undecided**; S3 proceeded with “NO RLS change” **[D]/[R]**. It is **not** the production RLS hold, and must not be conflated with it.

### 14.3 Does RLS block B3/B4?

**No.** B1/B2 added RLS-enabled objects with their own policies and touched no pre-existing policy/grant/RLS flag (independently verified in V2 §10). B3/B4 must follow the same pattern (RLS on new objects; no remediation of the production baseline). The **production deployment** of any of these remains blocked by G0-D, which is a *deployment* gate, not an RLS decision **[I]/[D]**.

### 14.4 Can any RLS work proceed independently?

Only if the PO **explicitly reopens** the gate; the first authorised step is expected to be `RLS-4A-1` (anonymous grant containment), which needs no RLS change and is independently verifiable **[D]**. Recommended preparation: a **re-verification of the §4 production snapshot** (stale-risk) plus a resolution of the §6.1 sequence discrepancy — both are PO/engineering decisions, not implementation.

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-RLS-SECURITY-REMEDIATION-PLAN-20260913-039
STATUS: PLANNED — NOT YET AUTHORIZED
TYPE: PLANNING / DECISION PREPARATION (no remediation)
============================================================

IMPLEMENTATION AUTHORIZATION GATE
Prepared instruction only. Cline MUST NOT execute it unless the PO has separately and explicitly
authorized CT-P8-RLS-SECURITY-REMEDIATION-PLAN-20260913-039.

READ FIRST
- docs/architecture/CARBONTALLY_PRODUCTION_RLS_SECURITY_HOLD_AND_REMEDIATION_REGISTER_20260912.md (§2–§11)
- docs/architecture/CARBONTALLY_RLS_PRODUCTION_BASELINE_AND_REMEDIATION_SPEC_20260912.md
- docs/architecture/CARBONTALLY_PHASE8_OPEN_DECISION_CLOSURE_AND_IMPLEMENTATION_AUTHORISATION_20260912.md (`A-RLS`, L979, L1080, L1165)
- AGENTS.md §44, §45, §67

CURRENT BASELINE
- Gate HELD; no remediation applied anywhere; production baseline recorded but possibly stale.
- `A-RLS` undecided; S3 shipped without RLS change.

OBJECTIVE
Produce a decision-ready reopening package: what must be re-verified, the exact ordered steps with
blast radius, the 13 pending decisions as formal entries, and the separation rules (RLS ≠ Phase 8-X
≠ A-RLS ≠ deployment backlog).

IN SCOPE
- Restate the baseline, the sequence discrepancy and the batching prohibition.
- Formal decision entries for `D-1…D-13` with recommendation, risk of deferral and verification method.
- A proposed first authorisable step (`RLS-4A-1`) with its own preflight, verification and rollback.
- An explicit re-verification plan for the production baseline snapshot.

OUT OF SCOPE
- Any RLS, grant, policy, function or schema change; any production access change.
- Bundling with Phase 8-X or with product work.

TEST REQUIREMENTS
- None; decision-completeness table instead.

INDEPENDENT VERIFICATION
Not applicable to a planning package; each future remediation step must be independently verified
before the next step begins.

REPORTING
Create: docs/cline/reports/CT-P8-RLS-SECURITY-REMEDIATION-PLAN-20260913-039.md
Required contents: current hold; baseline; decisions register; sequenced plan; separation rules;
no-change statement; verdict.

STOP CONDITIONS
Stop if the plan would require modifying production to be written, or if the PO has not reopened the gate.

FINAL STATUS
Return: RLS REOPENING PACKAGE READY FOR PO (or) BLOCKED — GATE HELD
============================================================
```


---

## 15. Phase 8-X status

### 15.1 Definition and structure **[D]**

Phase 8-X — **Operational Intelligence** is defined by the discovery + OHD companion as a **bounded extension/workstream of Phase 8, not a new Phase 9**, providing operational visibility over already-persisted signal. Its capabilities are classified **MUST (M1–M6) / SHOULD (S1–S6) / LATER (L1–L7) / OUT OF SCOPE**, and are delivered in **stages X1–X8**:

| Stage | Content | Prerequisite decisions |
|---|---|---|
| **X1** | Foundation and baseline (**no schema change**) | — |
| **X2** | Worker & queue operational visibility (**M1**) | — |
| **X3** | Health correctness & worker liveness (**M2**) | **PX-5** (heartbeat schema vs reuse-only) |
| **X4** | Aggregation layer: failures, SLA, usage, imports, delivery, AI (**M3, S3–S6**) | — |
| **X5** | Operations console extension (**M4**) | — |
| **X6** | Thresholds & alerting (**S1**) | **PX-6** (recipients/channels/thresholds) |
| **X7** | API runtime metrics (**S2**) | **PX-7** (retention) |
| **X8** | Testing, security verification, operational verification | **UNDEFINED as an artefact — no X8 document exists** |

**Grouping used by the readiness roadmap:** batch **X1 = stages X1–X3**, batch **X2 = stages X4–X7**; gates **VX1** and **VX2 (conditional)** **[D]**. Note the label collision: **X’s S1–S6 (SHOULD capabilities)** are *not* the **report-lifecycle S1–S6** — a documented hazard, not a defect **[R]**.

### 15.2 Current state

| Element | State |
|---|---|
| Discovery / architecture | **COMPLETE** (P8-X discovery §1–§20 + OHD report) **[D]** |
| PO decisions | **`PX-1…PX-12` ALL OPEN** **[D]** |
| Hard blockers | **BL-1** (no platform access: Render API/console), **BL-2** (no read-only production DB role) — these gate runtime/deployment intelligence verification **[D]** |
| Implementation | **NONE**; the ops API/console, retention, notifications and audit substrate exist and are designated **reuse** **[R]** |
| RLS relationship | Phase 8-X **must not** remediate RLS; where RLS is a dependency it is recorded as *DEPENDENCY — SEPARATE RLS SECURITY GATE* **[D]** |
| Governance | PX-1 (naming/scope) is unresolved and overlaps the Phase 9 conflict (§16) **[U]** |

### 15.3 What should NOT be implemented until the conflict is resolved

**All** X implementation. `PX-1`/the Phase 9 question determines *what Phase 8-X is extending* and whether the domain belongs to this workstream at all. X1’s *foundation* stage (documentation/no-schema baseline) is the least risky and could be authorised first **only after** the PO has confirmed the workstream’s identity.

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-P8X-X1-IMPLEMENTATION-20260913-043
STATUS: PLANNED — NOT YET AUTHORIZED
============================================================

IMPLEMENTATION AUTHORIZATION GATE
Prepared instruction only. Cline MUST NOT execute it unless the PO has separately and explicitly
authorized CT-P8-P8X-X1-IMPLEMENTATION-20260913-043 **and** resolved `PX-2` (MUST set), `PX-3`
(workstream identity) and, for stage X3, `PX-5` (heartbeat schema vs reuse-only).

READ FIRST
- docs/architecture/CARBONTALLY_PHASE8X_OPERATIONAL_INTELLIGENCE_DISCOVERY_AND_IMPLEMENTATION_BOUNDARY_20260912.md (§13 MUST/SHOULD/LATER, §15 stages X1–X3, §18 RLS relationship, §19 PX decisions)
- docs/architecture/CARBONTALLY_PHASE8_P8X_COMPREHENSIVE_IMPLEMENTATION_READINESS_20260912.md (§20 X1 row, §24)
- docs/ohd/reports/CT-P8X-DISCOVERY-OPERATIONAL-INTELLIGENCE-20260912-002.md
- AGENTS.md §31, §67, §77, §78

CURRENT BASELINE
- Phase 8-X designed and unauthorised; ~24 operational tables already persist the signal; no ops runtime endpoints exist.
- The `DATABASE_URL`/Render configuration incident (§11) is **NOT** Phase 8-X; it is a separate bounded remediation (PX-9).

OBJECTIVE
Implement stages X1–X3 (batch X1) exactly as bounded by the PO-ratified MUST set: worker/queue visibility,
health-path correctness and worker liveness, without schema change unless `PX-5` authorises a heartbeat.

IN SCOPE
- Operational read endpoints under the existing ops namespace and the existing internal-staff authorization model.
- Read models derived from existing tables; no new aggregation storage unless authorised.
- Health-path correctness: the health signal must exercise the real data path.
- Worker liveness visibility (reuse-only unless `PX-5` authorises a heartbeat migration).
- Audit/observability per the discovery’s design rules; internal-staff-only surfaces (`PX-8`).

OUT OF SCOPE
- Any RLS remediation; any production migration; any customer-visible surface.
- X4–X7 (batch X2); incident model (L1); SLOs (L2); APM procurement (L4); anomaly detection (L5); composite score (L6); `/admin` control plane (L7).
- Phase 8 product features; P1/P2; Insight; legacy reporting.

DEPENDENCIES
- `PX-2`, `PX-3`, `PX-5`, `PX-8`; **BL-1/BL-2** for any runtime/deployment *verification* claim.

IMPLEMENTATION REQUIREMENTS
- Reuse the existing ops API/console, auth helpers, audit substrate, retention service and notifications.
- Never weaken tenant isolation; never expose operational data to customers or PEs.
- Never present a proxy metric as a health determination without stating its basis.

TEST REQUIREMENTS
- Unit, API, runtime and security tests (ALLOW + DENY: customer denied, PE denied, non-staff denied).
- Failure-state tests for the health path and for a stuck/claimed queue item.

INDEPENDENT VERIFICATION
Separate task `…044` (gate VX1). Cline must not self-certify.

REPORTING
Create: docs/cline/reports/CT-P8-P8X-X1-IMPLEMENTATION-20260913-043.md with identity, scope, files,
endpoints, tests, runtime evidence, security evidence, worktree state, limitations, verdict.

STOP CONDITIONS
Stop if `PX-2`/`PX-3` are unresolved; if a stage needs a schema change not authorised by `PX-5`; if
the work would require production access; or if the Phase 9 reconciliation changes the workstream identity.

FINAL STATUS
Return: PHASE 8-X X1 IMPLEMENTATION COMPLETE — READY FOR INDEPENDENT VERIFICATION (or) BLOCKED — <reason>
============================================================
```

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-P8X-X1-INDEPENDENT-VERIFICATION-20260913-044
STATUS: PLANNED — NOT YET AUTHORIZED
============================================================

IMPLEMENTATION AUTHORIZATION GATE
Prepared instruction only. Cline MUST NOT execute it unless the PO has separately and explicitly
authorized CT-P8-P8X-X1-INDEPENDENT-VERIFICATION-20260913-044.

READ FIRST
- The ratified Phase 8-X MUST set and stage X1–X3 definitions
- docs/cline/reports/CT-P8-P8X-X1-IMPLEMENTATION-20260913-043.md (report) and the implementation itself
- AGENTS.md §44–§45, §72–§75

OBJECTIVE
Independently verify gate **VX1**: the operational visibility is real (reads persisted signal), the
health path is correct, worker liveness is observable, and no boundary is crossed.

IN SCOPE
- Independent probes and queries against real operational tables.
- Health-path correctness tests (including the previously verified blind spot).
- Security negative tests: customer → ops endpoint DENY; PE → ops endpoint DENY; non-staff DENY.
- Scope-boundary verification (no RLS remediation, no X2, no customer-visible leakage).

OUT OF SCOPE
- Fixing findings; changing code; deploying; re-litigating decisions.

REPORTING
Create: docs/cline/reports/CT-P8-P8X-X1-INDEPENDENT-VERIFICATION-20260913-044.md with environment,
Git SHA, probes/results, security results, findings with severity, limitations, verdict.

FINAL STATUS
Return: VX1 PASS — READY FOR PO CLOSURE (or) FAIL — <findings> (or) BLOCKED — <reason>
============================================================
```

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-P8X-X2-IMPLEMENTATION-20260913-045
STATUS: PLANNED — NOT YET AUTHORIZED
============================================================

IMPLEMENTATION AUTHORIZATION GATE
Prepared instruction only. Cline MUST NOT execute it unless the PO has separately and explicitly
authorized CT-P8-P8X-X2-IMPLEMENTATION-20260913-045 **and** resolved `PX-6` (alerting) and `PX-7`
(operational retention), with X1 closed.

READ FIRST
- Phase 8-X discovery §13 (S1–S6), §15 (stages X4–X7), §19
- The X1 implementation/verification reports
- AGENTS.md §42 (retention), §67

CURRENT BASELINE
- X1 implemented and VX1-verified; aggregation, console extension, thresholds and API metrics not implemented.

OBJECTIVE
Implement stages X4–X7: unified failure aggregation, console extension, threshold evaluation/alert
dispatch and API runtime metrics — exactly as bounded by the PO decisions.

IN SCOPE
- Read-model aggregation over existing failure/usage/import/delivery/AI tables.
- Console tabs consuming the read models.
- Threshold evaluation and alert dispatch **only** with PO-confirmed recipients/channels/thresholds; retention per PX-7.
- API runtime metrics within the authorised instrumentation envelope.

OUT OF SCOPE
- X8 definition (it must be produced as part of the X2 authorisation, not invented by the implementer).
- RLS remediation; production deployment; L1–L7; any customer-visible surface.

DEPENDENCIES
- X1 closed; `PX-6`, `PX-7`.

TEST REQUIREMENTS
- Aggregation correctness against seeded real data; alerting evaluation tests; security DENY tests;
  retention enforcement tests where PX-7 defines retention.

INDEPENDENT VERIFICATION
Separate task (gate VX2). Cline must not self-certify.

REPORTING
Create: docs/cline/reports/CT-P8-P8X-X2-IMPLEMENTATION-20260913-045.md.

STOP CONDITIONS
Stop if `PX-6`/`PX-7` are unresolved, if X1 is not closed, or if X8’s definition is required before
implementation can be bounded.

FINAL STATUS
Return: PHASE 8-X X2 IMPLEMENTATION COMPLETE — READY FOR INDEPENDENT VERIFICATION (or) BLOCKED — <reason>
============================================================
```


---

## 16. Phase 9 reconciliation (Phase 8-X vs Phase 9)

### 16.1 The records, side by side **[R]**

| Record | Position | Date |
|---|---|---|
| Phase 8-X discovery + P8-X OHD report | Defines **Phase 8-X — Operational Intelligence** as *“a bounded extension/workstream of Phase 8, **not a new Phase 9**”* | 2026-09-12 17:31 (discovery) / OHD report |
| RLS hold register §11 | *“**No Phase 9 is created or implied**… the `Phase 9` identifier was **never PO-ratified**… historical working terminology only. The bounded workstream is now referred to as **Phase 8-X — Operational Intelligence**.”* | 2026-09-12 |
| `CARBONTALLY_PHASE9_SYSTEM_RUNTIME_AND_OPERATIONAL_INTELLIGENCE_BASELINE_20260912.md` (commit `b471286`) | **Establishes Phase 9 = System Runtime & Operational Intelligence** as a baseline; §61 `PHASE 9 BASELINE ESTABLISHED`; discovery NOT STARTED; implementation **NOT AUTHORIZED**; and §63 itself flags **C-1** (Phase 9 not in the PO-ratified roadmap), **C-2** (Phase 8 documents use “Phase 9” with a customer-facing meaning), **C-3** (a legacy Phase 9 programme exists in the repo) — all MATERIAL, **PO clarification required** | 2026-09-12 15:43 |
| `CARBONTALLY_MASTER_PROJECT_ROADMAP_V1.0.md` | Names Phase 7 (Auditor/Assurance) and Phase 8 (Advanced Analytics), scope NOT defined; **contains no Phase 9** | ratified 10 Sep 2026 |
| `-023` §10.3 | Records the conflict: same domain, two names, **no reconciliation record** — **PO DECISION REQUIRED** | 2026-09-13 |

**The two positions are not reconcilable by an agent.** The Phase 9 baseline is now a *committed* document, while the RLS register and the P8-X discovery explicitly deny that Phase 9 exists. **Reported, not silently resolved.**

### 16.2 Explicit answers to the required questions

| # | Question | Answer |
|---|---|---|
| 1 | Is Phase 8-X still a valid workstream? | **Yes on the discovery/RLS-register reading; contested by the committed Phase 9 baseline.** Cannot be settled agent-side. |
| 2 | Is Phase 9 replacing/absorbing Phase 8-X scope? | **It describes the same domain** (system runtime & operational intelligence) with a broader capability register. Whether it *absorbs* Phase 8-X is **unresolved** — no PO record says either way **[U]**. |
| 3 | Which work belongs to Phase 8? | The reporting/disclosure programme: B1–B4, lifecycle S1–S8, P1, P2 (as separate batches), and the Phase 8-X **extension** per the ratified terminology disposition (Decision Record §1.1) **[D]**. |
| 4 | Which work belongs to Phase 8-X? | Stages X1–X8 / M1–M6 / S1–S6 / L1–L7, per the discovery, **if** the identifier survives reconciliation **[D]/[U]**. |
| 5 | Which work belongs to Phase 9? | *Undetermined.* The Phase 9 baseline claims the whole operational-intelligence domain (~60 capability areas), but it is **not ratified** and is **not authorised**, and the roadmap does not contain it **[U]**. |
| 6 | What requires PO reconciliation? | (a) which identifier owns the operational-intelligence domain; (b) whether the Phase 9 baseline supersedes the Phase 8-X PX register (PX-1…PX-12) or runs alongside it; (c) whether the Master Roadmap is extended; (d) whether X1/X2 gates (VX1/VX2) are still the delivery path **[U]**. |
| 7 | Is X8 defined sufficiently to plan? | **No.** X8 appears only as a stage name (“Testing, security verification, operational verification”); **no X8 artefact, scope statement or acceptance criteria exist** **[R]**. |
| 8 | Are any X1/X2 decisions already ratified? | **No.** `PX-1…PX-12` are all recorded as OPEN; only the *MUST/SHOULD/LATER classification* is a proposal awaiting `PX-2` **[D]**. |
| 9 | Which X decisions remain open? | All twelve (`PX-1…PX-12`), plus `BL-1`/`BL-2` access authorisations and `PX-9` (the `DATABASE_URL` remediation, explicitly *not* Phase 8-X) **[D]**. |
| 10 | What must NOT be implemented before the conflict is resolved? | **Any X stage and any Phase 9 stage** — and no Phase 9 document may be treated as authorising implementation until the PO ratifies the phase’s existence, name and scope **[D]/[U]**. |

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-P8X-PHASE9-PO-RECONCILIATION-20260913-040
STATUS: PLANNED — NOT YET AUTHORIZED
TYPE: RECONCILIATION / DECISION PREPARATION (documentation only)
============================================================

IMPLEMENTATION AUTHORIZATION GATE
Prepared instruction only. Cline MUST NOT execute it unless the PO has separately and explicitly
authorized CT-P8-P8X-PHASE9-PO-RECONCILIATION-20260913-040.

READ FIRST
- docs/architecture/CARBONTALLY_PHASE8X_OPERATIONAL_INTELLIGENCE_DISCOVERY_AND_IMPLEMENTATION_BOUNDARY_20260912.md (§2, §16–§20)
- docs/architecture/CARBONTALLY_PHASE9_SYSTEM_RUNTIME_AND_OPERATIONAL_INTELLIGENCE_BASELINE_20260912.md (§3–§5, §53, §59, §61, §62, §63)
- docs/architecture/CARBONTALLY_PRODUCTION_RLS_SECURITY_HOLD_AND_REMEDIATION_REGISTER_20260912.md (§11)
- docs/architecture/CARBONTALLY_MASTER_PROJECT_ROADMAP_V1.0.md (rows 3/what the roadmap contains)
- docs/architecture/CARBONTALLY_PHASE8_DISCLOSURE_MODEL_DECISION_RECORD_20260912.md §1.1 (terminology disposition)
- docs/cline/reports/CT-P8-CURRENT-STATE-RECOVERY-20260913-023.md §10.1, §10.3
- AGENTS.md §78 (§5 supersession)

CURRENT BASELINE
- Phase 8-X discovery (09-12) defines the domain for Phase 8-X and denies a new Phase 9.
- The Phase 9 baseline (committed `b471286`, 09-12) establishes the same domain under a Phase 9 identifier and itself flags C-1/C-2/C-3 as MATERIAL and PO clarification required.
- The RLS register (09-12) states the Phase 9 identifier was never ratified and is historical.

OBJECTIVE
Produce a PO-reconciliation package that (a) states each position with its exact citation and date,
(b) lists the mutually exclusive options, (c) states the consequences of each option for the PX register,
the VX gates, the Master Roadmap and the naming of future work, and (d) requests one PO decision.

IN SCOPE
- Citation-accurate comparison; option set; consequences; recommendation (clearly marked advisory).
- A statement of which documents become historical under each option.

OUT OF SCOPE
- Editing any Phase 7/8/8-X/9 document; any implementation; any change to the roadmap.
- Choosing the answer.

TEST REQUIREMENTS
- None; a citation-completeness table instead.

REPORTING
Create: docs/cline/reports/CT-P8-P8X-PHASE9-PO-RECONCILIATION-20260913-040.md
Required contents: positions; option set; consequence analysis; recommendation (advisory);
required PO decision wording; no-change statement; verdict.

STOP CONDITIONS
Stop if evidence is missing for either position rather than guessing.

FINAL STATUS
Return: RECONCILIATION PACKAGE READY FOR PO DECISION (or) INCONCLUSIVE — EVIDENCE INSUFFICIENT
============================================================
```

---

## 17. CarbonTally Insight / I1 status

### 17.1 The conflict **[R]**

| Source | Statement |
|---|---|
| Commit `d91ace5` (2026-09-12T14:37:34+06:00) subject | *“docs: ratify CarbonTally Insight architecture and **authorize I1**”* |
| The D2 document's own status line | *“D2 PO RATIFIED — CARBONTALLY INSIGHT ARCHITECTURE **READY FOR I1 IMPLEMENTATION AUTHORISATION**”* |
| D2 §24.4 | *“**A separate prompt must authorise I1 implementation.**”* and *“I1 implementation has NOT been performed.”* |
| Repository objects | **No `carbontally_insight_*` object exists** in schema, backend or frontend **[V]** |
| `-023` §10.7 | Records the ambiguity and requires **PO confirmation** |

### 17.2 Verdict

### `I1 AUTHORISATION AMBIGUOUS — PO CLARIFICATION REQUIRED`

A commit **subject line** is not an authorisation record, and the ratified document that the commit carries expressly says the *opposite* (ready **for** authorisation; a separate prompt required). Therefore the authoritative current state is: **I1 is ratified in architecture and NOT authorised for implementation** — pending PO confirmation, because the commit message is a discoverable artefact that contradicts it. Nothing of I1 is implemented.

### 17.3 Next action

`…041` asks the PO one question and records the answer durably; until then **I1 must be treated as unauthorised**.

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-I1-AUTHORISATION-CLARIFICATION-20260913-041
STATUS: PLANNED — NOT YET AUTHORIZED
TYPE: AUTHORISATION-CLARIFICATION RECORD (documentation only)
============================================================

IMPLEMENTATION AUTHORIZATION GATE
Prepared instruction only. Cline MUST NOT execute it unless the PO has separately and explicitly
authorized CT-P8-I1-AUTHORISATION-CLARIFICATION-20260913-041.

READ FIRST
- docs/architecture/CARBONTALLY_PHASE8_CARBONTALLY_INSIGHT_D2_PO_RATIFICATION_20260912.md (§23 staging, §24 I1 boundary, §24.4/§24.5)
- git history: `d91ace5` and `d204fda` (subjects and file contents)
- docs/cline/reports/CT-P8-CURRENT-STATE-RECOVERY-20260913-023.md §10.7, §11
- AGENTS.md §62, §73

CURRENT BASELINE
- D2 ratified; I1 scope defined (§24.2 MAY / §24.3 MUST NOT); implementation NOT performed; no Insight objects exist.

OBJECTIVE
Record the authoritative I1 authorisation state: present the commit-subject vs ratified-document conflict,
state the options (authorised / not authorised), and request one PO ruling; then record the ruling verbatim.

IN SCOPE
- The conflict statement with citations; the option set; the consequences (a second authorised-but-unimplemented
  candidate alongside any other); a durable record of the PO's answer once given.

OUT OF SCOPE
- Any I1 implementation; any schema, API, frontend or AI change; any amendment of the D2 document.

TEST REQUIREMENTS
- None; a consistency check that the recorded ruling is consistent with D2 §24.

REPORTING
Create: docs/cline/reports/CT-P8-I1-AUTHORISATION-CLARIFICATION-20260913-041.md

STOP CONDITIONS
Stop if the PO has not answered; report the question rather than inferring an answer.

FINAL STATUS
Return: I1 AUTHORISATION RULED BY PO — <state>  (or)  AWAITING PO RULING
============================================================
```


---

## 18. G0 gate reconciliation

### 18.1 State of each gate **[D]/[R]**

| Gate | Recorded state | Assessed state on current evidence | Needed to close |
|---|---|---|---|
| **G0-A** — ESRS E1 evidence closure | **OPEN** (PO/evidence) | **STILL OPEN** — `E1-COV` CONDITIONAL; identifiers unresolved; GHG Protocol Ch.9 lists and residual SECR numerics unresolved | authoritative evidence + PO ratification (`…048`) |
| **G0-B** — Phase 8 naming/scope | Resolved for this programme (Decision Record §1.1); optional roadmap update | **CLOSED (programme)**; a residual Master-Roadmap wording update is optional and deliberate | optional PO/roadmap action |
| **G0-C** — approve the batch plan (B1–B4, P1, P2, X1–X2; Option B) | **OPEN** | **STILL OPEN.** B1/B2 were executed under bounded per-task authorisations, and `PQ-8` ratified the *B1→B2→B3→B4 delivery sequence*, but **no record approves the whole batch plan** | PO: close G0-C, or record that per-prompt authorisation supersedes it (`…042`) |
| **G0-D** — production migration strategy for the 34-migration backlog | **OPEN** | **STILL OPEN.** B2 added 2 more (36). Production application of anything remains prohibited | PO/deployment decision (`…042`) |
| **G0-E** — confirm B2 scope | **OPEN** at `-008`; ratified subsequently | **CLOSED by ratification** — `B2-D1…B2-D12` all closed and implemented; no residual | none (record the closure in `…028`) |
| **G0-F** — historical PDF/IMAGE treatment | Recorded (`DM-7` §6 refinement) | **CLOSED as recorded** | none |
| **G0-G** — Phase 8-X `PX-2/PX-4/PX-5/PX-6/PX-7` | **OPEN** | **STILL OPEN** (and now entangled with the Phase 9 question) | PO (`…040`) |
| **G0-H** — RLS remediation stays separate | Recorded; confirmation open | **EFFECTIVELY CONFIRMED** (hold register §11 states the separation explicitly) — PO confirmation still desirable | PO confirmation |
| **G0-I** — `DM-7` refresh | **DONE** | **CLOSED** | none |
| **A-RLS** (report-table RLS) | not a G0 gate; recorded in the S0 package | **OPEN**, blocks S2 only | PO (`…042`/`…046`) |

**A batch authorisation does not close a global gate.** B1's and B2's bounded authorisations did **not** close G0-C or G0-D, and the production prohibition is unaffected by any batch authorisation **[I]/[D]**.

### 18.2 E1 evidence closure (G0-A) — the only evidence-blocked item

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-G0-A-E1-EVIDENCE-CLOSURE-20260913-048
STATUS: PLANNED — NOT YET AUTHORIZED
TYPE: EVIDENCE / DECISION PREPARATION (documentation only)
============================================================

IMPLEMENTATION AUTHORIZATION GATE
Prepared instruction only. Cline MUST NOT execute it unless the PO has separately and explicitly
authorized CT-P8-G0-A-E1-EVIDENCE-CLOSURE-20260913-048.

READ FIRST
- docs/architecture/CARBONTALLY_PHASE8_REPORTING_REGULATORY_REQUIREMENT_VERIFICATION_20260912.md
- docs/cline/reports/CT-P8-REPORTING-REGULATORY-REQUIREMENT-VERIFICATION-20260912-001.md
- docs/cline/reports/CT-P8-REPORTING-REGULATORY-EVIDENCE-CLOSURE-20260912-002.md and ...-COMPLETION-20260912-003.md
- docs/architecture/CARBONTALLY_PHASE8_DISCLOSURE_MODEL_DECISION_RECORD_20260912.md (§14 `E1-COV`, `E1-VER`)
- AGENTS.md §17, §27, §62

CURRENT BASELINE
- `E1-COV` CONDITIONAL; E1 identifiers, GHG Protocol Ch.9 required/optional lists and residual SECR
  numeric detail are unresolved; **no identifier may be invented** (D17).

OBJECTIVE
Produce the evidence closure package: for each unresolved regulatory element, state what evidence is
required, what authority could supply it, what remains blocked without it, and what the product must do
in the meantime (honest `NOT_SUPPORTED` / `CUSTOMER_INPUT_REQUIRED` handling).

IN SCOPE
- Evidence register; authority sources; blocked scope (E1 seed rows for B3); interim handling statement.
- A PO decision entry closing or continuing the CONDITIONAL status.

OUT OF SCOPE
- Inventing identifiers, thresholds or numeric values; fetching unverifiable web content as authority.
- Any implementation or seed row.

REPORTING
Create: docs/cline/reports/CT-P8-G0-A-E1-EVIDENCE-CLOSURE-20260913-048.md

STOP CONDITIONS
Stop if closure would require asserting an identifier that cannot be traced to an authoritative source.

FINAL STATUS
Return: G0-A EVIDENCE PACKAGE READY FOR PO (or) BLOCKED — AUTHORITATIVE EVIDENCE UNAVAILABLE
============================================================
```

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-G0-GATE-RECONCILIATION-20260913-042
STATUS: PLANNED — NOT YET AUTHORIZED
TYPE: GOVERNANCE RECONCILIATION (documentation only)
============================================================

IMPLEMENTATION AUTHORIZATION GATE
Prepared instruction only. Cline MUST NOT execute it unless the PO has separately and explicitly
authorized CT-P8-G0-GATE-RECONCILIATION-20260913-042.

READ FIRST
- docs/architecture/CARBONTALLY_PHASE8_P8X_COMPREHENSIVE_IMPLEMENTATION_READINESS_20260912.md §29
- docs/cline/reports/CT-P8-G0-GOVERNANCE-CONSOLIDATION-20260912-008.md §18
- docs/architecture/CARBONTALLY_PHASE8_B1_PO_DECISION_RATIFICATION_20260912.md (PQ-5, PQ-8)
- docs/architecture/CARBONTALLY_PHASE8_B2_PO_DECISION_RATIFICATION_20260913.md (B2-D1…B2-D12)
- docs/cline/reports/CT-P8-CURRENT-STATE-RECOVERY-20260913-023.md §10.2, §11, §12
- The RLS hold register §8/§10 (decisions D-1…D-13) and the S0 closure package (`A-RLS`)
- AGENTS.md §62

CURRENT BASELINE
- G0-A/C/D/G open; G0-E effectively closed by B2 ratification; G0-H effectively confirmed; G0-B/F/I closed.
- No record closes G0-C; no record decides A-RLS.

OBJECTIVE
Produce a single reconciliation table of every open gate/decision with its exact recorded status, the
current evidence, and the decision wording required — for one PO sitting.

IN SCOPE
- Gate/decision register (G0-*, A-RLS, PX-*, P1-D*, B3-PO-*/B4-PO-* topics, I1, RLS D-1…D-13).
- Recommended decision wording per item.

OUT OF SCOPE
- Closing anything; implementing anything; modifying any governance document other than the new report.

REPORTING
Create: docs/cline/reports/CT-P8-G0-GATE-RECONCILIATION-20260913-042.md

STOP CONDITIONS
Stop if recording a "closed" state would require inferring a decision that no record contains.

FINAL STATUS
Return: G0 RECONCILIATION PACKAGE READY FOR PO DECISIONS
============================================================
```

---

## 19. Fresh priority order (current state, not the stale table)

| Priority | Workstream | Current state | Required next gate | Implementation readiness | Dependencies |
|---|---|---|---|---|---|
| **1** | B2 closure record + F-025-1/F-025-2 disposition (`…028`) | IV PASS; closure asserted in prompt; **no repository closure artefact** | PO records closure; disposes F-025-1 (canonicalisation breadth) and F-025-2 (dead line) | N/A (governance) | `-025` |
| **2** | B1/B2 controlled application to **non-production** (`…029`) | Not applied anywhere; suites skip | PO bounded authorisation + named environment + restorable snapshot | **READY (operational)** | `PQ-5` permits dev/QA; #1 for lineage |
| **3** | B2 worktree reconciliation (track/commit) | 2 migrations + code + tests untracked; staged 0 | PO repository-discipline decision | N/A | #1 |
| **4** | G0 gate reconciliation (`…042`) | G0-A/C/D/G open; A-RLS open | PO sitting | N/A | — |
| **5** | Phase 8-X / Phase 9 reconciliation (`…040`) | Two names, one domain; Phase 9 baseline committed but unratified; X8 undefined | PO decision on identifier/ownership | N/A | Gates X1/X2 |
| **6** | Insight I1 clarification (`…041`) | Commit says “authorize I1”; ratified doc says “ready for authorisation” | PO ruling | N/A | — |
| **7** | **B3** contract + decisions (`…031`) | No contract; decisions not recoverable; `E1-COV` CONDITIONAL | PO ratifies contract + `B3-D1…n` | **NOT READY until contract** | #1, #2 (environment), G0-A for E1 seeds |
| **8** | **B3** implementation (`…032`) + IV (`…033`) | none | PO authorisation after #7 | NOT READY | #7 |
| **9** | **B4** contract + decisions (`…034`) | none; scope overlaps S4/S5 | PO ratifies contract + `B4-D1…n` | NOT READY | #8 (B3 closed) |
| **10** | **B4** implementation (`…035`) + IV (`…036`) | none | PO authorisation after #9 | NOT READY | #9 |
| **11** | S1/S3 independent verification (`…030`) | Implemented; **no IV record** | Verification authorisation (no PO decision needed) | READY (verification) | S3 applied locally |
| **12** | S2 + `A-RLS` (`…046`) | `A-RLS` undecided; S3 overtook S2 | PO decision on report-table RLS posture | NOT READY | PO |
| **13** | P1 decision package (`…037`) → P1 implementation | Contract complete; 7 decisions open | PO decisions | DESIGNED | independent |
| **14** | P2 census (`…038`) → remediation | Forensic complete; census blocked | PO authorises read-only session | DESIGNED | independent |
| **15** | RLS reopening package (`…039`) → remediation | Held; 13 decisions; sequence discrepancy | PO explicitly reopens | HELD | separate workstream |
| **16** | Phase 8-X X1 (`…043`/`…044`) | Designed; PX-2/3/5 open; BL-1/BL-2; naming conflict | #5 then PX decisions | NOT READY | #5 |
| **17** | Phase 8-X X2 (`…045`) | Designed; PX-6/PX-7 open | after X1 closed | NOT READY | X1 |
| **18** | G0-A E1 evidence (`…048`) | Evidence-blocked | PO/evidence | NOT READY | external evidence |
| **19** | S5/S6/S7 allocation decision | Unrecorded in the B-series | PO allocation decision | N/A | B3/B4 |
| **20** | S8 AI-assisted narrative; D16 legacy (`LEG`) | Deferred / separate | separate authorisation | NOT AUTHORISED | AI preconditions; separate |

**The first item is a governance action, not implementation** — because the only thing standing between the current state and B3 is the *record* of B2's closure plus a decision on how the F-025 observations are disposed of.


---

## 20. Dependency / critical-path graph

### 20.1 The real critical path (not the task numbering)

```text
G0 (partially open:  A  C  D  G | closed: B E F H I)
  |
  +-- R1  B2 closure record ....................................... [governance]
  |     |
  |     +-- R2  B1+B2 applied to a non-production environment .... [operational; PQ-5 permits dev/QA]
  |           |  (snapshot + preflight + apply x2 + idempotency + runtime suites)
  |           |
  |           +-- B3 contract (031) --> PO ratifies B3-Dn
  |                 |
  |                 +-- B3 implementation (032) --> V3 independent verification (033) --> PO closure
  |                       |
  |                       +-- B4 contract (034) --> PO ratifies B4-Dn
  |                             |
  |                             +-- B4 implementation (035) --> V4 independent verification (036) --> PO closure
  |
  +-- (parallel, independent of the B-chain)
        S1/S3 independent verification (030)
        A-RLS decision --> S2 report-table RLS (046)
        P1 decision package (037) --> P1 implementation --> VP
        P2 catalogue census (038) --> P2 remediation
        RLS reopening package (039) --> RLS-4A-1 --> ... (separate gate)
        Phase 8-X / Phase 9 reconciliation (040) --> X1 (043) --> VX1 (044) --> X2 (045) --> VX2
        I1 authorisation clarification (041)
        G0 reconciliation (042)
        G0-A E1 evidence closure (048)
```

### 20.2 Classification of every dependency

| Class | Items |
|---|---|
| **Must happen before** | R1 (B2 closure record) before R2’s report lineage; **R2 (B1+B2 in a non-production environment) before B3 implementation** — B3 cannot runtime-test against an absent schema; B3 closure before the B4 contract; B4 contract before B4 implementation; each implementation before its independent verification; each verification before its PO closure. |
| **Can happen in parallel** | S1/S3 verification (030); P1 decision package (037); P2 census (038, once read-only access is authorised); RLS reopening package (039); Phase 8-X/Phase 9 reconciliation (040); I1 clarification (041); G0 reconciliation (042); E1 evidence closure (048). None touches the B-chain or another’s artefacts. |
| **Requires PO decision** | B2 closure/F-025 disposition; B2 worktree reconciliation; G0-C; G0-D; G0-A; G0-G; A-RLS; Phase 8-X vs Phase 9; I1; `P1-D1…P1-D8`; `PX-2/3/4/5/6/7/8`; RLS reopen + D-1…D-13; `B3-Dn`; `B4-Dn`; S5/S6/S7 allocation. |
| **Requires independent verification** | B3 (V3/033); B4 (V4/036); P1+P2 (VP, verified separately); X1 (VX1/044); X2 (VX2); S1/S3 (030); the non-production environment state, if fresh evidence is required. |
| **Deployment-only dependency** | Production application of B1/B2 (and later B3/B4 migrations) — blocked by **G0-D**, not by any product decision. |
| **Separate workstream (must not be bundled)** | RLS remediation; D16 legacy disposition (`LEG`); S8 AI narrative; E1 evidence closure; Phase 9 (until ratified); Ask CarbonTally/Insight AI surface. |

---

## 21. Parallel workstreams

| Track | Can start when | Cannot be merged with |
|---|---|---|
| **A — Phase 8 core (`B3 → B4`)** | after R2 + B3 contract | P1, P2, RLS, Phase 8-X |
| **B — Extraction remediation (P1)** | after `…037` decisions | P2 (different root cause — ratified), B3/B4 |
| **C — Factor matching (P2)** | after `…038` census | P1, B3/B4 |
| **D — Report lifecycle completion (S1/S3 verification, S2/A-RLS, S5/S6/S7 allocation)** | S1/S3 verification now; S2 after A-RLS | RLS remediation |
| **E — Phase 8-X operational intelligence** | after `…040` + PX decisions | RLS remediation; Phase 9 documents |
| **F — Security (RLS)** | only on explicit PO reopening | everything else (ratified) |
| **G — Insight/I1** | after `…041` ruling | report lifecycle (separate domains; names must not collide — `S*` vs `I*`) |

**Ratified prohibitions preserved:** do **not** parallelise P1+P2 **[D]**; do **not** combine Phase 8-X with RLS **[D]**; do **not** resolve E1 rows by inventing IDs **[D]**; do **not** backfill by re-extraction **[D]**.

---

## 22. Required PO decisions (consolidated register)

| # | Decision | Blocks | Where |
|---|---|---|---|
| D-01 | Record B2 closure + dispose of F-025-1/F-025-2 | B3 lineage; repository truth | `…028` |
| D-02 | Authorise (or defer) B1/B2 application to a named non-production environment | B3/B4 build + runtime verification | `…029` |
| D-03 | B2 worktree reconciliation (track/commit or explicitly leave untracked) | traceability | `…028` |
| D-04 | Close G0-C (batch plan) or record per-prompt supersession | governance clarity | `…042` |
| D-05 | G0-D production migration strategy (36 outstanding) | any production deployment | `…042` |
| D-06 | G0-A E1 evidence closure | E1 seed rows in B3 | `…048` |
| D-07 | Phase 8-X vs Phase 9 identifier/ownership; extend the Master Roadmap? | all X work; Phase 9 baseline | `…040` |
| D-08 | `PX-2/PX-4/PX-5` (MUST set; platform access BL-1/BL-2; heartbeat schema) | X1 implementation + VX1 | `…040`, then authorisation |
| D-09 | `PX-6/PX-7` (alerting; operational retention) | X2 | after X1 |
| D-10 | `PX-8/PX-9/PX-10/PX-11/PX-12` (internal-only surfaces; `DATABASE_URL` remediation; incident model; APM; consolidation stance) | X scope clarity | `…040` / later |
| D-11 | Insight I1 authorisation ruling | I1 implementation | `…041` |
| D-12 | Ratify the B3 contract and `B3-D1…B3-Dn` | B3 implementation | after `…031` |
| D-13 | Ratify the B4 contract and `B4-D1…B4-Dn` | B4 implementation | after `…034` |
| D-14 | S5/S6/S7 allocation into B4 (or separate batches) | B4 scope | after B3 |
| D-15 | `P1-D1…P1-D8` | P1 implementation | `…037` |
| D-16 | P2 remediation approach (after census) | P2 implementation | after `…038` |
| D-17 | `A-RLS` report-table RLS posture | S2 | `…046` |
| D-18 | RLS gate reopening + `D-1…D-13` + sequence discrepancy | RLS remediation | `…039` |
| D-19 | D16 legacy disposition (`LEG`) authorisation | legacy retirement | separate |
| D-20 | S8 AI-narrative authorisation | AI narrative | separate |


---

## 23. Complete future implementation / verification sequence

Governance model preserved for every batch: **PO decision → implementation authorisation → implementation → independent verification → PO closure → controlled deployment.** No batch below has a contract or an authorisation unless its row says so.

| Seq | Task ID | Type | Prereq | Contract needed | PO decision needed | Then |
|---|---|---|---|---|---|---|
| 1 | `…028` | Governance record | `-025`; PO closure statement | n/a | D-01, D-03 | B3 lineage clean |
| 2 | `…029` | Operational application | `…028`; named env; snapshot | B2 contract §18.4 (exists) | D-02 | env holds B1+B2 |
| 3 | `…030` | Independent verification (S1/S3) | — | lifecycle spec (exists) | none | S1/S3 verified |
| 4 | `…042` | Governance reconciliation | — | n/a | D-04, D-05 | gates closed/decided |
| 5 | `…040` | PO reconciliation | — | n/a | D-07 | X workstream identity settled |
| 6 | `…041` | Authorisation clarification | — | n/a | D-11 | I1 state settled |
| 7 | `…031` | Contract + decisions | `…028` | produces it | D-12 after | B3 authorisable |
| 8 | `…032` | Implementation | `…029`, `…031`, D-12 | B3 contract | D-12 | B3 built |
| 9 | `…033` | Independent verification (V3) | `…032` | n/a | none | B3 verified |
| 10 | PO closure B3 | Decision | `…033` | n/a | PO | B3 closed |
| 11 | `…034` | Contract + decisions | B3 closed | produces it | D-13 after | B4 authorisable |
| 12 | `…035` | Implementation | `…034`, D-13 | B4 contract | D-13 | B4 built |
| 13 | `…036` | Independent verification (V4) | `…035` | n/a | none | B4 verified |
| 14 | PO closure B4 | Decision | `…036` | n/a | PO | Phase 8 core complete |
| 15 | `…037` → P1 impl → VP | Decision → impl → verification | — | P1 contract (exists) | D-15 | extraction fixed |
| 16 | `…038` → P2 remediation | Forensic → decision → impl | read-only access | needed | D-16 | picker fixed |
| 17 | `…046` | Implementation (S2 + report-table RLS) | `…042` | needed | D-17 | S2 complete |
| 18 | `…039` → RLS steps | Planning → implementation | PO reopening | needed per step | D-18 | security gate progressed |
| 19 | `…040` → `…043` → `…044` → `…045` | Decision → impl → VX1 → X2 | — | needed | D-07..D-10 | Phase 8-X delivered |
| 20 | `…041` → I1 → I-series | Decision → impl | — | needed | D-11 | Insight foundation |
| 21 | `…048` | Evidence closure | — | n/a | D-06 | E1 seedable |

### 23.1 `…028` — B2 closure record (the immediate next action)

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-B2-CLOSURE-RECORD-20260913-028
STATUS: PLANNED — NOT YET AUTHORIZED
TYPE: GOVERNANCE RECORD (documentation only)
============================================================

IMPLEMENTATION AUTHORIZATION GATE
Prepared instruction only. Cline MUST NOT execute it unless the PO has separately and explicitly
authorized CT-P8-B2-CLOSURE-RECORD-20260913-028.

READ FIRST
- docs/cline/reports/CT-P8-B2-IMPLEMENTATION-20260913-024.md
- docs/cline/reports/CT-P8-B2-INDEPENDENT-VERIFICATION-20260913-025.md (verdict; F-025-1/F-025-2)
- docs/cline/reports/CT-P8-B2-PO-IMPLEMENTATION-AUTHORISATION-20260913-019.md
- AGENTS.md §73, §74, §83

CURRENT BASELINE
- B2 implemented (`…024`) and independently verified PASS (`…025`).
- The PO closure is asserted (PO statement, 2026-09-13) but **no repository record exists**;
a repository-wide search finds no `CT-P8-B2-PO-CLOSURE-…-026` and no equivalent.

OBJECTIVE
Create the missing closure record: state the closure, cite the verification evidence, disposition
F-025-1 and F-025-2, record the two B1/B2 environment facts (not applied anywhere; suites skip), and
state what closure does and does not authorise (no deployment, no B3).

IN SCOPE
- The closure record document only.
- An explicit statement whether F-025-1 (numeric-looking text canonicalisation breadth) stays as-is or
  becomes a bounded follow-up; and whether F-025-2 (dead `return None` at backend/domain/line_items.py:350)
  is fixed in a future batch or left.
- A reconciliation note about B2 artefacts being untracked (with the PO's repository decision, if given).

OUT OF SCOPE
- Any code or migration change (including the F-025-2 cleanup unless the PO explicitly authorises it here);
  any commit; any deployment; any reopening of B2 design.

TEST REQUIREMENTS
- None. A consistency check that the closure record cites `-024` and `-025` accurately.

REPORTING
Create: docs/cline/reports/CT-P8-B2-PO-CLOSURE-20260913-028.md
Required contents: closure statement; evidence cited; F-025 dispositions with rationale; environment
facts; authorisation boundary (what closure does NOT authorise); worktree state; verdict
`B2 CLOSED — PO ACCEPTED`.

STOP CONDITIONS
Stop if the PO has not stated the closure in the current conversation; do not infer it from the
verification PASS (verification ≠ acceptance).

FINAL STATUS
Return: B2 CLOSED — PO ACCEPTED (or) AWAITING PO CLOSURE STATEMENT
============================================================
```

### 23.2 `…030` — S1/S3 independent verification

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-S1S3-INDEPENDENT-VERIFICATION-20260913-030
STATUS: PLANNED — NOT YET AUTHORIZED
============================================================

IMPLEMENTATION AUTHORIZATION GATE
Prepared instruction only. Cline MUST NOT execute it unless the PO has separately and explicitly
authorized CT-P8-S1S3-INDEPENDENT-VERIFICATION-20260913-030.

READ FIRST
- Commits `c86b83c` (S1) and `19e4f01` (S3), migration `20260913000000_p8_report_lifecycle_status.sql`
- docs/architecture/CARBONTALLY_PHASE8_REPORTING_LIFECYCLE_SPEC_20260912.md (§7, §12, §13, §14, §21, §32)
- backend/domain/report_lifecycle.py, backend/data/report_versions.py, backend/api/v3_reports.py, their tests
- CT-P8-B2-INDEPENDENT-VERIFICATION-20260913-025.md (methodology precedent)
- docs/cline/reports/CT-P8-CURRENT-STATE-RECOVERY-20260913-023.md §10.6 (the asymmetry being closed)
- AGENTS.md §44–§45, §72–§75

CURRENT BASELINE
- S1 and S3 implemented; S3's migration applied locally; **no independent verification report exists**
  (the S4 report’s reference to “S1 and S3 verification reports” resolves to no file).

OBJECTIVE
Independently verify S1 (single-current invariant) and S3 (lifecycle states, guarded transitions,
version-scoped endpoints, audit events) against the ratified specification, on a disposable clone.

IN SCOPE
- Migration apply/re-apply and parity evidence; `is_current` invariance under concurrency/expected-state guards.
- State-machine transition tests including every illegal transition (DENY).
- Authorization tests: customer roles ALLOW/DENY; PE denied; internal staff denied where specified.
- Audit-event verification for each transition.
- Scope-boundary verification (no narrative, no RLS change, no frozen artefact).

OUT OF SCOPE
- Fixing findings; changing code; adding features; S2/S4/S5; committing.

TEST REQUIREMENTS
- PASS/FAIL/SKIPPED/BLOCKED/UNVERIFIED per criterion; SKIPPED is never PASS.

REPORTING
Create: docs/cline/reports/CT-P8-S1S3-INDEPENDENT-VERIFICATION-20260913-030.md

FINAL STATUS
Return: S1/S3 INDEPENDENT VERIFICATION PASS — READY FOR PO CLOSURE (or) FAIL — <findings> (or) BLOCKED — <reason>
============================================================
```

### 23.3 `…046` — S2 lifecycle schema + report-table RLS (gated on `A-RLS`)

```text
============================================================
COPY/PASTE CLINE INSTRUCTION
TASK: CT-P8-S2-REPORT-TABLE-RLS-AND-SCHEMA-20260913-046
STATUS: PLANNED — NOT YET AUTHORIZED
============================================================

IMPLEMENTATION AUTHORIZATION GATE
Prepared instruction only. Cline MUST NOT execute it unless the PO has separately and explicitly
authorized CT-P8-S2-REPORT-TABLE-RLS-AND-SCHEMA-20260913-046 **and** decided `A-RLS`.

READ FIRST
- docs/architecture/CARBONTALLY_PHASE8_OPEN_DECISION_CLOSURE_AND_IMPLEMENTATION_AUTHORISATION_20260912.md (A-RLS L979/L1080/L1165)
- docs/architecture/CARBONTALLY_PHASE8_REPORTING_LIFECYCLE_SPEC_20260912.md (§22.3 the report-table RLS finding, §25 schema options, §35 S2)
- docs/architecture/CARBONTALLY_PRODUCTION_RLS_SECURITY_HOLD_AND_REMEDIATION_REGISTER_20260912.md (§11: RLS remediation is a separate gate)
- The S3 migration as the precedent for “NO RLS change”

CURRENT BASELINE
- `report_versions.status` exists (S3). Two report tables have no RLS policies. `A-RLS` is undecided.

OBJECTIVE
Implement exactly the PO-ratified S2 scope: the report-version schema additions and the report-table
RLS posture the PO chose (complete org-scoped policy coverage **or** a documented app-layer-only posture).

IN SCOPE
- Only the schema/policy work the ratified `A-RLS` decision names; additive and reversible.
- Tests proving the chosen posture (ALLOW for org members; DENY for cross-tenant, PE, Viewer where applicable).

OUT OF SCOPE
- The production RLS remediation (separate gate); narrative (S4/B4); frozen artefact (S5/B4).

REPORTING
Create: docs/cline/reports/CT-P8-S2-REPORT-TABLE-RLS-AND-SCHEMA-20260913-046.md

STOP CONDITIONS
Stop if `A-RLS` is undecided, or if the work would require touching the production RLS baseline.

FINAL STATUS
Return: S2 IMPLEMENTATION COMPLETE — READY FOR INDEPENDENT VERIFICATION (or) BLOCKED — A-RLS UNDECIDED
============================================================
```

### 23.4 `…035` — B4 implementation (narrative + frozen artefact + finalisation)

> **NOTE:** like §6.2, this block is complete in form but cannot be completed in substance until `…034` has produced a ratified B4 contract. Its `IN SCOPE` section instructs Cline to take the scope **verbatim from the ratified contract** and to **STOP** if that contract is absent.

```text
============================================================
COPY/PASTE CLINE IMPLEMENTATION INSTRUCTION
TASK: CT-P8-B4-IMPLEMENTATION-20260913-035
STATUS: PLANNED — NOT YET AUTHORIZED
============================================================

You are implementing CT-P8-B4-IMPLEMENTATION-20260913-035.

IMPLEMENTATION AUTHORIZATION
This task is authorized only if the PO has explicitly authorized
CT-P8-B4-IMPLEMENTATION-20260913-035 in the current conversation.
If explicit authorization is absent:
STOP immediately and report that implementation authorization has not been provided.

READ FIRST
- docs/architecture/CARBONTALLY_PHASE8_B4_IMPLEMENTATION_CONTRACT_20260913.md (the ratified contract)
- docs/cline/reports/CT-P8-B4-CONTRACT-AND-PO-DECISIONS-20260913-034.md
- docs/architecture/CARBONTALLY_PHASE8_B3_IMPLEMENTATION_CONTRACT_20260913.md (the B4↔B3 interface)
- docs/architecture/CARBONTALLY_PHASE8_REPORTING_LIFECYCLE_SPEC_20260912.md (§10, §15, §17–§19, §21, §22, §25, §26)
- docs/architecture/CARBONTALLY_PHASE8_DISCLOSURE_MODEL_DECISION_RECORD_20260912.md (A1, A3, P3, DM-5, DM-6)
- AGENTS.md

CURRENT BASELINE
- B3 implemented, independently verified and PO-closed; report lifecycle S1/S3 implemented; no narrative
  store, no frozen artefact, no finalisation gate beyond the version-level FINAL state.
Cline must capture the worktree state (branch, HEAD, staged, tracked modifications, untracked) before any change.

OBJECTIVE
Implement exactly the B4 deliverables authorised by the ratified contract: requirement-bound narrative
overlay, the finalisation gate, and the frozen export artefact — and nothing else.

IN SCOPE
<take verbatim from the ratified contract's implementation-boundary section; if the contract does not
exist, is unratified, or is ambiguous, STOP — do not infer scope from this plan>

OUT OF SCOPE
- Any new report engine; report-wide free-form editing; arbitrary narrative character limits (A3).
- AI-assisted narrative (S8) in any form; any LLM call.
- Activation of the dormant `report_comments` table unless the ratified contract explicitly includes it.
- The production RLS remediation; P1; P2; Phase 8-X; Insight; legacy D16 reporting.
- Any modification of B1/B2/B3 migration files or any unrelated migration.
- Writing to calculated emissions, provenance, factor data or system-derived authoritative values (P3).

DEPENDENCIES
- Ratified B4 contract and `B4-D1…B4-Dn` (incl. the S4 hard-stop resolution and the S5/S6/S7 allocation).
- B3 closed; the B3 requirement interface present in the environment.

IMPLEMENTATION REQUIREMENTS
- Narrative is bound per requirement only; enforced at the API boundary, not the UI.
- DRAFT-only narrative edits; approved/finalised versions are immutable (no silent rewrite, no retrofit).
- Frozen artefact: deterministic content hash, private storage, immutable association, recorded metadata.
- Finalisation gate per `DM-5`: `NOT_SUPPORTED` and `CUSTOMER_INPUT_REQUIRED` preserved, never conflated.
- Retention/deletion per the ratified decision; never weaken auditability or evidence traceability.
- Audit events through the existing append-only audit substrate for every state change.

TEST REQUIREMENTS
- Exactly the contract's tests, including: narrative write ALLOW/DENY by role; narrative outside a
  requirement binding DENY; cross-tenant DENY; PE DENY; stale-approval rejection; frozen-artefact
  immutability and hash reproducibility; post-approval edit rejection; no silent rewrite of history.
- Amend, never delete, existing tests.

INDEPENDENT VERIFICATION
Cline must not self-certify. The implementation report states "implemented, not independently verified"; verification is `…036`.

REPORTING
Create: docs/cline/reports/CT-P8-B4-IMPLEMENTATION-20260913-035.md
The report must include: task identity; scope; files created/changed; migrations; storage configuration;
API and frontend changes; tests and results; environment and provisioned state; runtime verification or an
explicit statement that it was not performed; security verification; worktree state before/after;
remaining limitations; verdict.

STOP CONDITIONS
Stop immediately if:
- the B4 contract does not exist, is not ratified, or is ambiguous;
- a required policy input (approval model, retention, storage, hashing) is not ratified;
- implementation would require changing the calculation engine, factor data, evidence classification,
  a B1/B2/B3 migration, or the production RLS baseline;
- a storage target cannot be created without affecting other tenants or the investor-demo data;
- the worktree contains unrelated changes that would be affected.

DO NOT
- expand scope; modify unrelated work; alter unrelated migrations; resolve unrelated findings;
- commit; push; deploy;
unless explicitly authorized by the applicable governance decision.

FINAL STATUS
Return: B4 IMPLEMENTATION COMPLETE — READY FOR INDEPENDENT VERIFICATION  (or) BLOCKED — <reason>
============================================================
```


---

## 24. Risks and unresolved conflicts (reported, not silently reconciled)

| # | Conflict / risk | Sources | Disposition |
|---|---|---|---|
| **C-1** | **B2 closure record absent.** The prompt states B2 is PO-closed under `…026`; **no such record exists** in the repository (whole-tree search). | task prompt; `-025`; repository | **Reported.** Treated as a PO statement, not repository fact. Action: `…028` creates the record. |
| **C-2** | **“Phase 8” carries at least six incompatible meanings** (Advanced Analytics; Workflow Orchestrator; Integration Verification; Batch/Work Item Audit; threat-model work; this reporting/disclosure programme). | `-023` §10.1; Master Roadmap; `RECONSTRUCTED_TASK_HISTORY` | Recorded; Decision Record §1.1 governs *this* programme; the Master Roadmap is deliberately unmodified. |
| **C-3** | **Phase 8-X vs Phase 9** — same domain, two names, one committed Phase 9 baseline that itself demands PO clarification, and an RLS register that denies Phase 9 exists. | §16.1 | **Unresolved — PO decision (D-07).** No X implementation before it. |
| **C-4** | **S4’s hard-stop grounds vs the later Disclosure Model ratification.** S4 stopped because the narrative allowlist was “PO DECISION REQUIRED”; the Disclosure Model Decision Record then ratified `A1` (allowlist), `A3` (numeric limits) and `P3` (editable content). Whether this fully satisfies S4’s stop conditions is **not recorded**. | S4 report; Decision Record §6 | **Reported.** The B4 contract (`…034`) must resolve each stop condition explicitly against the ratified decisions; **no agent may declare S4 unblocked by inference.** |
| **C-5** | **G0-C recorded open while the batch plan is executed.** B1/B2 proceeded under bounded prompts; `PQ-8` ratified the sequence, but no record approves the whole plan. | `-008` §18; `-023` §10.2 | PO decision (D-04). |
| **C-6** | **`A-RLS` overtaken by S3** without a recorded decision. | `-023` §10.5; closure package L1165; S3 migration | PO decision (D-17). |
| **C-7** | **Verification asymmetry:** B-series has full IV chains; S1/S3 have none. | `-023` §10.6, §11 | Action: `…030`. |
| **C-8** | **I1 authorisation ambiguity** (commit subject vs ratified status line). | `d91ace5`; D2 §24.4; `-023` §10.7 | PO ruling (D-11) via `…041`. |
| **C-9** | **Two “S” series collide** (report lifecycle S1–S8 vs Phase 8-X SHOULD S1–S6), and **Insight uses I1–I8** while report staging uses S0–S8. | `-023` §10.4; D2 §25.5 | Documentation hazard; the plan always qualifies which series is meant. |
| **C-10** | **X8 undefined** — no artefact, no acceptance criteria. | discovery §15; `-023` §7.1 | Must be defined before X2 authorisation (D-08/D-09). |
| **C-11** | **B3/B4 internal decisions “NOT RECOVERABLE”** while the roadmap lists them as *designed* batches. | `-023` §11 item 2; roadmap §20 | Resolved by `…031`/`…034` (contract + decision register). |
| **C-12** | **Untracked implementation artefacts** (B1, B2, and the S1/S3-era files): git cannot prove byte-identity, and B2’s reconciliation is pending. | V1R R5; `git status`; this task §2.1 | PO decision (D-03); no rewrite, no deletion. |
| **C-13** | **`E1-COV` CONDITIONAL** limits E1 content without blocking the B3 contract. | Decision Record §14; `PQ-7` | Recorded; G0-A (`…048`). |
| **C-14** | **Environment choice** — `carbontally_test` (114 tables, no B1 schema) was judged unsuitable as the §20.3 clone. | `-019` §17 O-2 | Open engineering/PO decision (§11.2.1). |
| **C-15** | **Two RLS matters must not be conflated:** `A-RLS` (report tables, blocks S2) vs the production RLS security hold. | closure package; hold register | Recorded; separate registers. |
| **C-16** | **Master Roadmap is Phase-6-era text** whose wording (“nothing authorized”) predates this programme. | `-023` §10.8; roadmap | Preserved deliberately (G0-B); optional roadmap governance update. |

### 24.1 Risk register for the recommended sequence

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| B3 built against an unprovisioned environment → runtime suites skip again (the V1 lesson) | Medium | High | `…029` before `…032`; “a SKIPPED suite is not a PASS” enforced in the B3 contract |
| Contract-free implementation of B3/B4 (scope invented) | Medium | High | `…031`/`…034` mandatory before any B3/B4 implementation authorisation |
| Phase 8-X/X2 started under an unresolved identifier → duplicated or discarded work | Medium | Medium | `…040` before `…043`; X8 defined before X2 |
| I1 started while “unauthorised” is the true state | Low | Medium | `…041` ruling recorded before any I1 prompt |
| Production touched while G0-D open | Low | Critical | G0-D remains open; production prohibition restated in every instruction’s STOP conditions |
| Investor-demo data mutated by any step | Low | Critical | D31 guard + AGENTS.md §55 + explicit STOP conditions |
| RLS work bundled with product work | Low | High | Separate workstream preserved; STOP conditions |

---

## 25. Explicit “NOT AUTHORISED” list

None of the following is authorised by this plan or by any record currently in the repository:

1. B3 implementation (no contract; decisions not ratified).
2. B4 implementation (no contract; depends on B3).
3. Any Phase 8-X stage X1–X8 or any Phase 9 work.
4. Any Phase 9 implementation, and any treatment of the Phase 9 baseline as a ratified phase.
5. Insight I1 (and I2–I8) implementation.
6. P1 extraction remediation (including any historical re-extraction).
7. P2 EF-E remediation (including any factor-data change).
8. RLS remediation of any kind, anywhere (production or otherwise), until the gate is explicitly reopened.
9. `A-RLS` report-table policy work.
10. S2, S5, S6, S7, S8 implementation.
11. **Any production migration or deployment** (G0-D open; 36 outstanding migrations).
12. The B2 Class-1 backfill **execution** (dry-run only, and only if separately authorised).
13. Committing, pushing or deploying any B2/B1 artefact.
14. Any change to a migration file, RLS policy, calculation engine, factor data, evidence classification or legacy route.
15. Any invented regulatory identifier, threshold, retention period or numeric value.
16. Any reuse of the investor-demo database for verification or mutation testing.

---

## 26. Copy/paste instruction index

| Seq | Task ID | Task | Type | Current status | Can copy to Cline now? |
|---|---|---|---|---|---|
| 1 | `CT-P8-B2-CLOSURE-RECORD-20260913-028` | Record B2 closure + F-025 dispositions | Governance | Awaiting PO authorisation | **No** — needs PO closure statement + authorisation |
| 2 | `CT-P8-B1B2-ENV-APPLICATION-20260913-029` | Apply B1+B2 to a named non-production environment | Operational | Awaiting PO authorisation | **No** — needs D-02 + environment name |
| 3 | `CT-P8-S1S3-INDEPENDENT-VERIFICATION-20260913-030` | Verify S1/S3 | Independent verification | Awaiting authorisation | No (authorisation only) |
| 4 | `CT-P8-B3-CONTRACT-AND-PO-DECISIONS-20260913-031` | B3 contract + decision register | Contract/decisions | Awaiting PO authorisation | **No** — authorisation only; no PO decision prerequisite |
| 5 | `CT-P8-B3-IMPLEMENTATION-20260913-032` | B3 implementation | Implementation | Awaiting contract + authorisation | **No** — blocked on `…031` + D-12 |
| 6 | `CT-P8-B3-INDEPENDENT-VERIFICATION-20260913-033` | B3 independent verification (V3) | Verification | Depends on `…032` | No |
| 7 | `CT-P8-B4-CONTRACT-AND-PO-DECISIONS-20260913-034` | B4 contract + decision register | Contract/decisions | Depends on B3 closure | No |
| 8 | `CT-P8-B4-IMPLEMENTATION-20260913-035` | B4 implementation | Implementation | Blocked on `…034` + D-13 | No |
| 9 | `CT-P8-B4-INDEPENDENT-VERIFICATION-20260913-036` | B4 independent verification (V4) | Verification | Depends on `…035` | No |
| 10 | `CT-P8-P1-AUTHORISATION-PREPARATION-20260913-037` | P1 decision package | Decision prep | Awaiting PO authorisation | No (authorisation only) |
| 11 | `CT-P8-P2-FACTOR-CATALOGUE-CENSUS-20260913-038` | Read-only factor census | Forensic | Needs read-only access authorisation | No |
| 12 | `CT-P8-RLS-SECURITY-REMEDIATION-PLAN-20260913-039` | RLS reopening package | Planning | Gate held | No — needs D-18 |
| 13 | `CT-P8-P8X-PHASE9-PO-RECONCILIATION-20260913-040` | Phase 8-X / Phase 9 reconciliation | Reconciliation | Awaiting PO authorisation | **No** — authorisation only; produces D-07 |
| 14 | `CT-P8-I1-AUTHORISATION-CLARIFICATION-20260913-041` | I1 authorisation ruling | Authorisation record | Awaiting PO authorisation | No (authorisation only) |
| 15 | `CT-P8-G0-GATE-RECONCILIATION-20260913-042` | G0/A-RLS/PX decision register | Governance | Awaiting PO authorisation | No (authorisation only) |
| 16 | `CT-P8-P8X-X1-IMPLEMENTATION-20260913-043` | Phase 8-X X1 (stages X1–X3) | Implementation | Blocked on D-07/D-08 | No |
| 17 | `CT-P8-P8X-X1-INDEPENDENT-VERIFICATION-20260913-044` | VX1 | Verification | Depends on `…043` | No |
| 18 | `CT-P8-P8X-X2-IMPLEMENTATION-20260913-045` | Phase 8-X X2 (stages X4–X7) | Implementation | Blocked on X1 + D-09 | No |
| 19 | `CT-P8-S2-REPORT-TABLE-RLS-AND-SCHEMA-20260913-046` | S2 + report-table RLS | Implementation | Blocked on D-17 (`A-RLS`) | No |
| 20 | `CT-P8-G0-A-E1-EVIDENCE-CLOSURE-20260913-048` | E1 evidence closure | Evidence/decision | Awaiting PO authorisation | No |

**None of these can be copied into Cline execution yet.** Every instruction begins with an authorisation gate, and the outstanding PO decisions/authorisations are enumerated in §22. The recommended first authorisation is **`…028`** (governance record), immediately followed by **`…029`** (non-production application) once the environment is named.

---

## 27. Final verdict

### `PHASE 8 / PHASE 8-X REMAINING WORK PLAN — READY FOR PO REVIEW`

**Basis.** The repository evidence is sufficient to produce a reliable, bounded, non-authorising plan: every remaining Phase 8 and Phase 8-X item is identified with its authoritative record, its implementation and verification status, its blockers and its next gate; the B3 and B4 readiness questions are answered (`B3 NOT READY — PO DECISIONS REQUIRED`; `B4 NOT READY — PO DECISIONS REQUIRED`); the B1/B2 environment question is resolved against the ratified `PQ-5` (dev/QA permitted, production prohibited); P1, P2, RLS, Phase 8-X, Phase 9, Insight/I1 and the G0 gates are each reconstructed and their unresolved conflicts are **reported rather than silently reconciled** (§24). One documentation discrepancy is material to traceability and is stated explicitly: **the B2 PO closure has no repository record** (C-1), and the recommended first action closes it.

**Authorisation status:** this document authorises **nothing**. It contains prepared instructions only; the PO authorises one task at a time.

*STOP — planning complete. No implementation, migration, database modification, commit, push or deployment occurred. The only file written by this task is this report (and, in Plan mode, its staging copy outside the repository).*

