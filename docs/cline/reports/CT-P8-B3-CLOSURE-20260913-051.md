# CT-P8-B3-CLOSURE-20260913-051

**Task ID:** `CT-P8-B3-CLOSURE-20260913-051`
**Title:** Phase 8 Batch B3 — Disclosure Model Integration — technical/governance closure
**Date:** 2026-09-13
**Authority:** PO blanket authorization `CT-P8-MASTER-SEQUENTIAL-EXECUTION-PO-AUTHORIZATION-20260913-050` §19 — *"I explicitly authorize Cline to create the technical/governance closure records required by the master playbook after implementation and required verification have passed."*
**Type:** CLOSURE RECORD (no implementation, no migration, no production action)

---

## 1. Closure statement

> **B3 — Disclosure Model Integration is CLOSED.**
> Implementation is complete under the ratified contract; independent verification (gate **V3**) has **PASSED** on a fresh production-shaped clone; the batch's evidence is recorded and reproducible.

This is a **technical/governance closure**, created under the PO authority quoted above. It records **no new PO decision** and does not convert verification into a PO *acceptance* beyond that authority (the B2 precedent required an explicit PO closure statement; B3 relies on §19).

## 2. What was delivered (contract §4 deliverables A–L)

| # | Deliverable | Evidence |
|---|---|---|
| **A** | `disclosure_intensity_denominator_types` (generic capability) + `disclosure_intensity_denominator_framework_links` (**separate** framework claims, with a CHECK that makes an unevidenced `STATUTORY_REQUIRED` claim impossible) + `disclosure_intensity_ratios` (org-scoped selection) | migrations `20260917000000`, `20260917010000`; applied `rc=0` ×2 |
| **B** | projection engine over the ratified closed `source_kind` producer registry; deterministic aggregation (`SUM_KG_CO2E`, `SUM`, `DISTINCT_COUNT`, `RATIO`, `PASSTHROUGH`); no emission recomputed | `backend/domain/disclosure_projection.py`; 23 pure tests |
| **C** | requirement/purpose mapping mechanism (identity-level only — B3 authors no mapping content, per `B3-D8`/`PQ-6`) | data layer + service |
| **D** | applicability engine (`APPL`: period-dated, version-bound, `UNDETERMINED` first-class, never a legal determination) | domain guards + runtime tests |
| **E** | purpose projections (`DM-2`: `purpose_code` as a projection; `report_type` unchanged) | purpose requirement set loading |
| **F** | value-status semantics (`PENDING`/`RESOLVED`/`UNRESOLVED`; `effective_class` authoritative; `NOT_SUPPORTED` ≠ `CUSTOMER_INPUT_REQUIRED`) | 23 pure + 4 runtime + 9 V3 tests |
| **G** | value→line enumeration read model (the B2 `COALESCE` join) | V3 §7.6 — real line-linked fixtures, both branches |
| **H/I** | intensity selection service + the seven contract §12.2 API endpoints | `backend/api/v3_disclosure.py`; 14 API tests |
| **J** | RLS on all three new tables + server-side authorization | V3 §7.5 security matrix |
| **K** | audit through the canonical append-only `audit_trail` (no new mechanism) | V3 §7.7 |
| **L** | static/pure/runtime/security/regression test contract | 175 tests executed on the clone |

## 3. Verification evidence (gate V3)

* **Environment:** `ct_b3_v3_20260913` — fresh production-shaped clone (116 pre-existing tables; schema-only restore `rc=0`).
* **Migration chain:** S3 → B1×2 → B2×2 → B3×2, all `rc=0`; **re-apply all seven `rc=0`**; idempotency diff **0 lines**.
* **Additive-only:** removed diff lines **0**; tables 128→131; policies 193→197; indexes 200→206; functions unchanged (67).
* **Suites:** B3 pure 23/23 · B3 API 14/14 · B3 runtime 4/4 · B3 V3 security/lineage/audit 9/9 · B1+B2 runtime 36/36 · S1/S3 89/89 → **175/175 PASS, 0 FAIL**.
* **Security matrix:** cross-tenant, non-member/PE-like, Viewer-write, Member-write, anonymous and customer-authored-denominator cases all **DENY**; Owner/Admin and member-read cases **ALLOW**; API-level 403/404/400 semantics verified.
* **Honesty constraints:** `UNDETERMINED` preserved (0 resolved); distinct unmapped / `NOT_SUPPORTED` / `CUSTOMER_INPUT_REQUIRED` reasons; no fabricated zeros; no secrets in audit payloads.

## 4. Governance decisions consumed (no new decisions created)

`D11-CAT` · `DM-1` · `DM-2` · `DM-3` · `DM-5` · `DM-6` · `DM-7` · `APPL`/D13 · `GP-GAS`/`GP-BY` (excluded) · `PQ-1` · `PQ-3` · `PQ-4` · `PQ-6` · `PQ-7` · `D15` · `D17` · `E1-COV` (CONDITIONAL — honoured) · PO direction of 2026-09-13 answering **`B3-D1`** (*CarbonTally denominator capability ≠ regulatory requirement*; four candidates generic; framework claims separated and evidence-gated) · PO correction *"there is no Phase 9"*.

## 5. Findings carried (none block closure)

| # | Finding | Severity | Disposition |
|---|---|---|---|
| F-B3-1…F-B3-6 | Implementation/test defects found and fixed during increments 2b (incl. the `DM-5` conflation risk, now regression-tested) | — | **CLOSED** (fixed + re-verified) |
| F-B3-8…F-B3-10 | V3 fixture/probe defects (mine) | — | **CLOSED** (fixed + re-run) |
| **F-B3-7** | Flaky `'777'` substring assertion in the **closed** B2 suite | Low | **Recorded only** — B2 not reopened (per instruction). Recommended: replace with a deterministic payload-discipline assertion as a bounded test-only change. |
| **F-030-1** | Pre-existing `user-1`/uuid test defect in S1's test file | Medium | Recorded; deselected in regressions. Recommended: bounded test-only fix. |
| Evidence dependency | E1 official identifiers / framework mapping content remain evidence-gated (`E1-COV` CONDITIONAL; `G0-A`) | — | Carried to `…048`; **no content invented** |

## 6. What this closure does NOT authorise

* No production migration or deployment (**G0-D** remains open).
* No backfill (the B2 Class-1 backfill remains unauthorised and unexecuted).
* No B4 implementation (narrative / finalisation / frozen artefact) — B4 still requires its own contract.
* No narrative UI, no AI narrative (`S8`), no `report_comments` activation.
* No change to the calculation engine, factor data, evidence classification, or any B1/B2 artefact.

## 7. Environment and repository state

| Item | Value |
|---|---|
| Verification environment | `ct_b3_v3_20260913` (disposable clone, retained for re-verification) |
| QA environment | `carbontally_qa_phase8` (B1+B2+B3 applied; runtime suites execute) |
| Production | untouched; deployment prohibited |
| Branch / HEAD | `main` / `37b19d13723b0b1eabceeade86ce1615a98ab400` |
| Commits | **none** |
| Migrations authored | 2 (B3-1, B3-2) — applied to non-production environments only |
| Unrelated work | preserved (the 218 pre-existing modifications plus the B3 API router wiring = 219 tracked modifications) |

## 8. Verdict

### `B3 CLOSED — IMPLEMENTED, INDEPENDENTLY VERIFIED (V3 PASS), GOVERNANCE RECORDED`
