# CT-P8-B4-CLOSURE-20260913-052

**Task ID:** `CT-P8-B4-CLOSURE-20260913-052`
**Title:** Phase 8 Batch B4 (Narrative, Finalisation and Frozen Artefact) — **gate V4 closure**
**Date:** 2026-09-13
**Environment:** `carbontally_qa_phase8` (persistent non-production QA) — **no production access, no production data or storage writes, no historical backfill/re-extraction**
**Authorization:** PO blanket authorization `…050`; PO rulings `B4-D1`, `B4-D5/D6/D7`, `B4-D8`, `B4-D10`, `B4-D4/D9` (contract Amendments 1–6)
**Git:** HEAD `37b19d13723b0b1eabceeade86ce1615a98ab400` · **no commits** · staged 0 · tracked modifications 219 (unchanged) · unrelated worktree changes preserved · no secrets

---

## 1. Scope closed

S4 (narrative) + S5 (frozen artefact) + the finalisation spine, with **S7 inside B4** and **S6 excluded** (PO `B4-D10`). Deliverables A–L of the B4 contract are implemented except the deliberately out-of-scope item (`B4-D12` commercial gating) and the S6 UI.

## 2. Gate V4 acceptance criteria — evidence

| # | Criterion | Status | Evidence |
|---|---|---|---|
| **A1** | Migrations apply and re-apply (`rc=0`) | **PASS** | B4-1 and B4-2 each applied **twice** to `carbontally_qa_phase8`: `rc=0` and `rc=0`. **Residual DISCHARGED (2026-09-14):** the fresh-clone replay has since been executed and verified — `CT-P8-VERIFICATION-FRESH-CLONE-REPLAY-20260914-055.md`: a disposable clone from the pre-B4 base (`ct_b4v4_20260914`) shows B4-1/B4-2 `rc=0`×2, **added 44 / removed 0**, and **0 unattributable** additions (every added object belongs to the two new B4 tables). |
| **A2** | Pre-existing objects unchanged | **PASS** | Both migrations issue DDL **only** against their own new objects (`ALTER TABLE public.disclosure_narrative_entries`, `ALTER TABLE public.report_version_artifacts`, their indexes, and `DROP POLICY IF EXISTS` on their own policies for idempotency). Zero `ALTER`/`DROP` of any pre-existing object; the new tables only *reference* existing tables. Public table count 131 → **133** (exactly the two mandated tables). |
| **A3** | Narrative is requirement-bound; no report-wide path | **PASS** | `requirement_version_id NOT NULL` **and inside** `UNIQUE (report_version_id, requirement_version_id, narrative_kind)`; domain refuses a missing binding; API 409 for a requirement that is not part of the report version |
| **A4** | Owner/Admin author; Member/Viewer/Consultant/PE/internal staff cannot | **PASS** | unit + runtime: Owner/Admin 200; the four other role classes and unauthenticated role `None` all refused with **no write attempted** |
| **A5** | No narrative write path reaches a calculated value/provenance/factor | **PASS** | the narrative table has no column, FK or trigger reaching `calculation_snapshots`, `emissions_logs`, `emission_factors` or `customer_factors`; no trigger at all |
| **A6** | `DM-5`: `CUSTOMER_INPUT_REQUIRED` blocks; `NOT_SUPPORTED` is surfaced; never collapsed | **PASS** | runtime: blocked ⇒ status stayed `APPROVED`, zero artefacts · `NOT_SUPPORTED` proceeds and is reported in `surfaced_limitations` · a forged collapse raises |
| **A7** | Authoring impossible on `APPROVED`/`FINAL` | **PASS** | domain guard + API 409 · runtime immutability proof |
| **A8** | Drill-down follows `DM-6`; cross-tenant/PE denial proven | **PASS** | `FULL`/`CONTROLLED`/`BOUNDED`/`DENIED` matrix with fail-closed field redaction; PE and internal staff denied with no field-name leakage |
| **A9** | Artefact immutable and hash-reproducible | **PASS** | SHA-256 recomputed byte-for-byte in the runtime suite; the key is **derived** by a DB `CHECK`; `UNIQUE (report_version_id)`; `authenticated` holds **no** `UPDATE`/`DELETE`; a changed render is refused; a new version leaves the FINAL artefact byte-identical |
| **A10** | Every B4 write audited; no secrets in payloads | **PASS** | append-only `audit_trail` entries for `disclosure_narrative.written`, `report_version.approved`, `report_version.finalised`, `frozen_artefact.created`, `frozen_artefact.signed_url_issued`; payloads carry state/key/hash/TTL only — the runtime suite asserts **no URL** appears in any B4 audit payload, and the narrative body is never duplicated into the trail |
| **A11** | No regression | **PASS** | `tests/unit/api` + `tests/unit/domain`: **EXIT=0** · integration set (report lifecycle, report versions, reports, V3 repositories, V3 RLS, B1, B2, B3 projection, B3 V3 security, B4): **EXIT=0** |

## 3. Defect found during A11 and repaired (recorded, not hidden)

**F-030-1** — the S1/S3 suites `test_report_versions.py` and `test_reports.py` passed the non-UUID fixture value `"user-1"` into `uuid` columns (`report_versions.created_by`, `report_generation_queue.created_by`), making **4 tests unverifiable** (`asyncpg` cast error). This was a **pre-existing test-data defect**, unrelated to B4 (the files were pristine — not in the worktree's tracked modifications), and it had been recorded previously without a fix.

**Repair:** the fixture now uses a valid canonical UUID and the assertions compare against the same value; a provenance comment naming `F-030-1` was added to both files. **No application code changed and no test was deleted or weakened** — the suite went from 11 passing + 4 erroring to **15/15 PASS**.

## 4. Decision register — final state

| Class | IDs |
|---|---|
| **Ratified by PO** | `B4-D1` (Owner+Admin approval authority) · `B4-D5`, `B4-D6`, `B4-D7` (mandatory frozen artefact; private bucket; derived key; SHA-256; append-only; no exception path) · `B4-D8` (retain indefinitely; no deletion path; no invented duration) · `B4-D10` (S4+S5 in B4; S7 in B4; S6 out) · `B4-D4`/`B4-D9` (comments outside B4; `A7` deferred to S6) |
| **Settled by already-ratified decisions / implemented** | `B4-D2` (no post-approval mutation; corrections via a new version) · `B4-D3` (review gate satisfied by S3) · `B4-D11` (download audit + staleness flag, never auto-invalidating) |
| **Out of scope, recorded** | `B4-D12` (commercial/entitlement gating — PO commercial policy required before any code) |
| **Ratified/contract-decided earlier** | `B4-D13`…`B4-D19` |

**No B4 decision remains open.**

## 5. Carried obligations (explicitly recorded, not silently dropped)

1. **S6 backlog item** (frontend lifecycle UI): decide + implement comment scope and `A7` visibility; build the UI against the ratified B4 APIs; surface `DM-6` depth, `DM-5` surfaced limitations, artefact hash and staleness honestly. `report_comments` stays **dormant**.
2. **A1 residual:** fresh-clone zero-delta replay for the two B4 migrations in the next independent verification pass.
3. **N3 retention configuration** (post-`B4-D8`): server-side retention configuration through the Settings/Admin control plane, without weakening auditability or `FINAL` immutability.
4. **`B4-D12`** commercial gating: requires a PO commercial decision before implementation.

## 6. Verdict

### `B4 CLOSED AT GATE V4 — ALL ELEVEN CRITERIA PASS, DECISIONS RATIFIED AND RECORDED, S7 SECURITY/REGRESSION VERIFICATION INSIDE B4, S6 CARRIED AS A BACKLOG ITEM`
