# CT-P6-2F-IV-20260911-001

**Prompt Ref:** `CT-P6-2F-IV-20260911-001`
**Response Ref:** `CT-P6-2F-IV-20260911-001-R1`
**Datetime:** 2026-09-11
**Role:** independent verification agent (verification-only)
**Verifies:** `CT-P6-2F-RESUME-20260911-001`
**Implementation HEAD:** `daad396523ac693352cc2f4ebb7fc58814a9e60b`
**Companion report:** `docs/architecture/CARBONTALLY_P6_2F_INDEPENDENT_VERIFICATION_20260911.md`
**Final verdict:** **`P6-2F VERIFIED — PASS WITH NON-BLOCKING FINDINGS`**

## 1. Task

Independently verify the completed P6-2F implementation in a fresh context, without trusting the
implementation agent's reported results, and determine whether P6-2F can be accepted and closed.
Verification-only: no implementation, test, migration, schema, RLS, seed-logic, frontend or backend
change; no fixes to any discovered defect; no commit/push/deploy.

## 2. Verification response (what was actually done)

The isolated environment was brought up from scratch (`reset.sh` → `APPLIED=27 SKIPPED=26 ALL_APPLIED`,
`seed_e2e.py` → `SEED OK` 0 errors, `seed_lifecycle_fixtures.py`), the backend was restarted against the
fresh database, and **three instruments written for this session** were used:

* `/tmp/iv_p6_2f.py` — authorization matrix, RLS boundary, workflow-state enforcement and D11 events,
  driving the real API/PostgREST with 8 real GoTrue personas and reading evidence back from persisted
  rows (**95/95 checks passed, 0 failed**).
* `/tmp/iv_chain_probe.py` — captured status **and body** at every step of the item chain; this is what
  exposed the true cause of a 409 (D37 `charge:item:<id>` idempotency, i.e. intended billing behaviour,
  not a state-machine or authorization defect).
* `/tmp/iv_lt1_sql.py` — extracted the **shipped** SQL from `backend/data/consultants.py` at runtime and
  executed it, plus a cast-removed variant, against real PostgreSQL inside rolled-back transactions.

Additional independent work: browser suite execution in two states; LT-2 evidence (column absence +
running the dedicated integration suite); exact regression counts parsed from raw pytest progress lines
(the repo sets `addopts = "-q"`, which suppresses the summary line).

**Independent result summary:** authorization matrix 25/25, RLS 4/4, invalid transitions 5/5, valid chain
5/5, rejection chain 3/3, D11 events 52/52 → **95/95**; LT-1 **shipped SQL executes, pre-fix SQL fails
with the claimed error**; browser **16/16 with 0 skips** on the pristine environment; `tests/unit`
**1,763 passed / 0 failed / 0 skipped / 0 errors**; `tests/e2e` **39 passed / 0 failed / 0 skipped /
0 errors**.

## 3. Findings

| ID | Severity | Finding |
|---|---|---|
| **LT-1** | ~~P1~~ **CLOSED** | Verified fixed: shipped SQL executes for every target status and for `consultant_tasks`; the cast-removed variant fails with `AmbiguousParameterError: inconsistent types deduced for parameter $2`; the endpoint returns 200 with `pending → active`, a durable `accepted` event, replay 409 and consultant 403 |
| **LT-2** | **P3** | **Confirmed** (not fixed): the dedicated integration DB `carbontally_test` lacks `can_extract`/`can_submit`, so its consultant suite fails on `UndefinedColumnError` before reaching any statement — the coverage gap that let LT-1 escape unit tests |
| **V1** | **P3** | The browser deep-link spec is **notification-order dependent**: it clicks the newest `Open` link and expects an item workspace, so it fails when the newest consultant notification is the engagement-level `accepted` event (link `/consultant`, a valid route, no item context). Observed 15 passed / 1 failed in that state vs **16/16** on the pristine fixture state |
| **V2** | **P3** | `seed_e2e.py` reports `SEED WITH_ERRORS` when re-run without a reset (409 duplicate key on `organization_members (organization_id, user_id)`; the upsert conflicts on `id` only) — data is correct, the summary is misleading |
| **V3** | **P3** (observation) | The ALLOW specs' `test.skip(!visible)` guard could convert a future authorization regression into a **skip** rather than a failure — pre-existing design, mitigated by the 0-skip acceptance expectation, the independent DENY specs, and the API-level matrix in the report |

No P0/P1 finding remains; no functional or security defect was downgraded to reach the verdict.

## 4. Files created by this verification

* `docs/architecture/CARBONTALLY_P6_2F_INDEPENDENT_VERIFICATION_20260911.md`
* `docs/cline/prompt-history/CT-P6-2F-IV-20260911-001.md` (this record)

Nothing else was written: the repository implementation is **unmodified** (all five implementation
checksums identical to the pre-verification baseline), HEAD is unchanged, staged changes are 0, and no
commit, push, deploy, reset or clean of the repository occurred. The isolated E2E database was reset by
its own authorised script (environment, not repository).

## 5. Verification-only boundary confirmation

No defect was fixed. `LT-1` was **not** modified (it was verified as already fixed); `LT-2`, `V1`, `V2`
and `V3` were **recorded only**, with severity, rationale, owner (test infrastructure) and a
"blocks first customer: no" assessment. No migration was applied to production (22 → 53 remain NOT
AUTHORIZED); production was not contacted or modified; no backup/restore/DR work was touched
(`DR-20` remains NOT SATISFIED, backup/DR parked); Phase 7 and Phase 8 were not started.

## 6. Verdict

> **`P6-2F VERIFIED — PASS WITH NON-BLOCKING FINDINGS`**

> **P6-2F is independently verified and ready for Phase 6 closure.**

Phase 7 requires a separate Product Owner authorization after Phase 6 closure. This verification session
**stopped** at the verification boundary; the next decision belongs to the Product Owner.
