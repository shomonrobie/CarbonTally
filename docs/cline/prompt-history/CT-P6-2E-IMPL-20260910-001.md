# CarbonTally — Durable Prompt History

**Prompt Ref:** `CT-P6-2E-IMPL-20260910-001`
**Prompt datetime:** 2026-09-10 (start of P6-2E implementation session)
**Response Ref:** `CT-P6-2E-IMPL-20260910-001-R1`
**Response datetime:** 2026-09-10 20:22 → 20:47 (+0600)
**Phase / Checkpoint / Gate:** Phase 6 / CP2 / P6-2E — Consultant Vocabulary & Lifecycle Events
**Mode:** IMPLEMENTATION
**Verdict:** `P6-2E IMPLEMENTED — READY FOR INDEPENDENT VERIFICATION`
**Status:** COMPLETE — stopped at the P6-2E boundary

---

## PART A — EXACT PROMPT (verbatim, fenced chunks)

### Chunk 1 (header → §8)

```markdown
# CarbonTally — P6-2E Implementation

**Prompt Ref:** `CT-P6-2E-IMPL-20260910-001`
**Date:** 2026-09-10
**Phase:** Phase 6
**Checkpoint:** CP2
**Gate:** P6-2E — Consultant Vocabulary & Lifecycle Events
**Mode:** IMPLEMENTATION
**Independent Verification:** REQUIRED AFTER THIS TASK

---

# 1. PURPOSE

Implement the ratified **P6-2E** scope following the existing Phase-6 architecture, PO decisions, implementation contract, and completed preflight.

This is a bounded implementation task.

The implementation must cover:

* D8 — existing conversation model verification/regression support;
* D11 — five consultant lifecycle events;
* deterministic/idempotent event creation;
* server-derived recipients;
* organisation/firm isolation;
* denied-path protection;
* appropriate focused tests;
* regression testing;
* durable implementation evidence.

Do not begin P6-2F.

Do not perform independent verification in this session.

---

# 2. AUTHORITATIVE BASIS

Before implementation, read:

1. `docs/architecture/CARBONTALLY_FINAL_ARCHITECTURE_BLUEPRINT_V1.3.md`
2. `docs/architecture/CARBONTALLY_MASTER_PROJECT_ROADMAP_V1.0.md`
3. `docs/architecture/CARBONTALLY_P6_2_PO_DECISION_REGISTER.md`
4. `docs/architecture/CARBONTALLY_PHASE6_REMAINDER_IMPLEMENTATION_CONTRACT_V1.0.md`
5. `docs/architecture/CARBONTALLY_P6_2_CONSULTANT_PROCESSING_WORKFLOW_ARCHITECTURE.md`
6. relevant messaging/conversation architecture
7. relevant notification/event infrastructure
8. `docs/cline/CARBONTALLY_P6_2E_PREFLIGHT_REPORT.md`
9. `docs/cline/CARBONTALLY_CP1_CLOSURE_REPORT.md`

Also inspect the existing implementation before changing anything.

The authoritative hierarchy remains:

**Blueprint → Master Roadmap → PO Decision Register → Phase-6 Implementation Contract → repository implementation/evidence.**

Do not create another master/control document.

---

# 3. CP1 STATUS

CP1 is closed.

P6-2D was independently verified:

`P6-2D INDEPENDENTLY VERIFIED — PASS WITH NON-BLOCKING FINDINGS`

F1 was explicitly accepted outside P6-2D scope.

Do not reopen P6-2D.

Do not implement provenance for `confirm`/`retry`.

P6-2D implementation is frozen.

---

# 4. D8 — CONVERSATION MODEL

D8 is ratified as:

> Reuse the existing conversation model.

The preflight established:

* consultants already participate through the existing conversation model;
* `conversation_kind` remains existing `org` / `entity` vocabulary;
* no dedicated consultant conversation kind is required;
* consultant access is governed through existing organisation/grant relationships.

Therefore:

### DO

* preserve the existing conversation model;
* add only the regression/verification coverage necessary to demonstrate D8 compliance.

### DO NOT

* create `consultant` as a new conversation kind;
* create a consultant-specific conversation table;
* redesign messaging;
* change existing conversation tenancy;
* introduce a new role/capability/permission.

---

# 5. D11 — FIVE LIFECYCLE EVENTS

Implement exactly these five consultant lifecycle events:

1. **accepted**
2. **submitted-to-QC**
3. **QC outcome**
4. **customer decision**
5. **rework**

Use existing notification/event infrastructure wherever possible.

Do not create a parallel event system.

---

# 6. EXISTING TRIGGER POINTS

The preflight identified existing trigger points.

Independently confirm them in source before implementation.

Expected trigger areas are:

### Event 1 — accepted

Consultant engagement acceptance.

### Event 2 — submitted-to-QC

Consultant submission to QC.

### Event 3 — QC outcome

CarbonTally QC decision/outcome.

### Event 4 — customer decision

Customer/organisation review decision.

### Event 5 — rework

Relevant rejected/rework transition, including existing:

* `ct_qc_rejected`
* `consultant.review:rework`

Use the actual authoritative state transition/event semantics found in the repository.

Do not invent duplicate transitions.

---

# 7. EVENT CREATION

Use the existing idempotent notification/event persistence mechanism.

The preflight established:

* `create_idempotent` already exists;
* `event_key` exists;
* database uniqueness exists through `uq_notifications_event_key`;
* `actor_domain` exists;
* `notification_type` is free-form.

Reuse these facilities.

Do not create a new event table unless an actual architectural blocker is discovered.

---

# 8. DETERMINISTIC EVENT KEYS

Every D11 event must have a deterministic event key.

The key must be:

* derived from the stable business event identity;
* deterministic;
* repeat-safe;
* unique for the intended event;
* independent of arbitrary request-generated randomness;
* safe against replay.

Do not expose event-key selection to the client.

Do not allow a client to choose a recipient or alter the event identity.

Use the existing database uniqueness mechanism as the final idempotency boundary.

Document the exact key scheme in the implementation report.
```

### Chunk 2 (§9 → §16)

```markdown
---

# 9. SERVER-DERIVED RECIPIENTS

Recipients must be determined server-side.

The client must never supply the authoritative notification recipient list.

Use existing relationship/resolver infrastructure where appropriate.

The preflight identified existing facilities including:

* `support_staff_user_ids`
* `entity_participant_user_ids`
* `list_firm_members`
* `list_active_client_grants`
* `get_members`
* `list_members_with_email`

Determine the correct recipient matrix from:

* existing organisation relationships;
* consultant firm membership;
* client organisation membership;
* CarbonTally QC/Ops roles already authorized by the existing system.

Do not invent new recipient roles.

Do not add permissions merely to make notification delivery possible.

---

# 10. RECIPIENT MATRIX

Before implementation, establish the actual intended matrix.

At minimum consider:

| Event             | Consultant/Firm recipients        | Client organisation recipients                  | CarbonTally/QC recipients          |
| ----------------- | --------------------------------- | ----------------------------------------------- | ---------------------------------- |
| accepted          | existing relevant firm recipients | existing relevant client recipients if required | only if existing workflow requires |
| submitted-to-QC   | relevant firm recipients          | relevant client recipients                      | QC/Ops recipients                  |
| QC outcome        | relevant firm recipients          | relevant client recipients                      | existing relevant staff            |
| customer decision | relevant firm recipients          | relevant client recipients                      | existing relevant staff            |
| rework            | relevant firm recipients          | relevant client recipients                      | relevant QC/Ops recipients         |

This table is a planning aid only.

Do not blindly implement every cell.

Derive the actual recipient matrix from the authoritative workflow and existing relationship semantics.

If the existing architecture does not support a required recipient safely, **STOP and report the ambiguity** rather than inventing authorization semantics.

---

# 11. SECURITY REQUIREMENTS

Every event path must preserve:

* authentication;
* organisation isolation;
* consultant firm isolation;
* active-grant requirements;
* canonical capability resolution;
* server-side event creation;
* server-side recipient determination;
* no client-controlled recipient;
* no cross-organisation leakage;
* no cross-firm leakage;
* deterministic idempotency;
* replay resistance;
* alternate-route protection;
* existing RLS;
* existing authorization semantics.

---

# 12. DENIED-PATH RULE

A denied or unauthorized action must not emit a D11 lifecycle event.

Ensure event creation occurs only after the relevant authorization/state transition has been successfully established.

Do not emit notification merely because a request was attempted.

Test this explicitly.

---

# 13. EVENT BUS WARNING

The preflight established that the current event bus is **in-process/non-durable**.

Therefore:

**Do NOT make the in-process event bus the authoritative D11 mechanism.**

The durable notification/event persistence layer must remain the authoritative mechanism.

The in-process mechanism may only be used if it does not compromise the durable event contract and is already architecturally appropriate.

---

# 14. RLS

Do not modify RLS.

The preflight found no requirement for RLS changes.

If implementation genuinely requires an RLS modification:

**STOP.**

Do not improvise a policy.

Report the blocker for PO/architecture review.

---

# 15. SCHEMA / MIGRATION

The preflight found:

**No migration required.**

The existing schema already provides the required event/idempotency fields.

If implementation discovers that a migration is genuinely required:

**STOP before creating it.**

Do not create a migration merely for convenience.

Do not change schema without explicit re-authorization.

---

# 16. ROLES / CAPABILITIES / PERMISSIONS

Do not create:

* new roles;
* new capabilities;
* new permissions;
* new grants.

Use the existing authorization model.

If a D11 requirement appears impossible without introducing one:

**STOP and report it.**
```

### Chunk 3 (§17 → §24)

```markdown
---

# 17. IMPLEMENTATION QUALITY

Prefer:

* small changes;
* existing infrastructure;
* centralized event definitions where appropriate;
* centralized recipient derivation where appropriate;
* deterministic key generation;
* clear domain semantics;
* no duplicated authorization logic;
* no duplicated notification machinery.

Do not perform unrelated refactoring.

Do not clean up unrelated technical debt.

---

# 18. TESTS — REQUIRED

Add focused tests for at least:

### D8

* consultant conversation participation;
* existing conversation-kind semantics;
* organisation isolation.

### D11

For each five lifecycle events:

* event is emitted at the correct successful transition;
* event identity is correct;
* event key is deterministic;
* duplicate/replay is suppressed;
* recipients are server-derived;
* client cannot inject recipients;
* cross-organisation recipients are excluded;
* cross-firm recipients are excluded;
* unauthorized/denied actions emit nothing.

Also test:

* repeated event creation;
* database uniqueness/idempotency;
* recipient derivation;
* relevant alternate routes;
* existing notification behavior is not broken.

Do not weaken or skip tests.

---

# 19. BASELINE

Before implementation, establish the current test baseline if possible.

Record:

* collected count;
* executed count;
* passed;
* failed;
* skipped;
* errors;
* exit code.

The last measured repository state had:

**1,627 tests collected**

but independently establish the current baseline rather than assuming it remains unchanged.

---

# 20. REGRESSION

After implementation:

1. run all new focused P6-2E tests;
2. run relevant existing messaging/notification tests;
3. run relevant P6-2C/P6-2D regression tests;
4. run the full unit suite.

Record actual results.

Do not claim a test passed unless it was actually executed successfully.

If the environment prevents complete regression, clearly report the limitation.

---

# 21. SECURITY TEST MATRIX

Produce a concrete matrix:

| Test                          | Expected          | Actual | Result |
| ----------------------------- | ----------------- | ------ | ------ |
| recipient injection           | ignored/rejected  |        |        |
| cross-org recipient           | excluded          |        |        |
| cross-firm recipient          | excluded          |        |        |
| unauthenticated event trigger | denied            |        |        |
| denied transition             | no event          |        |        |
| duplicate event               | one durable event |        |        |
| replay                        | no duplicate      |        |        |
| alternate route               | protected         |        |        |
| event-key collision           | safely suppressed |        |        |

Do not merely state “security tested.”

---

# 22. BILLING

Do not redesign billing.

Do not change:

* subscription;
* entitlement;
* charging;
* processing credits.

If event generation interacts with an existing billing path, preserve the existing billing boundary.

---

# 23. P6-2D PROVENANCE

Do not modify P6-2D provenance.

Specifically:

* do not add `confirm/retry` provenance;
* do not change consultant firm provenance;
* do not change processing-mode provenance;
* do not change processing-origin vocabulary.

P6-2D is closed.

---

# 24. P6-2F / LATER PHASES

Do not implement:

* `/consultant` UI redesign;
* browser E2E acceptance;
* final Phase-6 UI/UX;
* Auditor/Assurance;
* Analytics;
* Phase 7;
* Phase 8.

P6-2F is a later gate.
```

### Chunk 4 (§25 → §30 + ABSOLUTE RULE)

```markdown
---

# 25. DOCUMENTATION

Create:

`docs/cline/CARBONTALLY_P6_2E_IMPLEMENTATION_REPORT.md`

The report must contain:

1. Prompt Ref
2. Response Ref
3. implementation date/time
4. authority documents inspected
5. baseline repository state
6. D8 assessment
7. D11 design
8. five event definitions
9. trigger locations
10. deterministic event-key design
11. recipient matrix
12. authorization/security design
13. files created
14. files modified
15. files intentionally unchanged
16. tests added
17. tests executed
18. regression results
19. migration/schema assessment
20. RLS assessment
21. roles/capabilities/permissions assessment
22. security findings
23. limitations
24. risks
25. scope compliance
26. commands executed
27. git status
28. final verdict
29. explicit stop-condition confirmation

---

# 26. DURABLE PROMPT HISTORY

Create:

`docs/cline/prompt-history/CT-P6-2E-IMPL-20260910-001.md`

It must preserve:

* exact Prompt Ref;
* complete prompt;
* prompt datetime;
* complete Cline response;
* Response Ref;
* response datetime;
* files inspected;
* files changed/unchanged;
* commands;
* tests;
* findings;
* decisions;
* risks;
* status;
* verdict;
* stop-condition confirmation.

---

# 27. GIT GOVERNANCE

Do not:

* `git add .`;
* commit;
* push.

Before and after implementation, record:

* branch;
* HEAD;
* porcelain status;
* relevant changed files.

Separate P6-2E changes from pre-existing working-tree changes.

Do not clean unrelated modifications.

---

# 28. STOP CONDITIONS

STOP and report rather than improvising if:

* a new role/capability/permission appears necessary;
* RLS modification appears necessary;
* schema migration appears necessary;
* recipient semantics cannot be established safely;
* an existing authorization boundary conflicts with D11;
* cross-org/firm isolation cannot be guaranteed;
* deterministic idempotency cannot be guaranteed;
* an architectural conflict with the Blueprint/PO register appears;
* implementation would require reopening P6-2D;
* implementation would require adding Consultant as processing origin;
* implementation would require P6-2F or later-phase work.

Do not solve a governance ambiguity through code.

---

# 29. FINAL IMPLEMENTATION VERDICT

Use exactly one:

### Successful

`P6-2E IMPLEMENTED — READY FOR INDEPENDENT VERIFICATION`

### Partial / blocked

`P6-2E IMPLEMENTATION — BLOCKED / PARTIAL — INDEPENDENT VERIFICATION NOT READY`

Do not self-certify.

Even if all tests pass, the final status must explicitly state:

> Independent verification has NOT been performed in this session.

---

# 30. FINAL RESPONSE

Return:

## Executive Result

* implementation status;
* D8 result;
* D11 result;
* final verdict.

## D11 Event Matrix

Show all five events, trigger, event key, recipients and tests.

## Security

Summarize:

* recipient injection;
* cross-org;
* cross-firm;
* denied-path;
* replay/idempotency;
* alternate-route protection.

## Tests

Give actual:

* focused results;
* relevant regression;
* full unit suite;
* collection count;
* failures/errors/skips;
* exit codes.

## Repository

State:

* branch;
* HEAD;
* files created;
* files modified;
* unrelated/pre-existing modifications;
* commit/push status.

## Limitations / Risks

List every material limitation.

## Scope

Explicitly confirm no:

* P6-2D reopening;
* provenance expansion;
* RLS change;
* migration;
* new roles/capabilities/permissions;
* P6-2F;
* Phase 7/8.

## Stop Condition

Explicitly confirm that implementation stopped at the P6-2E boundary.

---

# ABSOLUTE RULE

**Implement P6-2E only.**

**Do not implement P6-2F.**

**Do not reopen P6-2D.**

**Do not implement F1.**

**Do not redesign architecture.**

**Do not modify RLS.**

**Do not create a migration unless explicitly re-authorized after a genuine blocker.**

**Do not create new roles/capabilities/permissions.**

**Do not commit.**

**Do not push.**

**Do not perform independent verification.**

After implementation, create the required evidence/history artifacts and STOP.

The next action after this session will be a **separate independent P6-2E verification session**.
```

---

## PART B — CLINE RESPONSE

**Response Ref:** `CT-P6-2E-IMPL-20260910-001-R1`
**Response datetime:** 2026-09-10 20:22 → 20:47 (+0600)
**Full evidence:** `docs/cline/CARBONTALLY_P6_2E_IMPLEMENTATION_REPORT.md` (29 sections).

### Executive Result

* **Implementation status:** COMPLETE — D8 verification coverage + D11 (five events) implemented and tested.
* **D8 result:** PASS without any production change — consultants already participate
  through the existing conversation model (participant role `consultant` via an
  active grant); `conversation_kind` remains exactly `org`/`entity`; no consultant
  kind/table/role/permission was added. Three regression tests added.
* **D11 result:** PASS — five lifecycle events persisted durably through the
  existing `notifications` layer with `create_idempotent`, deterministic
  server-generated event keys, server-derived recipients, post-success emission
  and denied-path silence. Six emit sites at five existing trigger points.
* **Final verdict:** `P6-2E IMPLEMENTED — READY FOR INDEPENDENT VERIFICATION`
  (independent verification NOT performed in this session).

### D11 Event Matrix

| # | Event | Trigger | Event key | Recipients | Tests |
| --- | --- | --- | --- | --- | --- |
| 1 | `accepted` | engagement `/accept` (org owner/admin) | `consultant.lifecycle.accepted:engagement:<id>` | engaged firm active members | emit, denied paths, replay |
| 2 | `submitted_to_qc` | `/items/{id}/consultant-submit` | `consultant.lifecycle.submitted_to_qc:item:<id>` | engaged firm members + internal ops | emit, ops recipients, denials, replay, alternate route |
| 3 | `qc_outcome` | `/ops/qc/items/{id}/decision` | `consultant.lifecycle.qc_outcome:item:<id>:approved\|rejected` | engaged firm members | approve, reject (+rework), denials, replay |
| 4 | `customer_decision` | `/items/{id}/customer-review` | `consultant.lifecycle.customer_decision:item:<id>:approved\|rejected` | engaged firm members | approve, reject, denials |
| 5 | `rework` | `/ops/qc/items/{id}/decision` (rejected) and `/items/{id}/consultant-review` (`passed=false`) | `consultant.lifecycle.rework:item:<id>:ct_qc_rejected\|consultant_review_rejected` | engaged firm members | reject→rework, passed review emits nothing, denied review emits nothing |

### Security summary

Recipient injection ignored/rejected; cross-org and cross-firm recipients
excluded; denied/unauthenticated actions emit nothing (401/403 + zero rows);
duplicate/replay suppressed at both the route and the durable layer; alternate
route (`ops/items/{id}/submit-review`) protected; event-key collisions safely
suppressed by the existing unique index. Full matrix in report §22.

### Tests (actual)

| Run | Collected | Passed | Failed/Errors/Skips | Exit |
| --- | --- | --- | --- | --- |
| Focused `test_p6_2e_consultant_lifecycle.py` | 27 | 27 | 0 / 0 / 0 | 0 |
| Targeted regression (`tests/unit/api` + `services` + `infra`) | 1170 | 1170 | 0 / 0 / 0 | 0 |
| Full unit suite (`tests/unit`) | **1654** (baseline 1627 + 27) | 1654 | 0 / 0 / 0 | 0 |

### Repository

* Branch `main`; HEAD `1639121` (unchanged).
* Created: `backend/services/consultant_lifecycle.py`,
  `backend/tests/unit/api/test_p6_2e_consultant_lifecycle.py`,
  `docs/cline/CARBONTALLY_P6_2E_IMPLEMENTATION_REPORT.md`,
  `docs/cline/prompt-history/CT-P6-2E-IMPL-20260910-001.md`.
* Modified (P6-2E hunks only): `backend/api/v3_organizations.py`,
  `backend/api/v3_processing_workflow.py`, `backend/api/v3_operations.py`,
  `backend/tests/unit/api/fakes.py`.
* Unrelated/pre-existing modifications present in the working tree were left
  untouched (284 tracked `M` before and after; porcelain 671 → 673).
* No `git add`, no commit, no push, no reset, no clean.

### Limitations / Risks / Decisions

* Decisions: firm-centric recipient matrix (client is the actor for events 1/4;
  events 3/5 are internal quality gates); rework source vocabulary limited to the
  two governed internal sources; the in-process event bus deliberately unused.
* Limitations/risks: no client-org recipient; per-cycle (second) rework does not
  re-notify; in-app delivery only; fake-based (not live-DB) verification; one
  extra batch read on the CT-QC path. Full list in report §23–§24.

### Status / verdict / stop condition

**Status:** COMPLETE. **Verdict:** `P6-2E IMPLEMENTED — READY FOR INDEPENDENT
VERIFICATION`. **Stop condition:** implementation stopped at the P6-2E boundary —
none of the declared STOP conditions was triggered. No P6-2D reopening, no
provenance/F1 change, no RLS change, no migration, no new
roles/capabilities/permissions, no P6-2F/Phase 7/8 work. The next action is a
separate independent P6-2E verification session.

---

## PART C — FILES, COMMANDS, FINDINGS

### Files inspected (source truth)

`backend/data/notifications.py`, `backend/data/consultants.py`,
`backend/data/organizations.py`, `backend/data/manual_extraction.py`,
`backend/api/v3_messaging.py`, `backend/api/v3_notifications.py`,
`backend/api/consultant_auth.py`, `backend/api/v3_organizations.py`,
`backend/api/v3_processing_workflow.py`, `backend/api/v3_operations.py`,
`backend/api/v3_consultants.py`, `backend/services/work_items.py`,
`backend/services/billing.py`, `backend/infra/event_bus.py`,
`backend/domain/messaging.py`, `backend/domain/operations.py`,
`backend/domain/partners.py`, `backend/domain/staff.py`,
`backend/tests/unit/api/conftest.py`, `backend/tests/unit/api/fakes.py`,
`backend/tests/unit/api/test_p6_1c_engagement_confirmation.py`,
`backend/tests/unit/api/test_p6_2b_1_consultant_review.py`,
`backend/tests/unit/api/test_p6_2b_2_consultant_submission.py`,
`backend/tests/unit/api/test_p6_2b_3_ct_qc_decision.py`,
`backend/tests/unit/api/test_p6_2c_approval_boundary.py`,
`backend/tests/unit/api/test_v3_notifications.py`,
`supabase/migrations/20260902040000_phase5_pe_operational_messaging.sql`,
`supabase/migrations/20260902050000_phase5_notification_event_key.sql`,
`supabase/migrations/00000000000000_init_schema.sql`,
and the authority/evidence documents listed in report §4.

### Files changed

* Created: `backend/services/consultant_lifecycle.py`,
  `backend/tests/unit/api/test_p6_2e_consultant_lifecycle.py`,
  `docs/cline/CARBONTALLY_P6_2E_IMPLEMENTATION_REPORT.md`,
  `docs/cline/prompt-history/CT-P6-2E-IMPL-20260910-001.md`.
* Modified (P6-2E hunks only, 6 hunks total): `backend/api/v3_organizations.py`,
  `backend/api/v3_processing_workflow.py`, `backend/api/v3_operations.py`,
  `backend/tests/unit/api/fakes.py`.

### Files intentionally unchanged

`backend/data/**`, `backend/api/v3_messaging.py`, `backend/api/consultant_auth.py`,
`backend/api/v3_notifications.py`, `backend/infra/event_bus.py`,
`backend/domain/**`, `supabase/migrations/**`, all RLS policies, all React
frontend code, and all P6-2C/P6-2D provenance code.

### Commands

```bash
git rev-parse --abbrev-ref HEAD ; git rev-parse --short HEAD ; git status --porcelain=v1
python -m pytest tests/unit --collect-only -q -p no:cacheprovider      # 1627 (baseline)
python -m pytest tests/unit/api/test_p6_2e_consultant_lifecycle.py -q  # 27 passed (final)
python -m pytest tests/unit/api tests/unit/services tests/unit/infra -q  # 1170 passed
python -m pytest tests/unit -q                                        # 1654 passed
python -m pytest tests/unit --collect-only -q -p no:cacheprovider      # 1654 (after)
git --no-pager diff --stat -- backend/api/v3_organizations.py \
  backend/api/v3_operations.py backend/api/v3_processing_workflow.py \
  backend/tests/unit/api/fakes.py
find backend frontend supabase tests -newermt '2026-09-10 20:20' -type f
```

### Findings

1. Consultants already participate in the existing conversation model — D8 needs
   no production change (confirmed in `_authorize_org_actor`).
2. All five D11 trigger points already existed; no new route/transition was
   invented.
3. `notifications.create_idempotent` + `uq_notifications_event_key` provide the
   durable idempotency boundary required by D11; no migration is needed.
4. `backend/tests/unit/api/fakes.py` previously stubbed `repos.notifications`
   with a no-op `_StubRepo`, so no API test could observe notifications; a
   stateful `MemoryNotifications` fake was required to test D11 end-to-end.
5. `support_staff_user_ids` (existing resolver) was sufficient for internal-ops
   recipients — no new staff-permission resolver was needed.

### Decisions

* Keep the in-process `EventBus` out of the D11 path (non-durable).
* Emit only after authorization + mutation + audit; emit best-effort and logged.
* Recipient matrix is firm-centric (+ internal ops for QC intake), justified in
  report §11.
* Event-key namespace mirrors `notification_type` (`consultant.lifecycle.*`).
* Rework sources are exactly `ct_qc_rejected` and `consultant_review_rejected`.

### Risks

See report §23 (limitations) and §24 (risks) — summarised in PART B.

---

## PART D — INTEGRITY

* Evidence artifacts written and verified present:
  `docs/cline/CARBONTALLY_P6_2E_IMPLEMENTATION_REPORT.md`,
  `docs/cline/prompt-history/CT-P6-2E-IMPL-20260910-001.md`.
* No secrets, credentials, tokens or signed URLs were introduced.
* No commit, no push, no `git add`, no history rewrite.
* Independent verification has NOT been performed in this session.





