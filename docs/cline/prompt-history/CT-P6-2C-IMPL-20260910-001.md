# CT-P6-2C-IMPL-20260910-001 — P6-2C Approval-Boundary Hardening Implementation (durable prompt-history record)

## 1. Prompt Ref

`CT-P6-2C-IMPL-20260910-001`

## 2. Response Ref

`CT-P6-2C-IMPL-20260910-001-R`

## 3. Prompt datetime

10 September 2026 (session date; wall-clock not stated in the prompt). Recorded at approx. 16:25 local (Asia/Dhaka +0600).

## 4. Exact complete prompt text

````text
CARBONTALLY — P6-2C APPROVAL-BOUNDARY HARDENING IMPLEMENTATION
PROMPT REF: CT-P6-2C-IMPL-20260910-001

DATE:
10 September 2026

ROLE:
You are acting as the CarbonTally implementation agent (Cline).

============================================================
SECTION 1 — AUTHORITATIVE IMPLEMENTATION CONTRACT
============================================================

IMPLEMENT EXACTLY:

Contract Ref:
CARBONTALLY-P6-2C-IC-20260910-001

Authoritative contract:
docs/architecture/CARBONTALLY_P6_2C_APPROVAL_BOUNDARY_HARDENING_IMPLEMENTATION_CONTRACT.md

Authoritative PO decisions:
docs/architecture/CARBONTALLY_P6_2_PO_DECISION_REGISTER.md

Relevant architecture authority:
docs/architecture/CARBONTALLY_FINAL_ARCHITECTURE_BLUEPRINT_V1.3.md

Relevant roadmap authority:
docs/architecture/CARBONTALLY_MASTER_PROJECT_ROADMAP_V1.0.md

The Product Owner has ratified:

PO Decision 1 = A
PO Decision 2 = A
PO Decision 3 = A

These decisions and the P6-2C implementation contract are authoritative.

DO NOT reinterpret, broaden, or redesign them.

============================================================
SECTION 2 — PRE-IMPLEMENTATION READ
============================================================

Before modifying anything, read and verify the current versions of:

1. docs/architecture/CARBONTALLY_FINAL_ARCHITECTURE_BLUEPRINT_V1.3.md
2. docs/architecture/CARBONTALLY_MASTER_PROJECT_ROADMAP_V1.0.md
3. docs/architecture/CARBONTALLY_P6_2_PO_DECISION_REGISTER.md
4. docs/architecture/CARBONTALLY_P6_2C_APPROVAL_BOUNDARY_HARDENING_IMPLEMENTATION_CONTRACT.md
5. docs/architecture/CARBONTALLY_P6_2_CONSULTANT_PROCESSING_WORKFLOW_ARCHITECTURE.md
6. docs/architecture/CARBONTALLY_P6_2B_CONSULTANT_REVIEW_QC_WORKFLOW_ARCHITECTURE.md
7. docs/cline/prompt-history/CT-P6-2C-RA-20260910-001.md
8. docs/cline/prompt-history/CT-P6-2C-PO-20260910-001.md

Also inspect the relevant P6-2A/R1 and P6-2B-1/2/3/4 implementation and verification reports.

Before coding, inspect the actual current implementations of the contract's identified files/functions.

Do not assume that the code is unchanged since the contract was created.

============================================================
SECTION 3 — IMPLEMENTATION OBJECTIVE
============================================================

Implement ONLY the approved P6-2C scope.

The objective is to finalize the approval boundary and workflow-stage authorization while preserving:

- automatic processing
- manual processing
- CT-QC boundary
- consultant authorization model
- Processing Entity authorization
- Operations authorization
- organization isolation
- RLS
- provenance
- audit behavior
- billing behavior
- existing API semantics
- existing state-machine semantics except where explicitly authorized by the contract
- existing P6-2B-4 protections

============================================================
SECTION 4 — S1: calculated → approved AUTOMATIC-ONLY
============================================================

Implement the contract's S1 exactly.

The existing:

calculated → approved

transition must remain available where required by the automatic-processing workflow.

DO NOT remove the state-machine transition merely to solve authorization.

The transition must NOT become a generic approval capability.

The customer-item approval route must explicitly distinguish the automatic-processing case using the existing authoritative processing-mode semantics identified by the contract.

Preserve the existing:

- state validation
- organization isolation
- PE-origin protection
- CT-QC protection
- reason validation
- billing behavior
- audit behavior
- concurrency/state-transition behavior

Manual/unauthorized callers must not gain a path to `approved` through `calculated`.

Do not redesign the automatic-processing worker or automatic-processing architecture.

Do not change the underlying meaning of:

processing_mode.item_is_automatic

unless the contract explicitly requires it.

============================================================
SECTION 5 — S2: CONSULTANT REVIEW-STAGE CAPABILITY GATING
============================================================

Implement the contract's S2 exactly.

A consultant with:

- active grant
- zero capabilities

must NOT be able to claim the generic:

review

stage merely because the grant exists.

Use the existing capability model.

Per the approved implementation contract, use:

can_submit

as the existing capability for the consultant `review` stage.

Do NOT create:

- a new capability
- a new role
- a new permission
- a new authorization concept

Use the canonical existing capability resolver rather than duplicating authorization logic.

The resulting authorization chain must remain:

identity → firm → grant → resource → action → stage

A consultant possessing the required existing capability may retain the legitimate review-stage action.

A consultant lacking that capability must be denied.

Do not broaden the capability's meaning elsewhere in the system.

============================================================
SECTION 6 — S3: SOURCE STAGE ALIAS
============================================================

Implement the contract's S3 exactly.

The contract identifies the same defect class for the existing:

source

stage alias.

Apply the existing capability model consistently using:

can_extract

for the source stage.

Do not create a new capability.

Do not expand extraction permissions beyond the existing authorization model.

If inspection reveals that the current architecture intentionally treats source differently from the contract, STOP and report the discrepancy rather than inventing behavior.

============================================================
SECTION 7 — S4: OPERATIONS / PE PARITY
============================================================

Implement only the parity protection explicitly required by the contract.

Verify that Operations and Processing Entity paths cannot bypass the approval boundary or consultant workflow-stage authorization through alternate routes.

Do NOT expand Operations or PE functionality.

Do NOT redesign PE workflow.

Do NOT redesign Operations workflow.

Preserve existing PE-origin protections.

Preserve existing organization/firm isolation.

============================================================
SECTION 8 — S5: TESTS
============================================================

Create the new focused P6-2C test suite identified by the contract:

backend/tests/unit/api/test_p6_2c_approval_boundary.py

The new suite must test the actual implemented behavior, not merely internal helper functions.

At minimum cover the contract's required matrix, including:

1. Consultant cannot reach approved.
2. Processing Entity cannot reach approved.
3. Operations cannot reach approved.
4. Regular organization member cannot reach approved.
5. Viewer cannot reach approved.
6. Unauthenticated actor cannot reach approved.
7. Cross-organization actor cannot reach approved.
8. Consultant with active grant + zero capabilities cannot claim review.
9. Consultant with existing can_submit capability can perform legitimate review-stage action.
10. Source-stage claim requires existing can_extract capability.
11. Manual item without required CT-QC prerequisite cannot reach customer review.
12. reviewed → customer_review remains blocked.
13. Alternate approval endpoint cannot bypass authorization.
14. Actor/identity injection cannot bypass authorization.
15. IDOR/cross-firm resource access remains blocked.
16. Invalid state transitions preserve existing semantics.
17. Concurrent approval attempts remain safe.
18. Automatic-processing approval path remains functional.
19. P6-2B-4 CT-QC guard remains intact.
20. PE-origin guard remains intact.
21. Audit behavior remains correct for denied and successful operations.
22. Provenance remains unchanged.
23. Existing billing behavior remains unchanged.

Where the existing test infrastructure already has fixtures/helpers for these cases, reuse them.

Do not weaken tests simply to obtain green results.

Do not modify existing tests unless the implementation contract explicitly requires such modification.

Prefer adding new focused tests.

============================================================
SECTION 9 — REQUIRED REGRESSION TESTS
============================================================

After focused tests, run the applicable existing regression suites identified in the contract.

At minimum attempt the relevant suites covering:

- P6-2B-4
- P6-2B-3
- P6-2B-2
- P6-2B-1
- P6-2A
- P6-2A-R1
- P6-BILL-1
- v3_processing_workflow
- phase1_core_regressions
- v3_operations
- v3_qc
- processing_origin_qc
- v1_2_dual_origin_workflow
- automatic jobs/payload/services
- operations_auth
- scope_aware_authorization
- pe_auth
- billing_core
- v3_consultants
- P6-1B
- P6-1C

Then run:

pytest tests/unit

The previously established baseline was:

1,576 collected
EXIT 0

Record the current collection count and result accurately.

If the test count changes because of the new test file, explain the expected increase.

Do not claim a baseline test result that you did not actually run.

============================================================
SECTION 10 — BILLING IS FROZEN FOR THIS TASK
============================================================

PO Decision 3 is authoritative.

DO NOT modify billing behavior.

In particular:

- do not modify services/billing.py
- do not add new charges
- do not remove charges
- do not redesign automatic job-review charging
- do not redesign subscription/credit logic
- do not add refund logic
- do not alter billing idempotency

The existing automatic job-review billing behavior remains unchanged.

The billing-policy question is DEFERRED.

Tests should prove that P6-2C did not unintentionally alter billing behavior.

============================================================
SECTION 11 — PROTECTED ARCHITECTURE
============================================================

Do NOT modify unless the contract explicitly requires it:

- Supabase schema
- Supabase migrations
- RLS policies
- database structure
- automatic-processing worker architecture
- automatic-processing services
- billing architecture
- provenance architecture
- audit architecture
- authentication architecture
- new roles
- new capabilities
- new permissions
- Master Roadmap
- Architecture Blueprint

In particular, do not modify:

backend/domain/partners.py

for removal of the existing calculated → approved transition.

The approved policy is to KEEP the transition.

Do not modify:

backend/api/processing_mode.py

predicate semantics.

Reuse the existing semantics.

Do not modify:

backend/api/v3_automatic_processing.py

unless inspection reveals an unavoidable contract violation. If that occurs, STOP and report before changing it.

============================================================
SECTION 12 — EXPECTED PRIMARY CODE AREA
============================================================

The contract identifies:

backend/api/v3_processing_workflow.py

as the primary implementation area.

Inspect the exact current implementation before editing.

Expected bounded areas include:

- start_item
- _STAGE_PERMISSION
- customer_review_item

Apply only the changes required by the P6-2C contract.

Do not refactor unrelated code.

Do not perform opportunistic cleanup.

Do not rename unrelated functions.

Do not change unrelated API behavior.

============================================================
SECTION 13 — HTTP / STATE SEMANTICS
============================================================

Preserve established semantics.

In particular:

- authorization failures must remain authorization failures
- invalid state transitions must retain the existing state-machine semantics
- do not reorder guards in a way that changes established 409/403 behavior unless the contract explicitly requires it
- authorization must happen before mutation
- billing must not occur before successful authorized approval
- denied operations must not create false success audit records
- concurrency protections must remain intact

Trace the existing code and tests before changing guard order.

============================================================
SECTION 14 — NO UI WORK
============================================================

P6-2F owns the final UI/UX and E2E acceptance gate.

Therefore this P6-2C task must NOT redesign or modify:

- consultant UI
- customer UI
- operations UI
- PE UI
- RoleRoute
- frontend navigation
- buttons
- review screens
- E2E UI flows

Server-side authorization is the authority.

UI changes are not a substitute for server-side enforcement.

If UI changes appear necessary for correctness, STOP and report them as a future P6-2F item rather than expanding this task.

============================================================
SECTION 15 — NO P6-2D/E/F WORK
============================================================

Do NOT implement:

P6-2D
- entitlement timing
- D6
- D7
- broader provenance changes

P6-2E
- D8
- D11
- D39/D40 notification vocabulary or unrelated workflow features

P6-2F
- consultant UI
- final UI/UX
- E2E acceptance
- final Phase 6 security acceptance

Do not implement Phase 7 or Phase 8.

============================================================
SECTION 16 — STOP CONDITIONS
============================================================

STOP IMMEDIATELY and report if any of the following occurs:

1. The implementation contract conflicts with the current authoritative Blueprint.
2. The PO decision record does not match the policy supplied in this prompt.
3. A new role/capability/permission appears necessary.
4. A schema migration appears necessary.
5. RLS changes appear necessary.
6. Billing changes appear necessary.
7. Automatic-processing architecture changes appear necessary.
8. The existing processing_mode semantics must be changed.
9. The contract requires behavior that contradicts an existing ratified decision.
10. A protected P6-2B-4 security invariant would need to be weakened.
11. An existing test must be weakened or deleted.
12. The implementation would require P6-2D/E/F scope.
13. You discover a material ambiguity not resolved by the contract.

If any stop condition is triggered:

DO NOT improvise.

Do not partially implement an unapproved interpretation.

Report the exact conflict and STOP.

============================================================
SECTION 17 — IMPLEMENTATION REPORT
============================================================

After successful implementation and testing, create a durable implementation report under:

docs/cline/

Use an explicit filename such as:

CARBONTALLY_P6_2C_APPROVAL_BOUNDARY_HARDENING_IMPLEMENTATION_REPORT.md

The report must contain:

1. Contract Ref
2. Prompt Ref
3. Implementation date/time
4. Objective
5. PO decisions relied upon
6. Files inspected
7. Files changed
8. Files intentionally unchanged
9. Detailed implementation changes
10. Security behavior
11. Workflow behavior
12. Automatic-processing behavior
13. Consultant capability behavior
14. Billing preservation
15. Provenance preservation
16. Audit behavior
17. Focused tests
18. Regression tests
19. Full-unit test result
20. Test counts
21. Failures, if any
22. Warnings, if any
23. Risks
24. Scope verification
25. Remaining deferred items
26. Final verdict
27. STOP-condition confirmation

Do not fabricate verification results.

============================================================
SECTION 18 — MANDATORY PROMPT-HISTORY RECORD
============================================================

For THIS prompt create:

docs/cline/prompt-history/CT-P6-2C-IMPL-20260910-001.md

Use:

Prompt Ref:
CT-P6-2C-IMPL-20260910-001

Response Ref:
CT-P6-2C-IMPL-20260910-001-R

The prompt-history record MUST contain:

1. Exact Prompt Ref
2. Exact Response Ref
3. Prompt datetime
4. Exact complete prompt text
5. Complete Cline response
6. Detailed task breakdown
7. Documents/files inspected
8. Files changed
9. Files unchanged
10. Commands executed
11. Tests executed
12. Findings
13. Implementation decisions
14. Risks
15. Final status/verdict
16. Scope verification
17. Stop-condition confirmation

The prompt-history record is an evidence/history artifact.

Do not treat it as a replacement for the authoritative implementation report or decision register.

============================================================
SECTION 19 — GIT / COMMIT POLICY
============================================================

Do NOT commit.

Do NOT push.

Do NOT create a commit merely because implementation is complete.

At the end report:

- branch
- HEAD
- git status
- changed files
- whether any unexpected files changed

If unrelated pre-existing modifications exist, do not overwrite or revert them.

Do not clean up unrelated user changes.

============================================================
SECTION 20 — FINAL VERDICT
============================================================

If implementation and required tests pass:

P6-2C IMPLEMENTED — READY FOR INDEPENDENT VERIFICATION

If implementation completes but verification has failures:

P6-2C IMPLEMENTED — VERIFICATION BLOCKED

If a contract/authority conflict occurs:

P6-2C BLOCKED — AUTHORITY/CONTRACT CONFLICT

If implementation cannot safely proceed:

P6-2C BLOCKED — IMPLEMENTATION STOP CONDITION

Do not claim P6-2C VERIFIED.

This task is implementation only.

Independent verification is a separate subsequent gate.

============================================================
SECTION 21 — FINAL RESPONSE FORMAT
============================================================

Return exactly this structure:

# P6-2C IMPLEMENTATION REPORT

## 1. Verdict

[one of the approved verdicts]

## 2. Contract

CARBONTALLY-P6-2C-IC-20260910-001

## 3. PO Decisions Applied

- D1 = A
- D2 = A
- D3 = A

## 4. Implementation Summary

[concise but technically precise]

## 5. Files Changed

[exact paths]

## 6. Files Unchanged / Protected

[exact paths or categories]

## 7. Focused Tests

[commands/results]

## 8. Regression Tests

[commands/results]

## 9. Full Unit Suite

[actual collection count and result]

## 10. Security Verification Performed

[specific matrix/results]

## 11. Billing

Confirm billing was not modified.

## 12. Schema / RLS

Confirm whether unchanged.

## 13. Git State

branch / HEAD / status

## 14. Implementation Report

exact report path

## 15. Prompt History

exact prompt-history path and Response Ref

## 16. Scope Check

Confirm whether any P6-2D/E/F, Phase 7/8, UI, billing redesign, schema/RLS, or unrelated work occurred.

## 17. Independent Verification

State explicitly:

"Independent verification has NOT been performed by this implementation task."

## 18. STOP CONDITION

State explicitly:

"P6-2C implementation is complete for this task. STOP. Do not perform independent verification, do not begin P6-2D, and do not commit or push."

============================================================
FINAL INSTRUCTION
============================================================

Implement EXACTLY:

CARBONTALLY-P6-2C-IC-20260910-001

Apply the three ratified PO decisions:

D1 = A
D2 = A
D3 = A

Do not reinterpret the policy.

Do not expand scope.

Do not modify billing.

Do not modify schema/RLS.

Do not create new capabilities.

Do not modify the automatic-processing architecture.

Do not perform UI/E2E work.

Do not start P6-2D/E/F.

Create the required focused tests.

Run the required regression tests.

Create the implementation report.

Create the mandatory prompt-history record preserving this complete prompt and your complete response.

Do NOT perform independent verification.

Do NOT commit.

Do NOT push.

When implementation and testing are complete, STOP and report:

P6-2C IMPLEMENTED — READY FOR INDEPENDENT VERIFICATION
````

## 5. Complete Cline response

````markdown
# P6-2C IMPLEMENTATION REPORT

## 1. Verdict

**P6-2C IMPLEMENTED — READY FOR INDEPENDENT VERIFICATION**

## 2. Contract

`CARBONTALLY-P6-2C-IC-20260910-001`

## 3. PO Decisions Applied

- D1 = A
- D2 = A
- D3 = A

## 4. Implementation Summary

Two bounded edits in `backend/api/v3_processing_workflow.py` implement the
contract:

1. **S1 (PO-P6-2C-D1)** — `customer_review_item`: for source status `calculated`,
   approval is now *explicitly* conditional on the item being AUTOMATIC via the
   existing, unchanged predicate `processing_mode.item_is_automatic`. Manual work
   gets **403**; the state machine, PE-origin guard, CT-QC prerequisite guard,
   reason validation, D37 charge (single site, before-write) and concurrency
   semantics are untouched.
2. **S2/S3 (PO-P6-2C-D2)** — `_STAGE_PERMISSION` now maps `review → submit`
   (`can_submit`) and `source → extract` (`can_extract`). `start_item` already
   passes this through the canonical resolver
   `ensure_consultant_processing_authorized`, so an active grant alone no longer
   yields the `review`/`source` stage claim. No new capability/role/permission;
   org-member/internal-staff behaviour unchanged; the `consultant-review` action
   endpoint (permission-free by ratified P6-2B-1 semantics) is untouched.

**S4** parity verified by tests (ops `can_review`; PE entity route rejects
review/validation). **S5** a 30-test focused suite was added. No billing, schema,
RLS, provenance, audit, automatic-processing, UI or test-modification work
occurred.

## 5. Files Changed

- `backend/api/v3_processing_workflow.py` (S1 + S2/S3)
- `backend/tests/unit/api/test_p6_2c_approval_boundary.py` (new, 30 tests)
- `docs/cline/CARBONTALLY_P6_2C_APPROVAL_BOUNDARY_HARDENING_IMPLEMENTATION_REPORT.md` (new)
- `docs/cline/prompt-history/CT-P6-2C-IMPL-20260910-001.md` (new)

## 6. Files Unchanged / Protected

`backend/domain/partners.py` (the `calculated → approved` entry is KEPT),
`backend/api/processing_mode.py` (predicate semantics untouched),
`backend/api/consultant_auth.py`, `backend/api/v3_automatic_processing.py`,
`backend/workers/automatic_processing.py`, `backend/services/automatic_processing.py`,
`backend/services/billing.py`, `backend/api/v3_pe.py`,
`backend/data/manual_extraction.py`, `backend/data/document_processing.py`,
`backend/api/dependencies.py`, all `supabase/**`, all pre-existing tests, all
frontend files, the Master Roadmap and the Architecture Blueprint.

## 7. Focused Tests

`pytest -q tests/unit/api/test_p6_2c_approval_boundary.py` → **30 passed**
(consultant/PE/ops/member/viewer/unauth/cross-org ⇒ `approved` denied; manual
`calculated` approve+reject denied; automatic approve works with one charge;
`ct_qc_approved` manual approve works; double approval idempotent; alternate
endpoints; actor injection; IDOR/cross-firm; invalid transition 409;
`reviewed → customer_review` 409; PE-origin guard; job-review stamp without
charge; 0-flag consultant `review` denied; wrong-flag denied; `can_submit`
allowed; `source` requires `can_extract`; org-member/internal-staff unchanged;
denied ⇒ no success audit / success ⇒ audit; provenance unchanged).

## 8. Regression Tests

`pytest -q <23 contract-listed suites>` → **408 tests, 100% pass, EXIT 0**
(P6-2B-4/3/2/1, P6-2A, P6-2A-R1, P6-BILL-1, `v3_processing_workflow`,
`phase1_core_regressions`, `v3_operations`, `v3_qc`, `processing_origin_qc`,
`v1_2_dual_origin_workflow`, automatic jobs/payload/services, `operations_auth`,
`scope_aware_authorization`, `pe_auth`, `billing_core`, `v3_consultants`,
P6-1B, P6-1C). **No existing test modified.**

## 9. Full Unit Suite

`pytest -q tests/unit` → **1,606 collected, EXIT 0** (baseline 1,576 + 30 new).

## 10. Security Verification Performed

The 23-row contract matrix (§15) is executed at runtime by the focused suite with
the results listed in §7/§8 above; every denial asserts zero mutation, zero charge
and no success audit.

## 11. Billing

Billing was **not modified**: `services/billing.py` untouched; no charge added,
removed or redesigned; idempotency unchanged. Tests prove one charge at Customer
Approval, none on denials, and unchanged (non-charging) automatic job-review.

## 12. Schema / RLS

**Unchanged.** No migration, schema, constraint or RLS policy was added or
modified; no migration file was touched.

## 13. Git State

Branch `main`; HEAD `1639121` (unchanged); only the four files in §5 changed
(3 new + 1 modified); no unexpected files changed; nothing committed or pushed.
Pre-existing unrelated working-tree modifications were left untouched.

## 14. Implementation Report

`docs/cline/CARBONTALLY_P6_2C_APPROVAL_BOUNDARY_HARDENING_IMPLEMENTATION_REPORT.md`

## 15. Prompt History

`docs/cline/prompt-history/CT-P6-2C-IMPL-20260910-001.md` — Response Ref
`CT-P6-2C-IMPL-20260910-001-R`

## 16. Scope Check

No P6-2D/E/F, Phase 7/8, UI, billing redesign, schema/RLS, or unrelated work
occurred.

## 17. Independent Verification

"Independent verification has NOT been performed by this implementation task."

## 18. STOP CONDITION

"P6-2C implementation is complete for this task. STOP. Do not perform independent
verification, do not begin P6-2D, and do not commit or push."
````

## 6. Detailed task breakdown

1. **Pre-implementation read** — verified the blueprint, roadmap, PO decision
   register, the P6-2C contract, the P6-2/P6-2B architectures, both prior
   prompt-history records, the P6-2A/R1 + P6-2B-1..4 evidence reports, and the
   **current** code of the contract's target functions (confirmed unchanged since
   the contract was written).
2. **S1 implemented** — explicit automatic-only guard for `calculated → approved`
   in `customer_review_item`, placed after the state machine and before the
   charge, reusing `processing_mode.item_is_automatic` unchanged.
3. **S2/S3 implemented** — `_STAGE_PERMISSION` extended with `review → submit`
   and `source → extract`; the canonical resolver already consumed that map, so
   no authorization logic was duplicated and no capability was created.
4. **S4 verified** — ops `can_review` parity and the PE entity route's rejection
   of review/validation/approval states are asserted by new tests (no PE/ops
   functional change).
5. **S5 implemented** — new focused suite (30 tests) covering the contract matrix.
6. **Tests run** — focused suite (30 passed), targeted regression group (408
   passed, EXIT 0), full unit suite (1,606 collected, EXIT 0).
7. **Documents produced** — implementation report + this prompt-history record.
8. **Stopped** — no commit, no push, no independent verification.

## 7. Documents/files inspected

Listed in §6 of the implementation report (blueprint, roadmap, PO decision
register, P6-2C contract, P6-2/P6-2B architectures, both prior prompt-history
records, P6-1C/P6-2A/R1/P6-2B-1..4 reports, and the current source of
`v3_processing_workflow.py`, `domain/partners.py`, `processing_mode.py`,
`consultant_auth.py`, `v3_operations.py`, `v3_pe.py`,
`v3_automatic_processing.py`, `auth.py`, `operations_auth.py`, plus the P6-2B-4
test file for fixture patterns).

## 8. Files changed

| Path | Action |
|---|---|
| `backend/api/v3_processing_workflow.py` | modified (S1, S2/S3) |
| `backend/tests/unit/api/test_p6_2c_approval_boundary.py` | created (30 tests) |
| `docs/cline/CARBONTALLY_P6_2C_APPROVAL_BOUNDARY_HARDENING_IMPLEMENTATION_REPORT.md` | created |
| `docs/cline/prompt-history/CT-P6-2C-IMPL-20260910-001.md` | created (this record) |

## 9. Files unchanged

`backend/domain/partners.py` (map kept), `backend/api/processing_mode.py`,
`backend/api/consultant_auth.py`, `backend/api/v3_automatic_processing.py`,
automatic worker/services, `backend/services/billing.py`, `backend/api/v3_pe.py`,
`backend/data/manual_extraction.py`, `backend/data/document_processing.py`,
`backend/api/dependencies.py`, `backend/auth.py`, `backend/api/operations_auth.py`,
all `supabase/**` (schema/migrations/RLS), all pre-existing tests, all frontend
files, the Master Roadmap, the Architecture Blueprint.

## 10. Commands executed

`py_compile` (implementation + suite); `pytest -q tests/unit/api/test_p6_2c_approval_boundary.py`;
`pytest -q <23 regression suites>`; `pytest -q tests/unit`;
`pytest --collect-only -q tests/unit` (count); read-only inspection via
`grep`/`sed -n`/`ls`/`stat`/`git`.

## 11. Tests executed

| Command | Result |
|---|---|
| focused P6-2C suite | **30 passed** |
| targeted regression group (23 suites) | **408 passed, EXIT 0** |
| full `pytest -q tests/unit` | **1,606 collected, EXIT 0** |

## 12. Findings

1. The contract's target code was **unchanged** since the contract was authored,
   so the contract applied directly (no stop condition).
2. `_STAGE_PERMISSION` lacked both `review` **and** the `source` alias — the two
   defects the contract's S2/S3 address; `WORKFLOW_STAGES` already permits both
   stage names, so the gap was real (not a 422 dead path).
3. Ops/internal stage-claim parity already existed (`require_internal_staff` +
   `can_review`); the PE entity route already rejects review/validation and all
   approval states — S4 needed tests, not code.
4. The consultant `review` **action** endpoint (`consultant-review`) intentionally
   requires no capability (ratified P6-2B-1 self-review); the new gating applies
   only to the generic `review` **stage claim**, so no ratified decision was
   contradicted.
5. The automatic job-review route still does not charge — recorded as
   **DEFERRED** (PO-D3), unchanged.

## 13. Implementation decisions

- Reused `processing_mode.item_is_automatic` **as-is** (no predicate change) and
  placed the S1 guard after the state machine (preserving the 409/403 ordering)
  and before the charge.
- Expressed S2/S3 purely as additions to the existing `_STAGE_PERMISSION` map so
  the **canonical resolver** remains the only consultant authorization logic.
- Did not modify the state map (PO-D1 keeps the transition).
- Did not touch billing (PO-D3).
- Did not modify any existing test.

## 14. Risks

As listed in the implementation report §23 (guard placement; over-gating; the
`source` parity extension being one line; billing drift; predicate dependence) —
all mitigated and test-proven.

## 15. Final status / verdict

**P6-2C IMPLEMENTED — READY FOR INDEPENDENT VERIFICATION**

## 16. Scope verification

Exactly contract scope S1–S5. No P6-2D/E/F, Phase 7/8, UI/E2E, billing redesign,
schema/migration/RLS change, provenance/audit redesign, automatic-processing
architecture change or new capability/role/permission.

## 17. Stop-condition confirmation

No stop condition was triggered. **"P6-2C implementation is complete for this
task. STOP. Do not perform independent verification, do not begin P6-2D, and do
not commit or push."** Nothing was committed or pushed.





