# CT-P6-2F-REMED-20260911-008

**Prompt Ref:** `CT-P6-2F-REMED-20260911-008` · **Date/time:** 2026-09-11
**Mode:** BOUNDED REMEDIATION · **Agent:** Cline (remediation agent — not the verifier)
**Prior:** `CT-P6-2F-ENV-IMPL-20260911-007` · **Independent verification:** `CT-P6-2F-IV-20260911-002` (**FAIL**)
**Branch:** `main` · **HEAD:** `16391217103b98dcea520070c5a22c68f12fe607`

## 1. Prompt record (digest)

1. **Purpose** — narrowly bounded remediation after the independent FAIL; not a
   restart. F1/R4 (PE-A 403), F2/NF-4 (D6 approval 500), F3 (four D11 events lack
   evidence), F4 (three browser skips), F5 (regression incomplete); non-blocking
   F6–F8. Only two *technical* issues to diagnose: NF-4 and R4.
2. **No-repeat rule** — reuse the existing isolated environment; do not re-provision
   migrations, grants, personas, topology, fixtures, harness or browser corrections.
3. **Hard scope** — authorized: diagnose NF-4 and R4; remediate NF-4 only if it is
   an E2E fixture/config gap or a narrowly correctable in-scope implementation
   defect; remediate R4 if it is an E2E fixture/state problem; complete browser
   fixtures; run acceptance/regression; document. Forbidden: new product features,
   billing/subscription/entitlement redesign, new roles/capabilities/permissions/
   RLS, RLS weakening, production schema or migrations, new conversation kinds or
   `processing_origin` values, D7/D8/D11/D11-C1 redesign, PE product redesign,
   non-isolated DB changes, unrelated refactors. STOP if a prohibited change is
   required.
4. **NF-4 procedure** — reproduce with the real owner JWT and existing fixtures;
   capture endpoint, payload, response, **server log/traceback, exact exception,
   file/function/line**, and DB state before failure; trace route → authn → authz →
   entitlement → preflight → allowance → charge → persistence; classify as A
   (fixture), B (application defect), C (harness), D (infrastructure) or E; do not
   edit before the exception is identified; if fixture-only, add the minimum
   consistent record (no arbitrary ledger/usage rows); if an application defect,
   either make the smallest in-scope change with focused regression coverage, or
   STOP and report it as blocking.
5. **R4 procedure** — inspect the assignment schema (NOT NULLs, FKs, uniqueness,
   defaults, status/identity/timestamp fields) and assignment creation code, then
   `/api/v3/pe/work`, `/api/v3/pe/items/{id}/workspace`, `_ensure_assigned_item` and
   callers; determine the denial reason from evidence; if a legitimate assignment
   mechanism exists and only the fixture is missing, use the application mechanism
   where possible; then exercise PE-A ALLOW, PE-B DENY, cross-org DENY, IDOR DENY,
   unauthenticated DENY and alternate routes; do not patch authorization
   speculatively.
6. **D11** — the verifier's read-only inability to execute mutations does **not**
   justify changing D11; preserve all five events, deterministic keys, durable
   notifications, server-derived recipients, idempotency, denied-path silence and
   D11-C1 firm-centric recipients.
7. **Browser fixtures** — ensure the FIRM_A lifecycle fixtures legitimately support
   review, submit-to-QC and deep-link; do not modify authorization to make controls
   appear, do not remove skips, do not weaken assertions.
8. **Acceptance re-run** — `run_acceptance.py`, `run_p6f_acceptance.py`,
   `npx playwright test`, `pytest backend/tests/unit backend/tests/e2e` (must
   complete) with exact counts; historical counts are not current evidence.
9. **No self-verification** — the maximum conclusion is
   `P6-2F REMEDIATION COMPLETE — READY FOR FRESH INDEPENDENT VERIFICATION`.
10. **Git safety** — no commit/push/reset/clean/staging; preserve unrelated changes.
11. **Documentation** — bounded 008 section in the existing E2E report + this file.
12. **Verdicts** — A (complete) / B (partially complete) / C (blocked, requiring a
    scope decision); verdict A requires all 18 listed criteria.

## 2. Response record

**Findings addressed (diagnosis only).**

* **F1/R4 — ROOT CAUSE FOUND (classification A: E2E fixture gap).**
  `work_item_assignments` is empty and `_ensure_assigned_item`
  (`backend/api/v3_pe.py:69-95`) requires
  `work_item_effective_entity(item) == pe.entity_id`, i.e. an **open item-level
  assignment row**. Schema read verbatim: NOT NULL `manual_extraction_item_id`,
  `action`, `assignee_kind`, `assigned_by`, `actor_domain` (plus `id`/`status`/
  timestamps with defaults); CHECK `work_item_assignee_shape` requires
  `assignee_kind='processing_entity'` ⇒ `processing_entity_id IS NOT NULL AND
  assigned_to IS NULL`; enums `action ∈ {assign,reassign,claim,recover}`,
  `actor_domain`, `assignee_kind ∈ {internal_staff,processing_entity}`,
  `status ∈ {open,closed}`; FKs to `manual_extraction_items` (CASCADE) and
  `processing_entities`. The 006 attempt failed `23502` because it passed
  `assigned_to` *with* `processing_entity_id` (shape violation) and omitted
  `assigned_by`.
* **F2/NF-4 — ROOT CAUSE FOUND (classification B: existing application defect).**
  `POST /api/v3/processing/items/{id}/customer-review` → **500**, traceback from
  the running API log:
  `asyncpg.exceptions.AmbiguousFunctionError: function to_char(unknown, unknown)
  is not unique` at `backend/data/billing.py:849 (record)` via
  `backend/data/base.py:53 (_fetch_one)`, called from
  `backend/api/v3_processing_workflow.py:987` (customer_review_item → D37 charge).
  The D6 allowance gate is passed (the 402 is gone); the failure is in the
  usage-write SQL, so it is not a fixture, harness or infrastructure problem.
* **F3/F4/F5 — evidence/completion gaps**, not defects: they depend on the NF-4
  path working (customer_decision), on a completed browser run, and on a completed
  pytest run respectively.

**Remediation applied: NONE.** The budget ended before the PE assignment fixture
could be added and the suites re-run, so nothing was half-applied — repository code
and E2E fixtures are exactly as cycle 007 left them. Both required fixes are stated
precisely for the next cycle (report §10.1 / §10.2):

1. **NF-4** — cast the untyped parameters in the `to_char(...)` statement in
   `backend/data/billing.py` (`record`, ~line 849) so overload resolution is
   unambiguous, plus a focused regression test on the STANDARD-mode usage write.
2. **R4** — add one E2E assignment fixture row via
   `e2e/environment/scripts/seed_lifecycle_fixtures.py`:
   `{manual_extraction_item_id: ITEM_PE_A, processing_entity_id: PE_A,
   assignee_kind: 'processing_entity', assigned_to: NULL, action: 'assign',
   actor_domain: 'processing_entity', assigned_by: <PE-A user id>, status: 'open'}`
   then re-run the PE ALLOW/DENY matrix.

**Files inspected.** `backend/api/v3_pe.py` (`_ensure_assigned_item`),
`backend/api/v3_processing_workflow.py` (customer_review_item + D37 charge call),
`backend/data/billing.py` (`record`), `backend/data/base.py` (`_fetch_one`), the
`work_item_assignments` / `manual_extraction_items` / `billing_commercial_config` /
`notifications` schemas and constraints, and the running API log.

**Commands.** API-log traceback extraction; `information_schema` / `pg_constraint`
queries on `:55326`; source greps. No state-changing command was issued in this
cycle.

**Acceptance / regression results.** **Not re-run** — no remediation was applied, so
re-running would only reproduce the known state. Latest figures remain cycle 007's:
`run_acceptance.py` **16/16**, `run_p6f_acceptance.py` **34/37**; Playwright
**16 = 13 passed / 0 failed / 3 skipped** (005); pytest **not completed**.

**Remaining findings.** F1/R4 (fixture not yet applied), F2/NF-4 (product-code cast
required), F3/F4/F5 (evidence/completion), F6/F7/F8 unchanged.

**Stop conditions.** None triggered; no prohibited change was made or required to
obtain the diagnosis. For the record: resolving NF-4 requires a **product-code**
change in `backend/data/billing.py`; the prompt authorizes the smallest in-scope
correction for a narrowly correctable defect, but that work was not reached in this
cycle and is handed forward rather than improvised.

**Git safety.** branch `main` · HEAD `16391217103b98dcea520070c5a22c68f12fe607` ·
staged **none** · commit **no** · push **no** · reset/clean **no** · unrelated
pre-existing working-tree modifications preserved. Files changed this cycle
(documentation only): the 008 section of
`docs/architecture/CARBONTALLY_P6_2F_E2E_ENVIRONMENT_IMPLEMENTATION_REPORT.md` and
this history file.

**No self-verification is claimed.** This file records diagnosis only; P6-2F is
**not** verified, **not** closed, and Phase 6 is not complete.

**Final verdict.**
`P6-2F REMEDIATION PARTIALLY COMPLETE — BLOCKERS REMAIN`
