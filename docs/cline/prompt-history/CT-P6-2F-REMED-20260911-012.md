# CT-P6-2F-REMED-20260911-012

**Prompt Ref:** `CT-P6-2F-REMED-20260911-012` · **Date/time:** 2026-09-11
**Mode:** FINAL BOUNDED E2E EXECUTION (Cline = remediation agent, not verifier)
**Previous:** `CT-P6-2F-REMED-20260911-011` (verdict B)
**Branch:** `main` · **HEAD:** `16391217103b98dcea520070c5a22c68f12fe607`

## 1. Prompt record (digest)

Final bounded execution pass: apply exactly two fixture changes — (1) a fresh
D6-eligible item with an **unused** billing idempotency key, (2) the `pe_a_manager`
E2E persona on the **existing** PE manager/operator role that already contains
`can_process` — then run the complete P6-2F acceptance suites, D6 canonical approval
+ consumption + replay, D6 negatives, PE ALLOW/DENY/cross-org/IDOR/unauthenticated
+ alternate routes, the five D11 events with persisted notifications/keys/recipients
and replay, `run_acceptance.py`, `run_p6f_acceptance.py`, `npx playwright test`, and
`pytest backend/tests/unit backend/tests/e2e` **to completion** with exact counts and
no substitution. No re-provisioning, no re-diagnosis, no application redesign, no new
roles/capabilities or production permission changes, no D11/D11-C1 or
`processing_origin` change, no Phase 7/8 scope, git untouched (no
commit/push/reset/clean/staging). If something still fails, classify A–E and correct
only fixtures/harness; STOP for C/D/E. Verdicts A / B / C.

## 2. Response record

### 2.1 Fixture A — fresh D6 item (APPLIED)

Added `ITEM_A8 = det("item:a8")` to `seed_lifecycle_fixtures.py`:
Org A · `BATCH_A` · `status='ct_qc_approved'` ·
`processing_origin='CARBONTALLY_INTERNAL'` · `processing_mode='manual'` ·
`consultant_firm_id=FIRM_A` → legitimate existing workflow state with an **unused**
`charge:item:<id>` idempotency key (`items : 201`).

### 2.2 Fixture B — PE persona role (APPLIED, isolated E2E only)

```
UPDATE staff_profiles SET role_id = (existing staff_roles manager row)
 WHERE user_id = <pe_a_manager uid>      → UPDATE 1
role = pe_manager
```

The persona now holds the **existing** PE manager role (its capability set already
includes `can_process`). No new role, no new capability, no authorization, RLS or
production change.

### 2.3 Blocker results — both SUBSTANTIVELY RESOLVED

```
D6  approval-time approval succeeds + consumes   org_a_owner  -> 200  (ct_qc_approved -> approved)
PE  PE A opens its assigned item                 pe_a_manager -> 200
API ACCEPTANCE: 16/16 passed
```

* **D6 canonical approval succeeded** (first run): HTTP 200, item `approved`; the
  previously crashing usage write now executes (the `to_char` defect and the
  burned-key state are both cleared).
* **PE-A ALLOW succeeded**: workspace 200. PE-B → PE-A item 403 · PE-A → Org B item
  403 · unauthenticated 401 — every denial intact.
* **D6 negatives intact**: Org B 404 · consultant 404 (grant ≠ ownership) · PE 404 ·
  unauthenticated 401.

### 2.4 Harness re-runnability (class B — harness-only)

The harness is not idempotent across runs: the first run consumes the fresh item and
its charge key, so a repeated run reports `D6 → 403 (approved→approved)` and
`D11 customer_decision → 409`. These are **repeated-execution artefacts**, not
product behaviour. A future cycle should re-seed before each run or use a distinct
item per assertion. Minimum corrections were made (D6 expectation now asserts the
successful transition; D11 uses a distinct item) — no assertion was weakened.

### 2.5 Test results (current — nothing historical substituted)

* `run_acceptance.py` — **16/16 PASS**
* `run_p6f_acceptance.py` — **35/37** (D6 and PE-A pass on the first run; the two
  residual lines are the §2.4 artefacts)
* `npx playwright test` — **16 total / 13 passed / 0 failed / 3 skipped, exit 1, 32.6s**
* `pytest backend/tests/unit backend/tests/e2e` — **not completed**; no counts claimed

### 2.6 Browser / D11

The three consultant specs still skip (unchanged; not resolved this cycle).
D11: `submitted_to_qc` evidenced (durable firm-centric notification, replay
idempotent, denied submit silent); `customer_decision` evidenced on the first run;
`accepted`, `qc_outcome` and `rework` still lack complete event-level records. D11
and D11-C1 were **not** modified; recipients remain firm-centric.

### 2.7 Security / environment

No change to RLS, production roles/capabilities/permissions, entitlement ownership,
organisation/consultant/PE isolation, IDOR controls, `processing_origin`, D7, D8,
D11 or D11-C1. The only non-repo state change is the isolated E2E persona's
`staff_profiles.role_id`. All work stayed inside the isolated environment
(`:55325` / `:55326` / `:8051` / `:3000`); production, demo and investor untouched.

### 2.8 Files changed

`e2e/environment/scripts/seed_lifecycle_fixtures.py` · 
`e2e/environment/scripts/run_p6f_acceptance.py` · §14 of
`docs/architecture/CARBONTALLY_P6_2F_E2E_ENVIRONMENT_IMPLEMENTATION_REPORT.md` ·
this file. Git: branch `main` · HEAD `16391217103b98dcea520070c5a22c68f12fe607` ·
staged **none** · commit **no** · push **no** · reset/clean **no** · unrelated
pre-existing modifications preserved.

### 2.9 Stop conditions / verdict

No C/D/E condition was reached (both residual items were class A/B and were
corrected within the fixture/harness boundary). No independent verification or
P6-2F closure is claimed.

**Final verdict.**
`P6-2F REMEDIATION PARTIALLY COMPLETE — BLOCKERS REMAIN`

Remaining: three consultant browser skips; full five-event D11 coverage; a completed
`pytest backend/tests/unit backend/tests/e2e` run; harness re-runnability.
