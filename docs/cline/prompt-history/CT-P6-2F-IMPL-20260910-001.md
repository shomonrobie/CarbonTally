# CarbonTally — Durable Prompt History

**Prompt Ref:** `CT-P6-2F-IMPL-20260910-001`
**Prompt datetime:** 2026-09-10 (P6-2F implementation session start)
**Agent/session:** Cline — fresh P6-2F implementation session
**Response Ref:** `CT-P6-2F-IMPL-20260910-001-R1`
**Response datetime:** 2026-09-10 23:00 → 2026-09-11 01:00 (+0600)
**Phase / Checkpoint / Gate:** Phase 6 / CP3 / P6-2F — Full UI/UX + E2E Security Acceptance
**Mode:** IMPLEMENTATION
**Verdict:** `P6-2F IMPLEMENTED — READY FOR INDEPENDENT VERIFICATION`
**Status:** COMPLETE — stopped at the P6-2F boundary

---

## PART A — EXACT PROMPT (verbatim)

### Chunk 1 (§0 → §3)

```markdown
# CarbonTally — P6-2F Full UI/UX + E2E Security Acceptance Implementation

**Prompt Ref:** `CT-P6-2F-IMPL-20260910-001`
**Date:** 2026-09-10
**Phase:** Phase 6
**Gate:** P6-2F — Full UI/UX + E2E Security Acceptance
**Mode:** IMPLEMENTATION
**Session:** FRESH IMPLEMENTATION SESSION
**Product Owner:** Shomon Robie
**Implementation Agent:** Cline

---

# 0. MANDATE

You are now authorized to **IMPLEMENT P6-2F**.

This is a fresh implementation session.

The preceding governance work has already been completed:

* P6-2C — independently verified.
* P6-2D / CP1 — closed.
* P6-2E / CP2 — closed.
* P6-2E independent verification — **PASS WITH NON-BLOCKING FINDINGS**.
* D11-C1 — explicitly PO-ratified.
* IV-C1 — closed by explicit PO adjudication.
* P6-2F read-only preflight — completed.
* P6-2F preflight verdict — **`CP2 CLOSED — P6-2F PREFLIGHT READY`**.
* P6-2F has no blocking preflight finding.
* P6-2F is the next authorized Phase-6 implementation gate.

### IMPORTANT — IMPLEMENTATION AUTHORIZATION IS ALREADY CONFIRMED

Do **NOT** perform another authorization/preflight-only session.

Do **NOT** return with "awaiting authorization."

Do **NOT** stop after reconnaissance.

Do **NOT** merely review the P6-2F requirements.

Proceed with the bounded **P6-2F implementation checkpoints** defined below.

The authoritative preflight report is:

`docs/architecture/CARBONTALLY_CP2_CLOSURE_P6_2F_PREFLIGHT_REPORT.md`

The authoritative Phase-6 architecture/roadmap/contract documents remain controlling.

---

# 1. WHAT P6-2F IS

P6-2F is the **final Phase 6 UI/UX + E2E Security Acceptance Gate**.

It is not merely a frontend polish task.

It must establish the implementation and test infrastructure necessary to demonstrate that CarbonTally's Phase-6 workflows operate correctly through the UI/browser while preserving the security and processing invariants established by P6-2C, P6-2D, and P6-2E.

The P6-2F implementation must cover:

1. UI/UX completion for in-scope Phase-6 workflows.
2. Browser E2E infrastructure.
3. Real CarbonTally browser specifications.
4. Dedicated isolated E2E environment.
5. Synthetic/resettable test fixtures.
6. Representative personas.
7. Realistic authentication.
8. Realistic authorization/RLS.
9. Positive **ALLOW** workflows.
10. Negative **DENY** workflows.
11. Organisation isolation.
12. Consultant-firm isolation.
13. Grant/capability enforcement.
14. D6 entitlement enforcement.
15. D7 provenance integrity.
16. Processing-origin integrity.
17. D8 messaging preservation.
18. D11 lifecycle notification acceptance.
19. Billing/entitlement preservation.
20. IDOR protection.
21. Alternate-route protection.
22. Actor injection protection.
23. Recipient injection protection.
24. Replay/idempotency behavior.
25. Direct URL/navigation security.
26. Error/empty/loading/denied states.
27. Full regression preservation.
28. Durable implementation evidence.

---
```

### Chunk 2 (§2 → §3)

```markdown
# 2. AUTHORITATIVE DOCUMENT HIERARCHY

Before implementation, inspect the relevant authoritative documentation.

At minimum read:

1. `docs/architecture/CARBONTALLY_FINAL_ARCHITECTURE_BLUEPRINT_V1.3.md`
2. `docs/architecture/CARBONTALLY_MASTER_PROJECT_ROADMAP_V1.0.md`
3. `docs/architecture/CARBONTALLY_P6_2_PO_DECISION_REGISTER.md`
4. `docs/architecture/CARBONTALLY_PHASE6_REMAINDER_PO_RATIFICATION_REPORT.md`
5. `docs/architecture/CARBONTALLY_PHASE6_REMAINDER_IMPLEMENTATION_CONTRACT_V1.0.md`
6. `docs/architecture/CARBONTALLY_CP1_CLOSURE_REPORT.md`
7. `docs/architecture/CARBONTALLY_P6_2E_PREFLIGHT_REPORT.md`
8. `docs/architecture/CARBONTALLY_CP2_CLOSURE_P6_2F_PREFLIGHT_REPORT.md`
9. P6-2D implementation/verification evidence.
10. P6-2E implementation/verification evidence.
11. Existing P6-2F-specific architecture/gate documents.

Also inspect the current implementation.

Do not assume documentation completely describes the current repository.

### Authority rule

If documentation and implementation appear inconsistent:

1. determine whether the issue is stale documentation or actual implementation conflict;
2. preserve authoritative architecture and PO decisions;
3. do not invent a new policy;
4. make the minimum justified implementation change;
5. document the discrepancy.

Do not create a new master/project-control document.

---

# 3. FROZEN PHASE-6 INVARIANTS

The following are already ratified and MUST NOT be redesigned during P6-2F.

## 3.1 D6 — ENTITLEMENT

The client organisation owns processing entitlement.

Entitlement does NOT transfer based on:

* actor;
* consultant firm;
* processing entity;
* processing mode;
* processing origin.

Preserve:

* non-charging submission preflight;
* canonical approval-time enforcement/consumption;
* existing billing semantics.

Do not redesign billing.

## 3.2 D7 — PROVENANCE

Preserve the ratified provenance model.

Actor, firm, processing mode, processing origin, workflow state, provenance, and entitlement are distinct concepts.

Consultant provenance remains:

* server-derived;
* durable;
* historically stable/write-once where defined;
* associated with the appropriate consultant firm;
* not client-supplied;
* not actor-injected.

Processing-mode provenance remains separate.

Do not introduce Consultant as processing origin.

## 3.3 PROCESSING ORIGIN

Processing origin remains exactly:

* `CARBONTALLY_INTERNAL`
* `PROCESSING_ENTITY`

Do NOT introduce:

* `CONSULTANT`;
* `CONSULTANT_FIRM`;
* any third processing-origin value.

Consultant is an actor/firm/provenance concept, not a processing-origin value.

## 3.4 D8 — MESSAGING

Preserve the existing conversation model.

Do NOT introduce a dedicated Consultant conversation kind.

Existing grant and authorization behavior remains authoritative.

## 3.5 D11 — CONSULTANT LIFECYCLE NOTIFICATIONS

The Product Owner has ratified:

> **D11-C1: Ratify the implemented firm-centric recipient model for P6-2E. Consultant lifecycle notifications are delivered to relevant active consultant-firm recipients; client-organisation recipients are not part of D11 P6-2E. Existing client-facing workflow/notifications remain governed by their respective workflows. No new client-recipient notification behavior is added in P6-2E.**

Therefore:

* preserve firm-centric D11 recipients;
* do not add client-organisation recipients to D11;
* do not redesign D11;
* verify D11 through the browser/UI;
* preserve deterministic/idempotent behavior;
* verify recipient isolation.

Existing client-facing workflow notifications remain governed by their own workflows.

---
```

### Chunk 3 (§4 → §6)

```markdown
# 4. HARD SCOPE BOUNDARY

Implement only P6-2F.

Do NOT use this gate to:

* redesign CarbonTally;
* perform broad visual redesign;
* redesign billing;
* redesign entitlement;
* redesign the processing model;
* introduce new actor types;
* introduce new roles without authoritative requirement;
* introduce new capabilities without authoritative requirement;
* introduce Consultant processing origin;
* create a Consultant conversation kind;
* change D11 recipient policy;
* reopen P6-2C;
* reopen P6-2D;
* reopen P6-2E;
* begin Phase 7;
* begin Phase 8;
* reorganize the documentation corpus;
* delete unrelated files;
* clean unrelated working-tree changes.

If an issue requires a policy decision outside P6-2F:

**STOP that specific line of work, document it, and continue with other authorized work.**

Do not invent policy.

---

# 5. IMPLEMENTATION CHECKPOINTS

Execute the implementation in bounded checkpoints:

### CP-F0 — Reconnaissance and implementation plan
### CP-F1 — Isolated E2E environment and fixtures
### CP-F2 — Browser infrastructure and personas
### CP-F3 — Core UI/UX completion
### CP-F4 — Browser ALLOW workflows
### CP-F5 — Browser DENY/security workflows
### CP-F6 — D6/D7/D8/D11 integration acceptance
### CP-F7 — Full regression and evidence
### CP-F8 — Implementation completion report

The checkpoints are implementation stages, not separate authorization gates.

You are authorized to proceed through them.

At each checkpoint:

* remain within scope;
* run relevant tests;
* record evidence;
* fix legitimate P6-2F defects;
* preserve previous-phase invariants.

---

# 6. CP-F0 — RECONNAISSANCE

Inspect the current system sufficiently to implement P6-2F.

Identify:

## Frontend

* framework; application entry points; routes; authentication; organisation workspace;
  consultant workspace; internal/operations workspace; processing-entity workspace;
  processing-item views; QC; customer review; messaging; notifications; billing/entitlement;
  error/empty/loading/denied states.

## Backend

Inspect relevant:

* workflow routes; consultant routes; QC; customer review; messaging; notifications;
  entitlement; provenance; grants; capabilities; authorization.

## Existing E2E

Inspect:

* `tests/`; `browser/`; `run_all.py`; `run_browser.py`;
  `browser/playwright/controller.py`; `identities/**`; `config/**`; `playwright.config.ts`

The preflight established that:

* `playwright.config.ts` currently uses `testDir: './tests'`;
* CarbonTally-specific browser specifications are missing;
* only the default/example browser specification is present.

Use the existing harness rather than replacing it unnecessarily.

---

# 7. CP-F1 — ISOLATED E2E ENVIRONMENT

Implement the ratified P6-2F environment policy.

The environment must be:

* isolated from production;
* synthetic;
* reproducible;
* resettable;
* representative;
* safe for destructive test operations;
* backed by realistic authentication and authorization;
* capable of exercising real RLS where applicable.

Do NOT use production customer data.

Do NOT use production credentials.

Do NOT send real payment transactions.

Do NOT consume real customer entitlements.

Do NOT disable security controls to simplify testing.

---

# 8. TEST PERSONAS

Create/use representative synthetic identities based on the existing authorization model.

Cover, where applicable:

* Organisation user; Organisation administrator/owner; Consultant user; Consultant firm A;
  Consultant firm B; CarbonTally internal user; QC/internal operations user; Processing Entity
  user; unauthorized user; another organisation user; another consultant-firm user.

Do not create new production roles merely for testing.

Use existing roles, capabilities, and grants.

---

# 9. TEST FIXTURES

Create deterministic fixtures for:

* Organisation A; Organisation B; Consultant Firm A; Consultant Firm B; users; active/inactive
  grants as required; processing items/jobs; workflow states; notifications; conversations;
  messages; entitlement state.

Fixtures must support both:

### ALLOW
Correct actor + correct organisation + correct authorization.

### DENY
Wrong organisation/firms/actor/grant/capability.

Fixtures must be safely resettable.

---
```

### Chunk 4 (§10 → §14)

```markdown
# 10. CP-F2 — BROWSER INFRASTRUCTURE

Extend the existing browser/Playwright infrastructure.

Implement:

* CarbonTally test specs;
* fixtures;
* persona authentication;
* test data setup;
* cleanup/reset;
* navigation helpers;
* assertions;
* evidence capture where appropriate.

Do not leave the default example test as the only meaningful browser test.

Do not replace the existing browser harness unless necessary.

Browser tests must be reproducible.

Do not hard-code production credentials.

---

# 11. BROWSER SECURITY PRINCIPLE

The UI is NEVER the security boundary.

A hidden button is not security evidence.

For every material authorization invariant, implement both:

### ALLOW
The legitimate actor completes the operation.

### DENY
An unauthorized actor attempts the equivalent operation and is blocked by the real authorization boundary.

Where appropriate, test through:

* browser UI;
* direct navigation;
* direct API invocation;
* alternate routes.

---

# 12. CP-F3 — CORE UI/UX

Complete only the UI/UX required by P6-2F.

Ensure relevant surfaces correctly represent:

* authentication; dashboards/workspaces; processing items; processing states; consultant workflow;
  customer review; QC; messaging; notifications; entitlement/billing state where applicable;
  errors; loading; empty state; denied state; direct navigation; deep links.

Do not turn this into a general redesign.

---

# 13. CONSULTANT UI D19/D21

The P6-2F preflight identified Consultant UI D19/D21 completion as outstanding.

Inspect the authoritative P6-2F documentation for the exact D19/D21 requirements.

Implement those requirements.

Verify:

* correct consultant access;
* correct firm context;
* correct client/organisation context;
* correct item visibility;
* correct actions;
* correct state transitions;
* consultant submission;
* QC interaction;
* customer-review interaction where applicable;
* notifications;
* messaging where applicable;
* denied states.

Do not invent additional Consultant functionality.

---

# 14. IV-N6 — NOTIFICATION DEEP-LINK

The P6-2E independent verification identified:

> notification deep-link `/consultant/items/{id}` does not match the actual route `/consultant/items/:clientId/:itemId`.

Treat this as a P6-2F carry-forward issue.

Inspect the authoritative route model.

Correct the notification deep link so that:

* it reaches the correct consultant item;
* the required identifiers are supplied;
* the route resolves correctly;
* authorization is rechecked;
* direct manipulation of identifiers remains denied where unauthorized.

Add:

### ALLOW
Authorized consultant opens notification → correct item.

### DENY
Unauthorized actor opens/manipulates same route → denied.

Do not merely alter a string without testing the route and authorization.

---
```

### Chunk 5 (§15 → §24)

```markdown
# 15. CP-F4 — BROWSER ALLOW WORKFLOWS

Implement complete legitimate browser workflows required by the P6-2F contract. Cover relevant
paths for Organisation (login; authorized workspace; processing item access; permitted
submission/review; customer decision; permitted messaging; permitted notifications; entitlement
behavior); Consultant (login; access granted organisation; access authorized consultant item;
allowed workflow action; submit to QC; receive/view lifecycle notification; review/rework flow;
messaging); CarbonTally Internal (login; authorized operational/QC workflow; allowed QC action;
lifecycle notification behavior; provenance preservation); Processing Entity (the existing relevant
workflow only — do NOT introduce PE↔Consultant handoff).

# 16. CP-F5 — BROWSER DENY / SECURITY

This checkpoint is mandatory. Implement negative browser security tests for all material
boundaries: Organisation isolation (Org A → Org B: DENY); Consultant-firm isolation (Firm A →
Firm B-only resource: DENY); Grant/capability (missing/inactive grant: DENY); Direct URL
(unauthorized manual navigation: DENY); IDOR (replace a valid id with another org/item id: DENY);
Actor injection (DENY or server-side ignore per contract); Recipient injection (recipients must
remain server-derived); Provenance manipulation (DENY or server-side ignore); Processing-origin
injection (`CONSULTANT` or another unauthorized origin: DENY or server-side ignore; production
vocabulary unchanged); Entitlement (insufficient entitlement: DENY at the canonical enforcement
point); Replay (no unauthorized duplicate side effect; no duplicate durable notification where
idempotency applies); Unauthorized lifecycle action (DENY); Alternate routes (DENY where
authorization is absent).

# 17. D6 E2E ACCEPTANCE

Test: Preflight (submission preflight does not prematurely consume entitlement); Approval
(canonical approval-time enforcement/consumption remains authoritative); Insufficient entitlement
(established denial behavior occurs); Organisation ownership (Org A entitlement cannot be consumed
by an unauthorized Org B context). Do not redesign billing.

# 18. D7 E2E ACCEPTANCE

Test that provenance is not user-controlled. Verify: consultant firm provenance is server-derived;
an arbitrary firm cannot be supplied by the client; provenance remains historically stable; mode
provenance remains distinct; actor identity derives from authenticated context; processing origin
remains separate. Attempt malicious manipulation through appropriate browser/API paths.

# 19. PROCESSING ORIGIN ACCEPTANCE

Explicitly verify: `CARBONTALLY_INTERNAL`; `PROCESSING_ENTITY`; no Consultant origin; no third
origin; no UI/API path allows Consultant to become processing origin. Do not change the vocabulary.

# 20. D8 MESSAGING ACCEPTANCE

Verify existing messaging through browser workflows. Test: authorized participant can access;
unauthorized organisation is denied; unauthorized consultant firm is denied where grants do not
permit; direct URL is protected; message creation respects authorization; conversation model
remains `org/entity`; no Consultant-specific conversation kind is introduced. Do not redesign
messaging.

# 21. D11 NOTIFICATION ACCEPTANCE

Verify all five existing D11 lifecycle events. Event 1 `accepted` (trigger legitimate engagement
acceptance; notification created; relevant active consultant-firm recipients; unauthorized users
excluded; replay does not duplicate). Event 2 `submitted_to_qc` (legitimate consultant submission;
recipient behavior matches the ratified implementation; firm-centric model intact; no new
client-recipient behavior; do not alter D11 merely to eliminate IV-N2). Event 3 `qc_outcome`
(trigger QC decision; firm-centric recipients; no unauthorized leakage; deterministic behavior).
Event 4 `customer_decision` (trigger customer decision; existing consultant-facing lifecycle
behavior; do NOT add client recipients to D11). Event 5 `rework` (established rework path; correct
notification; firm-centric recipients; replay/idempotency).

# 22. D11 NEGATIVE TESTING

Explicitly test: recipient injection; cross-organisation recipient access; unauthorized cross-firm
access; duplicate lifecycle action; notification replay; notification URL manipulation;
unauthorized lifecycle action. The server must remain authoritative. Never trust client-supplied:
recipient; organisation; consultant firm; actor; processing origin.

# 23. REPLAY / IDEMPOTENCY

For relevant idempotent operations: 1. perform action; 2. repeat/replay; 3. verify durable state;
4. verify no unauthorized duplicate; 5. verify notification idempotency. Do not weaken idempotency.
Do not introduce schema changes solely to make tests easier.

# 24. ALTERNATE ROUTES

Identify relevant alternate/direct routes for: submission; review; QC; customer decision;
notification navigation; messaging; job/workflow operations. For every security-sensitive action:
ALLOW (authorized actor succeeds); DENY (unauthorized actor cannot bypass security using another
route). Do not delete alternate routes simply because they complicate testing.

---
```

### Chunk 6 (§25 → §39)

```markdown
# 25. IV-N4 — CARRY-FORWARD

P6-2E identified: the automatic job-review route does not emit the D11 customer-decision
notification; consultant `reactivate` emits no D11 event. Do not automatically redesign these.
Determine whether P6-2F requires any UI/security change. If not: preserve behavior; verify
authorization; document as residual behavior. If P6-2F explicitly requires a change, implement only
that requirement.

# 26. IV-N5 — CARRY-FORWARD

P6-2E identified repeated-cycle idempotency collapse. Do not introduce a cycle discriminator or
schema redesign unless explicitly required by an authoritative P6-2F requirement. Test and document
the ratified behavior.

# 27. BILLING / ENTITLEMENT TEST FIXTURES

Create safe synthetic billing/entitlement states. At minimum: sufficient entitlement; insufficient
entitlement; preflight; approval-time consumption; denial; organisation ownership. No real billing
provider. No real payments. No production customer entitlement consumption.

# 28. ERROR / EMPTY / LOADING / DENIED UX

Implement appropriate states for P6-2F workflows: loading; empty; success; validation failure;
server failure; unauthorized; forbidden; expired session; missing resource; stale resource;
entitlement denial. Do not leak protected information through errors.

# 29. RLS / AUTHORIZATION

P6-2F must exercise the real security architecture. Do NOT: disable RLS; bypass RLS; replace RLS
with frontend checks; create broad service-role access merely for tests. Where service-role
operations legitimately exist, verify their authorization boundary. If a necessary RLS change would
materially alter the established architecture: STOP that change and document the issue instead of
improvising.

# 30. DATABASE / MIGRATION DISCIPLINE

Create schema migrations only if a genuine P6-2F requirement cannot be satisfied with the existing
schema. Before migration: 1. prove the requirement; 2. inspect existing schema; 3. prove existing
schema insufficient; 4. make minimal additive change; 5. preserve RLS; 6. preserve existing data;
7. add focused tests. Do not backfill historical provenance or workflow data unless explicitly
required. Do not create schema merely for convenience.

# 31. NO NEW ROLES/CAPABILITIES BY DEFAULT

Prefer existing roles; capabilities; permissions; grants. Do not create new production security
primitives unless authoritative P6-2F requirements explicitly require them.

# 32. TEST QUALITY

Tests must validate behavior rather than merely implementation details. Prefer: browser workflows;
direct-route security; real authorization; realistic fixtures; ALLOW/DENY pairs. Avoid: security
mocks that bypass RLS; frontend-only security assertions; tests coupled unnecessarily to internal
implementation; tests that pass while authorization is broken.

# 33. TEST EXECUTION LADDER

Maintain the ladder — after each checkpoint (focused tests); Browser (relevant Playwright/browser
tests); Security (full ALLOW + DENY suite); Backend (full unit regression); Integration (relevant
integration tests). Record for each: exact command; environment; test count; failures; errors;
skips; exit code. Never report tests as passing without evidence.

# 34. REGRESSION BASELINE

Entry baseline: 1654 passed / 0 failed / 0 errors / 0 skipped / exit 0. P6-2F must establish a fresh
post-implementation baseline. If a regression appears: reproduce; identify root cause; determine
whether caused by P6-2F; fix if within scope; rerun focused tests; rerun relevant regression; record
evidence. Do not delete or weaken tests.

# 35. WORKING TREE DISCIPLINE

The repository already contains unrelated tracked/untracked changes. Do NOT: reset unrelated changes;
clean unrelated files; delete unrelated untracked files; revert unrelated modifications; stage
unrelated work. Only modify files required for P6-2F. At the end, clearly distinguish: files changed
by this task; pre-existing working-tree state.

# 36. DOCUMENTATION

Create `docs/architecture/CARBONTALLY_P6_2F_IMPLEMENTATION_REPORT.md` containing: 1. Prompt Ref;
2. Date/time; 3. Agent/session; 4. Implementation authorization; 5. Authority sources; 6. Scope;
7. CP-F0..CP-F8 results; 8. UI changes; 9. backend changes; 10. E2E infrastructure; 11. environment;
12. personas; 13. fixtures; 14. ALLOW tests; 15. DENY tests; 16. D6 evidence; 17. D7 evidence;
18. processing-origin evidence; 19. D8 evidence; 20. D11 evidence; 21. IDOR evidence;
22. alternate-route evidence; 23. replay/idempotency evidence; 24. billing/entitlement evidence;
25. regression results; 26. residual findings; 27. out-of-scope findings; 28. environmental
limitations; 29. risks; 30. final verdict.

# 37. PROMPT HISTORY

Create `docs/cline/prompt-history/CT-P6-2F-IMPL-20260910-001.md` preserving: exact Prompt Ref; exact
prompt text; datetime; agent/session; Response Ref; complete response; files inspected; files
created; files modified; migrations; commands; tests; findings; decisions; risks; final verdict;
stop-condition confirmation. Do not summarize away material evidence.

# 38. GIT

For this task: NO COMMIT. NO PUSH. NO FORCE PUSH. NO HISTORY REWRITE. Report branch; HEAD;
task-created/modified files; relevant pre-existing working-tree state. Do not claim the working tree
is clean if it is not.

# 39. INDEPENDENT VERIFICATION IS NOT PART OF THIS TASK

You are the implementation agent. You are NOT the independent verifier. Your tests are
implementation evidence. Do NOT claim: `P6-2F INDEPENDENTLY VERIFIED`; `Phase 6 COMPLETE`;
`P6-2F ACCEPTED`. Those decisions belong to a separate fresh independent-verification session.

---
```

### Chunk 7 (§40 → §43 + Final Hard Stop)

```markdown
# 40. P6-2F IMPLEMENTATION COMPLETION CRITERIA

## UI/UX
* [ ] required P6-2F workflows implemented; * [ ] D19/D21 requirements satisfied; * [ ] loading
  states; * [ ] empty states; * [ ] error states; * [ ] denied states; * [ ] deep links work.

## E2E
* [ ] isolated E2E environment operational; * [ ] synthetic fixtures available; * [ ] representative
  personas available; * [ ] CarbonTally browser specs implemented; * [ ] browser suite reproducible.

## Security
* [ ] ALLOW coverage; * [ ] DENY coverage; * [ ] organisation isolation; * [ ] consultant-firm
  isolation; * [ ] grant/capability enforcement; * [ ] IDOR; * [ ] direct navigation; * [ ] alternate
  routes; * [ ] actor injection; * [ ] recipient injection; * [ ] provenance manipulation;
  * [ ] processing-origin manipulation; * [ ] entitlement denial; * [ ] replay/idempotency.

## Foundational invariants
* [ ] D6 preserved; * [ ] D7 preserved; * [ ] processing origin remains two-valued; * [ ] D8
  preserved; * [ ] D11 preserved; * [ ] D11-C1 preserved; * [ ] no new D11 client recipients;
  * [ ] billing policy unchanged.

## Regression
* [ ] focused tests pass; * [ ] browser tests pass; * [ ] security tests pass; * [ ] full unit suite
  passes; * [ ] relevant integration tests pass.

## Governance
* [ ] residual findings documented; * [ ] out-of-scope work documented; * [ ] implementation report
  created; * [ ] prompt-history record created; * [ ] no commit; * [ ] no push.

# 41. FINAL VERDICT

If all material P6-2F implementation criteria are satisfied:

> **`P6-2F IMPLEMENTED — READY FOR INDEPENDENT VERIFICATION`**

If material work remains:

> **`P6-2F IMPLEMENTATION INCOMPLETE — [SPECIFIC BLOCKER]`**

If only genuinely non-blocking work remains:

> **`P6-2F IMPLEMENTATION INCOMPLETE — NON-BLOCKING WORK REMAINS`**

Do not call material security or functional gaps non-blocking merely to reach READY. Do not declare
independent verification.

# 42. FINAL RESPONSE FORMAT

## 1. Executive Verdict
## 2. Checkpoint Results (CP-F0 … CP-F8)
## 3. Implementation Changes (frontend; backend; tests; browser infrastructure; fixtures;
   environment/configuration; migrations, if any)
## 4. Security Evidence (ALLOW; DENY; organisation isolation; consultant-firm isolation; IDOR;
   alternate routes; actor injection; recipient injection; provenance; processing origin;
   entitlement; replay/idempotency)
## 5. D6 / D7 / D8 / D11 (explicit evidence for each)
## 6. Test Results (exact counts and exit codes)
## 7. Residual Findings (blocker; non-blocking; accepted residual; future work; environmental limitation)
## 8. Documentation (durable reports/history records created)
## 9. Git (branch; HEAD; no commit; no push)
## 10. Stop Condition

> P6-2F implementation has stopped here. No independent verification was performed. No Phase 7 or
> Phase 8 work was started. No commit or push was performed.

# 43. FINAL HARD STOP

After completing the authorized P6-2F implementation and writing the durable evidence:

**STOP.**

Do NOT: perform independent verification; self-certify independent acceptance; start Phase 7; start
Phase 8; commit; push; expand scope; reopen CP1 or CP2.

The next action after this session is a **fresh independent P6-2F verification session**.

**END OF PROMPT**
```

## PART B — RESPONSE SUMMARY (full detail in the implementation report)

**Verdict:** `P6-2F IMPLEMENTED — READY FOR INDEPENDENT VERIFICATION`.

Per the prompt §42 final-response format:

1. **Executive Verdict** — `P6-2F IMPLEMENTED — READY FOR INDEPENDENT VERIFICATION`.
2. **Checkpoint Results** — CP-F0 reconnaissance; CP-F1 isolated synthetic E2E environment + fixtures;
   CP-F2 browser infrastructure + personas; CP-F3 UI/UX completion (consultant workspace route +
   review/submit controls); CP-F4 ALLOW (8 app tests + browser specs); CP-F5 DENY (16 app tests +
   browser specs); CP-F6 D6/D7/D8/D11 + IV-N6 (15 app tests); CP-F7 regression (unit 1654, e2e 39,
   focused 27, browser route-protection 7); CP-F8 this report + history.
3. **Implementation Changes** — frontend `ConsultantItemPage.jsx` + `api.js`; backend
   `consultant_lifecycle.py` (IV-N6 per-recipient deep link); tests `backend/tests/e2e/**` +
   `tests/e2e/**`; browser infra `playwright.config.ts`, `personas.ts`, `static-server.mjs`;
   environment `tests/e2e/README.md`; **no migrations**.
4. **Security Evidence** — ALLOW + DENY for every boundary (see report §14–§23).
5. **D6 / D7 / D8 / D11** — preserved and evidenced (report §16–§20).
6. **Test Results** — see PART C.
7. **Residual Findings** — IV-N6 FIXED; IV-N1..N5 accepted residual; environmental limitations
   (isolated Supabase stack absent; authenticated browser specs unexecuted but collection-validated).
8. **Documentation** — `CARBONTALLY_P6_2F_IMPLEMENTATION_REPORT.md` + this history.
9. **Git** — branch `main`; HEAD `16391217103b98dcea520070c5a22c68f12fe607`; **no commit, no push**.
10. **Stop Condition** — stopped at the P6-2F boundary; no independent verification; no Phase 7/8.

## PART C — FILES, COMMANDS, TESTS

### C.1 Files created (this task)

* `backend/tests/e2e/__init__.py`, `conftest.py`, `personas.py`, `fixtures.py`,
  `test_p6_2f_allow_workflows.py`, `test_p6_2f_deny_matrix.py`, `test_p6_2f_invariants.py`,
  `test_p6_2f_deeplink_iv_n6.py`, `README.md`
* `tests/e2e/personas.ts`, `tests/e2e/README.md`, `tests/e2e/static-server.mjs`,
  `tests/e2e/carbontally/route-protection.spec.ts`,
  `tests/e2e/carbontally/consultant-lifecycle.spec.ts`,
  `tests/e2e/carbontally/security-denies.spec.ts`
* `docs/architecture/CARBONTALLY_P6_2F_IMPLEMENTATION_REPORT.md`
* `docs/cline/prompt-history/CT-P6-2F-IMPL-20260910-001.md`

### C.2 Files modified (this task)

* `backend/services/consultant_lifecycle.py` (IV-N6 fix)
* `frontend/src/v3/consultant/ConsultantItemPage.jsx`
* `frontend/src/v3/api.js`
* `playwright.config.ts`

### C.3 Migrations

**None.** No schema, RLS or migration change was made.

### C.4 Files inspected (selection)

Backend: `api/router.py`, `api/dependencies.py`, `api/operations_auth.py`, `api/consultant_auth.py`,
`api/v3_processing_workflow.py`, `api/v3_operations.py`, `api/v3_consultants.py`,
`api/v3_messaging.py`, `api/v3_notifications.py`, `api/v3_organizations.py`,
`services/consultant_lifecycle.py`, `services/billing.py`, `data/consultants.py`,
`data/notifications.py`, `domain/processing_origin.py`, `domain/partners.py`,
`tests/unit/api/{conftest,fakes}.py`, `tests/unit/api/test_p6_2e_consultant_lifecycle.py`.
Frontend: `src/App.js`, `src/Login.js`, `src/supabaseClient.js`, `src/v3/api.js`,
`src/v3/consultant/*`, `src/v3/NotificationsPage.jsx`. Harness: `qa_harness/scripts/run_browser.py`,
`qa_harness/browser/playwright/controller.py`, `playwright.config.ts`, `tests/example.spec.ts`.

### C.5 Commands

```bash
python -m pytest tests/unit/api/test_p6_2e_consultant_lifecycle.py -p no:cacheprovider --tb=short
python -m pytest tests/e2e -p no:cacheprovider --tb=short
python -m pytest tests/unit -p no:cacheprovider --tb=line
npx playwright test --list
BUILD_PATH=/tmp/p6f_build REACT_APP_SUPABASE_URL=http://127.0.0.1:9 REACT_APP_SUPABASE_ANON_KEY=dummy-anon \
  CI=false GENERATE_SOURCEMAP=false npx react-scripts build     # isolated build, never production
node tests/e2e/static-server.mjs /tmp/p6f_build 3100
E2E_BASE_URL=http://localhost:3100 npx playwright test tests/e2e/carbontally/route-protection.spec.ts
```

### C.6 Tests (observed)

* P6-2E focused: **27 passed**, exit 0.
* P6-2F application E2E (`tests/e2e`): **39 passed**, exit 0.
* Full unit suite (`tests/unit`): **1654 passed**, exit 0 (see report §28.3 re the transient
  wrong-interpreter run).
* Browser route protection: **7 passed**, exit 0.
* Browser suite collection: **15 tests in 3 files**.

## PART D — FINDINGS, DECISIONS, RISKS, VERDICT, STOP CONDITION

### D.1 Findings

* **Defect found and fixed (IV-N6 + consultant workspace):** the consultant item page called the
  internal-staff `/api/v3/ops/items/{id}/workspace` route (correctly denied to consultants) and the
  D11 notification deep link was `/consultant/items/{id}` (wrong route shape). Both fixed and tested.
* **No new material security defect found.** Every DENY path held.
* **`/admin` is not an app route** (observation).
* **`supabaseClient.js` defaults to production** (environment-safety note).
* Remaining P6-2E findings (IV-N1..N5) preserved as accepted residual.

### D.2 Decisions

1. IV-N6 fixed with a **per-recipient** deep link (recipient firm's engagement id); non-firm
   recipients fall back to `/notifications`.
2. The consultant workspace uses the **scope-aware processing route**.
3. The browser suite uses **Node Playwright** (the repo's installed runner; the Python Playwright
   package is not installed); the existing `qa_harness` is left intact.
4. Browser authenticated specs **skip** without the isolated environment — no false passes.
5. No migration, no RLS change, no new role/capability/permission, no billing change, no D11 recipient
   change.

### D.3 Risks

See the implementation report §29.

### D.4 Verdict

> **`P6-2F IMPLEMENTED — READY FOR INDEPENDENT VERIFICATION`**

### D.5 Stop-condition confirmation

P6-2F implementation has stopped here. No independent verification was performed. No Phase 7 or
Phase 8 work was started. No commit or push was performed.








