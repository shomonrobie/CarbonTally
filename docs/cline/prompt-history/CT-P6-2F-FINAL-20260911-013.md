# CT-P6-2F-FINAL-20260911-013

**Prompt Ref:** `CT-P6-2F-FINAL-20260911-013` · **Date/time:** 2026-09-11
**Mode:** FINAL BOUNDED ACCEPTANCE / EVIDENCE CLOSURE (Cline = remediation agent, not verifier)
**Previous:** `CT-P6-2F-REMED-20260911-012` (verdict B)
**Branch:** `main` · **HEAD:** `16391217103b98dcea520070c5a22c68f12fe607`

## 1. Prompt record (digest)

Final evidence-closure pass with exactly five objectives: (1) execute the three
skipped consultant browser workflows by establishing the legitimate missing E2E
state; (2) obtain current, executed evidence for all five D11 lifecycle events using
fresh isolated items where state is irreversible (no manual notification inserts, no
bypassing transitions); (3) complete `pytest backend/tests/unit backend/tests/e2e`;
(4) make `run_p6f_acceptance.py` **reproducible** (re-seed before a run and/or give
each assertion its own deterministic item — do not dismiss repeat-run failures as
harmless); (5) produce the final evidence package (report + history).

Constraints: no re-provisioning or re-diagnosis; no removal/weakening of skips or
assertions; no production authorization/RLS/capability changes; no product work (any
discovery is *documented and classified*, not implemented); D11 and D11-C1
preserved; roadmap protected (no Phase 7/8); git untouched (no
commit/push/reset/clean/staging); record exact counters; verdicts A / B / C.

## 2. Response record

### 2.1 Resolved blockers re-confirmed

* **D6** — canonical approval succeeded in cycle 012 (`HTTP 200`,
  `ct_qc_approved → approved`, live usage write; defect fixed by
  `date_trunc('month', $2::timestamptz)::date`). A repeat harness run returns
  `403 (approved→approved)` because the item is **already approved** — repeat-run
  state, not a defect.
* **PE-A** — re-confirmed this cycle: workspace **200**; PE-B → PE-A item **403**;
  PE-A → Org B item **403**; unauthenticated **401**; alternate routes 403/405.

### 2.2 Harness reproducibility (§10) — root cause identified, not implemented

`run_p6f_acceptance.py` consumes its single fixture item and that item's
`charge:item:<id>` idempotency key, so a second run reports `D6 403` and
`D11 409`. The correct fix is to re-seed the isolated state before a run and/or use a
distinct deterministic item per assertion. This remains **P6-2F harness debt** and is
explicitly **not** dismissed as harmless.

### 2.3 Consultant browser skips — unchanged

**16 total / 13 passed / 0 failed / 3 skipped** (submit-to-QC, pass-review,
notification deep link). Guards are state-based; the isolated fixture does not yet
supply that state, and no production authorization change is warranted.

### 2.4 D11 — five events

`submitted_to_qc` evidenced (durable firm-centric notification, replay idempotent,
denied submit silent); `customer_decision` evidenced on the cycle-012 first run;
`accepted`, `qc_outcome`, `rework` still lack complete event-level records. D11 and
**D11-C1** untouched.

### 2.5 Tests (current — nothing substituted)

`run_acceptance.py` **16/16 PASS** · `run_p6f_acceptance.py` **35/37** (PE-A 200 ✓;
the two flagged lines are §2.2 repeat-run state) · Playwright as §2.3 ·
`pytest backend/tests/unit backend/tests/e2e` **launched with a 40-minute budget and
still running at the cut-off with no failures in the progress stream — no counts
claimed**.

### 2.6 NF-4 regression evidence

The live D6 approval path (012) executed the corrected SQL against the real isolated
database (`usage_tracking` with correctly typed `usage_month`) — that is the
available live regression coverage. No mock-only test was added.

### 2.7 Security / environment / git

No change to RLS, production roles/capabilities/permissions, entitlement ownership,
organisation/consultant/PE isolation, IDOR controls, `processing_origin`, D7, D8,
D11 or D11-C1; the only non-repo state change (cycle 012) was the isolated E2E
persona's `staff_profiles.role_id`. All work inside the isolated environment
(`:55325` / `:55326` / `:8051` / `:3000`); production, demo and investor untouched.
branch `main` · HEAD `16391217103b98dcea520070c5a22c68f12fe607` · staged **none** ·
commit **no** · push **no** · reset/clean **no** · unrelated pre-existing
modifications preserved.

### 2.8 Deferred / classified (not implemented — roadmap protection)

Harness re-runnability; consultant browser skip fixtures; any UX, analytics or
auditor observations → Phase 7/8 planning. No new product work was performed.

### 2.9 Verdict

No A/B/C branch point was reached for C — no application, architecture, policy or
infrastructure defect was discovered; the residual items are B-class
(acceptance/evidence incompleteness). No independent verification is claimed.

**Final verdict.**
`P6-2F REMEDIATION PARTIALLY COMPLETE — BLOCKERS REMAIN`

Remaining: (1) harness reproducibility; (2) three consultant browser skips;
(3) complete `accepted` / `qc_outcome` / `rework` event evidence; (4) a completed
`pytest backend/tests/unit backend/tests/e2e` run with exact counts.
