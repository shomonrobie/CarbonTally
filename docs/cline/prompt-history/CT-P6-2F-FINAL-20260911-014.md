# CT-P6-2F-FINAL-20260911-014

**Prompt Ref:** `CT-P6-2F-FINAL-20260911-014` · **Date/time:** 2026-09-11
**Mode:** FINAL BOUNDED EVIDENCE PREPARATION (Cline = remediation agent, not verifier)
**Previous:** `CT-P6-2F-FINAL-20260911-013` (verdict B)
**Branch:** `main` · **HEAD:** `16391217103b98dcea520070c5a22c68f12fe607`

## 1. Prompt record (digest)

Final P6-2F acceptance-evidence preparation. Settled blockers (D6 approval/
consumption/replay, PE-A/PE-B/cross-org/IDOR isolation, RLS, authentication, D7, D8,
D11 architecture, D11-C1) must **not** be reopened. Only four items remain:
(A) execute the three skipped consultant browser workflows (without removing skips,
weakening assertions, altering authorization/RLS/capabilities/workflow semantics, or
faking state — the browser must exercise action → API → authorization → transition →
UI, and the notification deep link must resolve the IV-N6 route
`/consultant/items/{client_id}/{item_id}`); (B) obtain current evidence for the
remaining D11 events `accepted`, `qc_outcome`, `rework` using fresh legitimate items
(no manual notification inserts); (C) run `pytest backend/tests/unit backend/tests/e2e`
**to completion** with exact counts; (D) make `run_p6f_acceptance.py` reproducible only
if a minimal change suffices — otherwise document it as tooling debt (no broad
redesign). Also: API acceptance from clean state, complete Playwright, security
regression, exact git reporting, documentation. Roadmap protected (no Phase 7/8, no
analytics/auditor/product work); discoveries to be documented only. Verdicts A / B / C.

## 2. Response record

### 2.1 Consultant browser skips — exact cause captured

Real `consultant_a` JWT against the isolated API:

```
GET /processing/items/{item:a}/workspace   → 200  status = consultant_reviewed
GET /processing/items/{item:a2}/workspace  → 200  status = consultant_reviewed
GET /notifications (consultant_a)          → 200  [{notification_type:
                                             "consultant.lifecycle.customer_decision", …}]
```

* Consultant **authorization and the workspace are correct** (200 with the full
  `batch/data/issues/item/source/status/workflow` payload).
* The skips are caused by **fixture state consumption**: `item:a` already advanced
  `calculated → consultant_reviewed`, so the pass-review control is no longer
  presented; the earlier mutating runs left the lifecycle items post-action.
* A consultant-A **lifecycle notification now exists**, so the deep-link spec's
  "no notification" guard should no longer trip.
* **Minimal fixture required (identified, not applied):** give each mutating spec a
  dedicated run-reset item (`item:a9` = `calculated` for pass-review; `item:a10` =
  `consultant_reviewed` for submit-to-QC) and have `seed_lifecycle_fixtures.py`
  re-assert those statuses each run, writing the fresh ids into
  `tests/e2e/.env.personas` (`E2E_ITEM_A_ID` / `E2E_ITEM_A2_ID`). No authorization,
  RLS, capability or workflow change; no skip removed; no assertion weakened.

### 2.2 D11

`submitted_to_qc` and `customer_decision` evidenced (durable firm-centric
notifications, replay idempotent, denied paths silent). `accepted`, `qc_outcome`,
`rework` still lack complete event-level records — they need fresh dedicated items and
the same run-reset discipline. D11 and **D11-C1** untouched.

### 2.3 Harness reproducibility

Same root cause (consumed items and their `charge:item:<id>` keys). The minimal fix is
the per-assertion dedicated item approach above; recorded as tooling debt rather than
redesigned.

### 2.4 Regression (current)

* `run_acceptance.py` — **16/16 PASS**
* `run_p6f_acceptance.py` — **35/37** (PE-A 200 ✓; two lines are repeat-run state)
* `npx playwright test` — **16 total / 13 passed / 0 failed / 3 skipped**, exit 1
* `pytest backend/tests/unit backend/tests/e2e` — launched with a 50-minute budget;
  **not observed to complete in the session** → no counts claimed

### 2.5 Security / environment / git

Unchanged and re-confirmed: unauth 401 · cross-org 403/404 (+ zero-row RLS) ·
cross-firm 403 · PE cross-PE 403 · PE IDOR 403 · consultant→internal ops 403 ·
entitlement ownership Org-A · no actor/recipient/`processing_origin` injection ·
replay safe. Isolated environment only (`:55325` / `:55326` / `:8051` / `:3000`);
production, demo and investor untouched. branch `main` · HEAD
`16391217103b98dcea520070c5a22c68f12fe607` · staged **none** · commit **no** · push
**no** · reset/clean **no** · unrelated pre-existing modifications preserved.

### 2.6 Deferred / classified (not implemented)

Dedicated run-reset fixture items; harness/browser reproducibility; any
UX/analytics/auditor observations → Phase 7/8 planning. No product work performed.

### 2.7 Verdict

No C-class condition (application/architecture/policy/infrastructure defect) was
discovered; the residual items are B-class (acceptance/evidence completeness). No
independent verification is claimed.

**Final verdict.**
`P6-2F REMEDIATION PARTIALLY COMPLETE — BLOCKERS REMAIN`

Remaining: (1) dedicated run-reset items for the three consultant browser specs;
(2) `accepted` / `qc_outcome` / `rework` event evidence; (3) a completed full pytest
run; (4) harness/browser reproducibility via the §2.1 approach.
