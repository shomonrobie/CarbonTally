# CT-P6-2F-ENV-IMPL-20260911-006

**Prompt Ref:** `CT-P6-2F-ENV-IMPL-20260911-006` · **Prompt datetime:** 2026-09-11
**Response Ref:** `CT-P6-2F-ENV-IMPL-20260911-006-R1` · **Response datetime:** 2026-09-11
**Phase:** P6-2F · **Mode:** IMPLEMENTATION / ACCEPTANCE COMPLETION
**Prior:** `CT-P6-2F-ENV-IMPL-20260911-005` · **HEAD:** `16391217103b98dcea520070c5a22c68f12fe607`

## 1. Prompt record (structure-preserving digest, 21 sections)

1. **Mandatory continuation** — continue from the exact tree/running E2E state; do
   not repeat 005's provisioning or evidence.
2. **Exact remaining findings** — R1 (no allowance fixture → 402, D6
   ALLOW-at-approval unevidenced), R2 (`customer_decision` unevidenced for the same
   reason), R3 (harness notification column/field mismatch), R4 (PE-A workspace 403
   — do not assume entitlement, do not fix without evidence), R5 (lifecycle items
   `consultant_firm_id = NULL` → three browser skips).
3. **Hard scope** — authorized: (A) minimum allowance fixture, (B) firm-scope the
   lifecycle items, (C) fix the harness notification read, (D) re-run affected
   tests, (E) investigate R4 only with evidence, (F) complete regression + browser,
   (G) update report/history. Forbidden: production/demo/investor or RLS changes,
   migrations, new roles/capabilities/permissions, billing/subscription redesign,
   entitlement-ownership change, D6/D7/D8/D11/D11-C1/origin changes, new
   conversation kinds, new PE functionality, refactors, doc reorganisation, commit/
   push/reset/clean/staging. If an authorized item needs a forbidden change: STOP.
4. **Authoritative decisions to preserve** — D6 (client-org-owned entitlement,
   actor/firm/PE never the owner, non-charging preflight vs canonical approval-time
   enforcement), D7 (server-derived firm provenance, stable), D7b (no `CONSULTANT`
   origin), D8 (existing conversation model, no consultant kind), D11 (five
   events), D11-C1 (firm-centric recipients).
5. **First action** — inspect git/HEAD/stack/report/005 history/harness/fixtures/
   subscription+allowance schema/notification columns/the three skipped specs/the
   PE-A workspace path before any change.
6. **R1** — minimum isolated allowance fixture using the existing model; read CHECK
   constraints first; allowance must belong to the client organisation; consultant/
   PE must not become owner; do not bypass the canonical gate; prove before/actor/
   owner/action/result/consumption/replay.
7. **R2** — real `customer_decision` ALLOW: gate permits, event emitted, key
   deterministic, notification durable, recipient server-derived, replay
   idempotent, unauthorized denied, denied path silent; D11-C1 unchanged.
8. **R5** — firm-scope the fixture items via `consultant_firm_id`; consultant A
   authorized, consultant B not; no authorization change, no new capability, no
   assertion weakened, no force-rendered buttons.
9. **R3** — fix only the harness read against the real notification model; no schema/
   persistence/recipient changes, no fabricated rows, no hard-coded counts.
10. **D11 event re-run** — all five events with durable side effects (authorized
    actor, state transition, durable notification, correct recipient, deterministic
    key, replay/idempotency), and denied paths with refusal + no mutation + no
    notification.
11. **R4** — investigate only after R1/R2/R3/R5; rerun PE-A first; if 403 persists,
    identify the exact authorization cause from evidence; STOP if it requires
    product/schema/RLS/role/capability changes.
12. **Playwright** — full suite on isolated env with real auth/JWT; 0 failures and
    0 required skips; do not delete skips, fix the legitimate fixture or report.
13. **API harness** — `run_acceptance.py` 16/16, then `run_p6f_acceptance.py`; never
    count blocked/inconclusive as PASS.
14. **Full regression** — `pytest backend/tests/unit backend/tests/e2e` to
    completion with exact counts; the 004 figure must not be reused.
15. **Reset/reproducibility** — wire new fixtures into bootstrap/reset; one reset/
    reseed cycle if needed; never reset the repository or non-isolated environments.
16. **Security preservation** — the allowance fixture must not let a consultant
    approve as owner, a PE consume org entitlement, another org consume Org A
    allowance, or an unauthorized firm consume it.
17. **Environment integrity** — Supabase `:55325`, DB `:55326`, API `:8051`,
    frontend `:3000` (repo default points at production and must not be used).
18. **Git safety** — no commit/push/reset/clean/staging; report state.
19. **Documentation** — bounded 006 section in the existing report + this history
    with all 21 required elements; hide nothing unresolved.
20. **Final verdict** — exactly A/B/C; VERIFIED / INDEPENDENTLY VERIFIED / CLOSED /
    PHASE 6 COMPLETE forbidden.
21. **Completion criteria / response format** — verdict A only if all 18 criteria
    hold; R4 must be reported explicitly if unresolved.

## 2. Response record

**Reused, not repeated.** The isolated stack, migrations, E2E-only grants, RLS
proof, personas, synthetic topology, `seed_e2e.py`, `run_acceptance.py`,
FastAPI+CRA bring-up and isolation were all reused unchanged; no rebuild/reseed.

**Files changed.** `e2e/environment/scripts/seed_lifecycle_fixtures.py` (allowance
config fixture, `consultant_firm_id = FIRM_A` scoping, PE assignment attempt,
`ITEM_A`/`ITEM_PE_A` imports) · `e2e/environment/scripts/run_p6f_acceptance.py`
(notification read now uses the real columns) ·
`docs/architecture/CARBONTALLY_P6_2F_E2E_ENVIRONMENT_IMPLEMENTATION_REPORT.md` (§9)
· `docs/cline/prompt-history/CT-P6-2F-ENV-IMPL-20260911-006.md` (this file).

**Commands.** `information_schema`/`pg_constraint` queries on `:55326`; greps of
`backend/services/billing.py`, `backend/data/billing.py`,
`backend/api/v3_processing_workflow.py`, `backend/api/v3_pe.py`;
`seed_lifecycle_fixtures.py`; `run_p6f_acceptance.py`; `npx playwright test`;
`pytest backend/tests/unit backend/tests/e2e` (background, incomplete).

**Exact test results.** `run_p6f_acceptance.py` **34/37**. Fixture inserts:
allowance config **201**, item / item-A upserts **200**, PE assignment
**400 `23502`** (rejected). D6 approval-time **500** (was 402); D11
`customer_decision` **409** (was 402); PE-A item workspace **403** (unchanged);
R3 harness read now returns **2 real notification rows** (was 0). Playwright and
pytest did **not** complete within the session — **no numbers claimed**.

**D6 evidence.** Owner entitlement read **200** (`organization_id` = Org A); other
org **404**; consultant **404** (grant ≠ ownership); PE **404**; unauthenticated
**401**; approval-time gate **500 after the allowance fixture** — the 402 was
cleared, proving the entitlement/allowance gate itself is satisfied, but the
canonical charge path then errors, so **ALLOW-at-approval remains unevidenced**.

**D8 evidence.** `conversation_kind` observed `[]` (no consultant kind); consultant
A authorised org conversation **200**; cross-org **403**; other-firm **403**;
unauthenticated **401** — unchanged from 005.

**D11 evidence.** `submitted_to_qc` real submit **200** with a durable firm-centric
notification and replay **409** with no duplicate rows; denied submit (consultant
B) **403 with zero new rows**; `accepted`/review **409** (state idempotency);
`rework` **403** authorization-scoped; `qc_outcome` route **405** on GET;
`customer_decision` **409/500 — not evidenced** (NF-4); notification rows are read
from the real table (2 rows) — **D11-C1 firm-centric model unchanged**.

**PE evidence.** PE-A `/api/v3/pe/work` **200**; PE-B → PE-A workspace **403**; all
alternate routes refuse PE-B (`/work` 403, `/status`, `/pe-review`, `/pe-qc`,
`/clarify` 405); PE-B → internal item **403**; PE-A → Org B item **403**;
consultant → PE route **403**; unauthenticated **401**. **PE-A item workspace 403
(R4 unresolved).**

**Browser evidence.** Fixtures firm-scoped successfully, but the 006 browser run
did not finish within the session — **no 006 browser numbers claimed** (last
captured: 005's 16 total = 13 passed / 0 failed / 3 skipped).

**Security evidence.** Unauthenticated 401; cross-org 403/404; cross-firm 403;
suspended-grant path denied; PE isolation 403; IDOR 403; alternate-route 403/405;
replay idempotency (no duplicate rows); denied-path silence; RLS boundary 16/16
with Org A → Org B returning **200 and zero rows**. The new allowance fixture is
org-scoped only: consultant/PE/other-org reads of the entitlement remain 404/401.

**Regression evidence.** Not available for 006 (run incomplete at ~12%).

**Findings.** R1 gate cleared but **NF-4: approval path returns 500 with a
legitimate STANDARD allowance** (unresolved; diagnose from logs — do not patch
speculatively). R3 **fixed**. R4 **unresolved** (assignment NOT NULL `23502`;
PE-A 403). R5 fixtures applied, verification outstanding.

**Risks / limitations.** NF-4 may be a fixture gap or a genuine product defect —
undetermined. R4's status as a P6-2F acceptance blocker is undetermined. 006 lacks
browser and regression evidence.

**Final verdict.**
`P6-2F E2E ENVIRONMENT IMPLEMENTATION — PARTIALLY COMPLETE / BLOCKERS REMAIN`

**Stop-condition confirmation.** No stop condition was triggered and none was
worked around: no production/demo/investor change, no RLS/schema/migration change,
no new role/capability/permission, no billing/subscription/entitlement-ownership or
D6/D7/D8/D11/D11-C1/origin change, no assertion weakened, no fabricated evidence.
R4 and NF-4 were reported rather than patched speculatively.

**Git-safety confirmation.** branch `main` · HEAD
`16391217103b98dcea520070c5a22c68f12fe607` · staged **none** · commit **no** ·
push **no** · reset/clean **no** · unrelated pre-existing working-tree changes
preserved untouched.
