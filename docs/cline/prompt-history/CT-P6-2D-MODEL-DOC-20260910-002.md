# CT-P6-2D-MODEL-DOC-20260910-002 — Residual P6-2B D7b Documentation Clarification (durable task-history record)

## 1. Prompt Ref

`CT-P6-2D-MODEL-DOC-20260910-002`

## 2. Response Ref

`CT-P6-2D-MODEL-DOC-20260910-002-R`

## 3. Prompt datetime

10 September 2026 (session date; wall-clock not stated in the prompt). Received
approx. 18:00 local (Asia/Dhaka +0600).

## 4. Response datetime

10 September 2026, approx. 18:20 local.

## 5. Exact complete prompt

````text
# Cline Task — Residual P6-2B D7b Documentation Clarification

**Prompt Ref:** `CT-P6-2D-MODEL-DOC-20260910-002`
**Response Ref:** `CT-P6-2D-MODEL-DOC-20260910-002-R`
**Task Type:** DOCUMENTATION-ONLY CLARIFICATION
**Phase:** Pre-PO ratification — P6-2D / P6-2E / P6-2F
**Implementation:** NONE
**Schema changes:** NONE
**Migration:** NONE
**Code changes:** NONE
**Commit/push:** NONE

---

# 1. Purpose

Perform a narrowly scoped documentation-only clarification to remove the remaining **live-looking stale proposal wording** concerning a `CONSULTANT` `processing_origin` value.

The previous read-only reconciliation and documentation amendment established:

> `processing_origin` is the manual-processing capacity/control path: CarbonTally internal vs Processing Entity.

It is:

* NOT actor identity;
* NOT processing mode;
* NOT consultant identity;
* NOT a generic processor/actor vocabulary.

The Product Owner has withdrawn the proposed `CONSULTANT` processing-origin value.

This task exists only because the previous documentation amendment identified one residual stale statement in:

`docs/architecture/CARBONTALLY_P6_2B_CONSULTANT_REVIEW_QC_WORKFLOW_ARCHITECTURE.md`

specifically:

* §22, approximately line 415: wording concerning "consultant origin value and queue labelling"
* §5, approximately line 130, if the same proposal is represented there.

---

# 2. Required action

Inspect the current P6-2B architecture document and determine exactly where the stale proposal remains.

Add a **concise clarification/cross-reference note** at the appropriate location(s), stating that:

1. the earlier proposal to introduce `CONSULTANT` as a `processing_origin` value has been withdrawn;
2. `processing_origin` retains its established meaning;
3. consultant is an actor, not a `processing_origin` value;
4. automatic/manual processing remains an independent dimension;
5. the authoritative current definition is in:

   * `CARBONTALLY_FINAL_ARCHITECTURE_BLUEPRINT_V1.3.md`
   * `CARBONTALLY_V1.2_DUAL_ORIGIN_WORKFLOW_DESIGN.md`
   * `CARBONTALLY_P6_2_PO_DECISION_REGISTER.md`
6. no P6-2B routing semantics are changed by this clarification.

Use terminology consistent with those authoritative documents.

---

# 3. Important historical-record rule

Do NOT rewrite history.

The following historical/evidence documents may contain the old proposal:

* `docs/cline/CARBONTALLY_PHASE6_REMAINDER_UNIFIED_READINESS_ANALYSIS.md`
* relevant P6-2B implementation/readiness reports
* prompt-history records.

Do NOT alter those merely to make history appear as though the proposal never existed.

They are historical evidence.

The objective is only to prevent the **authoritative P6-2B architecture document** from being misread as containing a currently valid proposal.

If you believe any historical document genuinely requires a correction rather than historical preservation, STOP and report it instead of editing it.

---

# 4. Do not change architecture

Do not:

* add `CONSULTANT` to any enum;
* remove `CONSULTANT` from any database enum;
* change a CHECK constraint;
* modify routing;
* modify queue behavior;
* modify permissions;
* modify capabilities;
* modify RLS;
* modify workflow;
* modify code;
* modify tests;
* modify migrations;
* modify billing;
* modify frontend;
* change P6-2B behavior.

This is documentation only.

# 5. Required source inspection

Before editing, inspect:

`docs/architecture/CARBONTALLY_P6_2B_CONSULTANT_REVIEW_QC_WORKFLOW_ARCHITECTURE.md`

and the already-amended authoritative documents:

`docs/architecture/CARBONTALLY_FINAL_ARCHITECTURE_BLUEPRINT_V1.3.md`

`docs/architecture/CARBONTALLY_V1.2_DUAL_ORIGIN_WORKFLOW_DESIGN.md`

`docs/architecture/CARBONTALLY_P6_2_CONSULTANT_PROCESSING_WORKFLOW_ARCHITECTURE.md`

`docs/architecture/CARBONTALLY_P6_2_PO_DECISION_REGISTER.md`

Also inspect:

`docs/cline/CARBONTALLY_P6_2_PROCESSING_MODEL_DOCUMENTATION_AMENDMENT_REPORT.md`

Use the existing reconciliation evidence rather than inventing new terminology.

---

# 6. Editing principle

Prefer the smallest possible additive clarification.

Do NOT rewrite §22 or the P6-2B document wholesale.

If a single explanatory note/cross-reference is sufficient, use that.

Preserve:

* historical context;
* P6-2B design;
* existing routing;
* existing QC semantics;
* closed-gate status.

The clarification should make it impossible for a future implementer to interpret the old phrase "consultant origin value" as an active requirement.

---

# 7. Required validation

After editing:

Search the P6-2B architecture document for:

* `consultant origin`
* `consultant-origin`
* `CONSULTANT`
* `processing_origin`
* `queue labelling`
* `queue labeling`

Confirm that any remaining reference to `CONSULTANT` is clearly historical/contextual or explicitly says the proposal was withdrawn.

Confirm there is no remaining wording that instructs a future implementation to:

> add `CONSULTANT` to `processing_origin`.

Also perform a targeted consistency search across the three current authoritative architecture documents and PO register.

Do not perform a broad historical rewrite.

---

# 8. Required report

Create:

`docs/cline/CARBONTALLY_P6_2D_RESIDUAL_ORIGIN_WORDING_CLARIFICATION_REPORT.md`

Include:

1. Executive Summary
2. Stale Wording Found
3. Location(s)
4. Clarification Applied
5. Historical Documents Preserved
6. Consistency Validation
7. Files Changed
8. Files Unchanged
9. Git State
10. Final Verdict
11. Stop-Condition Confirmation

---

# 9. Durable task-history record

Create:

`docs/cline/prompt-history/CT-P6-2D-MODEL-DOC-20260910-002.md`

It must preserve:

* Prompt Ref
* Response Ref
* exact complete prompt
* prompt datetime
* response datetime
* complete Cline response
* files inspected
* files changed
* files unchanged
* exact wording/sections changed
* validation commands
* findings
* risks
* verdict
* stop-condition confirmation.

---

# 10. Git discipline

Before:

* record branch;
* record HEAD;
* record working-tree status.

After:

* record branch;
* record HEAD;
* record working-tree status.

Expected modification:

* P6-2B architecture documentation;
* new clarification report;
* new prompt-history record.

No commit.

No push.

Do not revert pre-existing changes.

If unexpected implementation files change:

**STOP immediately.**

---

# 11. Final verdict

Use:

`DOCUMENTATION CLARIFICATION COMPLETE — READY FOR PO RATIFICATION`

if the stale authoritative wording has been clarified and no contradiction remains.

Use:

`DOCUMENTATION CLARIFICATION COMPLETE — ADDITIONAL CLARIFICATION REQUIRED`

if another authoritative contradiction remains.

Use:

`STOPPED — UNEXPECTED AUTHORITY CONFLICT`

if an actual authority conflict is discovered.

---

# 12. Final response

Return:

## 1. Final Verdict

## 2. Stale Wording Found

## 3. Clarification Applied

## 4. Historical Records Preserved

## 5. Consistency Check

## 6. Files Changed

## 7. Files Unchanged

## 8. Git State

## 9. Durable Record

## 10. Stop Condition

**This task is documentation-only. Do not modify code, schema, migration, RLS, permissions, billing, workflow, frontend, tests or data.**
````

## 6. Complete Cline response

Substance (the full formatted response is returned to the user):

1. **Final Verdict** — `DOCUMENTATION CLARIFICATION COMPLETE — READY FOR PO RATIFICATION`.
2. **Stale Wording Found** — three locations in the P6-2B architecture document
   (§21 `P6-2B-P3` recommendation "a distinct `CONSULTANT` origin"; §22 `P6-2B-2`
   bullet "consultant origin value and queue labelling"; §6 item 4 "consultant-origin
   processing").
3. **Clarification Applied** — two nested additive clarification notes (§21, §22) plus
   one inline italic parenthetical (§6 item 4), each stating the withdrawal
   (`PO-P6-2D-D7b-R-20260910`), the retained origin meaning, consultant = actor (not an
   origin value), mode independence, the authoritative references, and that no P6-2B
   routing/QC semantics change.
4. **Historical Records Preserved** — no historical/evidence document was edited.
5. **Consistency Check** — no remaining wording instructs adding `CONSULTANT` to
   `processing_origin`; no contradiction between the clarified P6-2B document and the
   Blueprint V1.3 / V1.2 §4 / PO register.
6. **Files Changed** — the P6-2B architecture document; two new `docs/cline/` files.
7. **Files Unchanged** — all code/tests/schema/RLS/billing/frontend/config and every
   other document.
8. **Git State** — `main`, HEAD `1639121`, 661 → 662 porcelain entries (the +1 is the
   new report); no commit or push.
9. **Durable Record** — this file.
10. **Stop Condition** — completed normally.

## 7. Files inspected

`docs/architecture/CARBONTALLY_P6_2B_CONSULTANT_REVIEW_QC_WORKFLOW_ARCHITECTURE.md`
(§6, §17, §21, §22 and all `consultant origin`/`CONSULTANT`/`processing_origin`
occurrences);
`docs/architecture/CARBONTALLY_FINAL_ARCHITECTURE_BLUEPRINT_V1.3.md` (§9 amendment);
`docs/architecture/CARBONTALLY_V1.2_DUAL_ORIGIN_WORKFLOW_DESIGN.md` (§4/§5);
`docs/architecture/CARBONTALLY_P6_2_CONSULTANT_PROCESSING_WORKFLOW_ARCHITECTURE.md`
(§13 amendment note);
`docs/architecture/CARBONTALLY_P6_2_PO_DECISION_REGISTER.md` (D5/D7 notes, P6-2D
decision section);
`docs/cline/CARBONTALLY_P6_2_PROCESSING_MODEL_DOCUMENTATION_AMENDMENT_REPORT.md`;
`docs/cline/CARBONTALLY_P6_2_PROCESSING_MODEL_RECONCILIATION_REPORT.md`.

## 8. Files changed

| File | Change |
|---|---|
| `docs/architecture/CARBONTALLY_P6_2B_CONSULTANT_REVIEW_QC_WORKFLOW_ARCHITECTURE.md` | +§21 P6-2B-P3 nested clarification note (l.408–420); +§22 P6-2B-2 nested clarification note (l.432–443); +§6 item 4 inline parenthetical (l.130–132); one blank line restored before `## 22` |
| `docs/cline/CARBONTALLY_P6_2D_RESIDUAL_ORIGIN_WORDING_CLARIFICATION_REPORT.md` | **new** |
| `docs/cline/prompt-history/CT-P6-2D-MODEL-DOC-20260910-002.md` | **new** (this record) |

## 9. Files unchanged

All backend, frontend, test, `supabase/**`, billing, auth, permission, capability,
configuration, fixture and demo-data files (repo-wide mtime scan of `backend/`,
`frontend/`, `supabase/`, `tools/` returned **zero** hits); the three authoritative
documents amended in the previous task; the PO register; the Master Roadmap; the P6-2C
artefacts; all P6-2B implementation/verification reports; all prompt-history records;
the pre-existing working-tree change set.

## 10. Exact wording/sections changed

- §6 item 4 — appended: *("Consultant-origin processing" here means processing
  performed **by a consultant actor** — it is not a `processing_origin` value; see the
  clarification note in §22.)*
- §21 P6-2B-P3 — appended nested note: *"Clarification (10 September 2026) — this
  recommendation is superseded.* The proposed `CONSULTANT` **`processing_origin` value
  is withdrawn** … `processing_origin` retains its established meaning: the
  **manual-processing capacity/control path** … It is **not** actor identity, **not**
  processing mode and **not** a consultant vocabulary. A consultant is an **actor**;
  automatic vs manual processing remains an **independent dimension**. Only the
  **queue/label visibility and CT-QC guard-parity** aspects of this item remain
  relevant. Authoritative current definitions: Blueprint V1.3 §9, V1.2 dual-origin §4,
  PO decision register."
- §22 P6-2B-2 — appended nested note: *"Clarification (10 September 2026):*
  'consultant origin value' in this bullet is **withdrawn** — no `CONSULTANT` value is
  added to `processing_origin` … what remains applicable is **queue/label
  visibility** … plus **customer-review guard parity** … **No P6-2B routing, QC or
  state-machine semantics are changed by this clarification** …"

## 11. Validation commands

```
git rev-parse --abbrev-ref HEAD ; git rev-parse --short HEAD ; git status --porcelain=v1 | wc -l ; date
stat -c '%y %n' docs/architecture/CARBONTALLY_P6_2B_CONSULTANT_REVIEW_QC_WORKFLOW_ARCHITECTURE.md
grep -n 'consultant origin\|consultant-origin\|CONSULTANT\|processing_origin\|queue labelling\|queue labeling' CARBONTALLY_P6_2B_CONSULTANT_REVIEW_QC_WORKFLOW_ARCHITECTURE.md
grep -n 'withdrawn\|Clarification (10 September 2026)' CARBONTALLY_P6_2B_CONSULTANT_REVIEW_QC_WORKFLOW_ARCHITECTURE.md
grep -n 'processing_origin.*actor\|actor.*processing_origin\|origin.*processing mode' <Blueprint V1.3> <V1.2> <P6-2 arch> <register>
grep -rn 'add .*CONSULTANT.*processing_origin\|CONSULTANT.*origin value' <P6-2B arch> <P6-2 arch> <Blueprint> <V1.2>
find backend frontend supabase tools -type f -newermt '2026-09-10 18:00' ! -path '*__pycache__*' ! -path '*node_modules*'
```

Results: the target-term scan shows every remaining occurrence is either
contextualised by a note, a capability-map identifier (`CONSULTANT_PERMISSIONS`), the
correct V1.2 origin statement, or preserved historical text immediately followed by an
explicit "withdrawn/superseded" note; the "add `CONSULTANT` to `processing_origin`"
search returned **no matches**; the mtime scan returned **no implementation files**.

## 12. Findings

1. The stale proposal existed in exactly three places (one recommendation, one
   workstream bullet, one descriptive phrase) — all now annotated additively.
2. No other P6-2B document content contradicts the current origin model; §17's origin
   statement and the `CONSULTANT_PERMISSIONS` reference are correct/orthogonal and were
   left untouched.
3. No authority-vs-authority conflict exists — the earlier issue was
   proposal-vs-architecture and is now fully resolved in the authoritative corpus.
4. Historical evidence documents were deliberately preserved unedited.

## 13. Risks

| # | Risk | Mitigation |
|---|---|---|
| R1 | Nested notes could be overlooked by a reader skimming bullets | Notes sit immediately adjacent to the stale text and quote the exact phrase; the register decision is the authority |
| R2 | Historical preservation could be mistaken for an active proposal | Each stale passage now carries an explicit "withdrawn/superseded" note |
| R3 | Future edits re-introduce consultant-origin language | Blueprint §9 and V1.2 §4 state the concept boundary explicitly; the register records the reason |

## 14. Final verdict

`DOCUMENTATION CLARIFICATION COMPLETE — READY FOR PO RATIFICATION`

## 15. Stop-condition confirmation

Completed normally; no stop condition triggered; nothing committed or pushed.
