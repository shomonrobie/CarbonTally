# CT-P6-2F-ENV-IMPL-20260911-005

**Prompt Ref:** `CT-P6-2F-ENV-IMPL-20260911-005`
**Prompt datetime:** 2026-09-11
**Response Ref:** `CT-P6-2F-ENV-IMPL-20260911-005-R1`
**Response datetime:** 2026-09-11
**Phase:** P6-2F · **Mode:** IMPLEMENTATION / ACCEPTANCE COMPLETION
**Prior:** `CT-P6-2F-ENV-IMPL-20260911-004` · **HEAD:** `16391217103b98dcea520070c5a22c68f12fe607`

## 1. Prompt record (structure-preserving digest, 20 sections)

1. **Mandatory continuation** — continue from the exact working tree/running E2E
   state; do not repeat 004's provisioning, seeding, grants, RLS-preservation
   proof, personas, harness, or the 16/16 · 1,693/1,693 · 10/3/3 evidence.
2. **Authoritative context** — minimum reading of the blueprint, roadmap,
   implementation contract, PO decision register, CP-2 closure preflight, the
   latest E2E environment report and prompt-history 004. Frozen D6, D7, D7b, D8,
   D11, D11-C1 and the P6-2F gate must remain unchanged.
3. **Only remaining scope** — (A) three browser DENY expectation mismatches,
   (B) three state-based skips, (C) D6 evidence, (D) D8 evidence, (E) D11
   event-level evidence, (F) PE alternate-route/browser denial, (G) the narrow
   fixture/harness changes needed, (H) full regression, (I) report + history.
4. **Hard scope boundaries** — no production/demo/investor change, no RLS change
   or weakening, no broad privileges, no new roles/capabilities/permissions, no
   consultant conversation kind, no `CONSULTANT` origin, no D6/D7/D8/D11 or
   billing redesign, no IV-N1…IV-N5 change, no Phase 7/8, no commit/push/reset/
   clean, no unrelated refactor or doc reorganisation.
5. **First action** — inspect state (git, HEAD, stack, report, specs, the exact
   3 FAIL and 3 SKIP specs and their conditions, existing D6/D8/D11/PE harness),
   build a checklist, *then* proceed.
6. **A — expectation mismatches** — do not weaken assertions; identify spec,
   request, intended contract, legitimately possible statuses and semantic
   validity; make the smallest defensible correction; record old/new assertion
   and why security is not weakened; STOP and record if indeterminate.
7. **B — eliminate skips** — not by deletion/pass-faking/weakening/fake auth;
   create the minimum deterministic E2E fixture/state, real Auth/JWT where
   required, legitimately reaching the lifecycle state, notifications generated
   through the actual workflow, D11-C1 unchanged; STOP if a schema/RLS/role/
   capability change would be required.
8. **C — D6** — entitlement permits/denies, client-org ownership, no ownership
   transfer to consultant/PE, actor injection cannot transfer, approval-time
   enforcement canonical, replay/idempotency; real identities; no billing
   redesign; record endpoint/identity/expected/observed/evidence.
9. **D — D8** — existing conversation model only, authorised consultant
   communication, cross-org/cross-firm denial, no new kind/table/role; record the
   observed `conversation_kind` values.
10. **E — D11** — all five events through the real workflow with trigger, actor,
    recipients, deterministic key, durable persistence, replay idempotency,
    recipient derivation, isolation, denied-path silence, no direct notification
    insertion for the primary acceptance, EventBus not authoritative.
11. **F — PE** — PE-A ALLOW, PE-B DENY, IDOR substitution, alternate routes,
    browser denial where a route exists, no new PE capability, document absent
    routes and prove the inventory was checked.
12. **Browser acceptance** — real isolated Supabase/GoTrue/JWT/CRA/backend/data;
    no required skips; capture totals, pass/fail/skip, exit code, duration.
13. **Security negative testing** — preserve/rerun the full negative matrix; not
    status alone — verify data, DB, notification and audit side effects.
14. **Regression** — `pytest backend/tests/unit backend/tests/e2e` + full
    Playwright + focused P6-2E/D6/D8/D11 tests; exact counts and exit codes; no
    claims from partial runs.
15. **Environment safety** — all data inside the isolated E2E environment; prove
    no production endpoint/credential; demo/investor untouched; do not tear down
    if the next session needs the stack.
16. **Git safety** — no commit/push/reset/clean; report branch, HEAD, modified,
    new, and preserved unrelated changes.
17. **Documentation** — update the existing E2E environment report (no new master
    document) distinguishing 004 vs 005 evidence, and create this history file
    with every listed element, omitting no failed/skipped/blocked evidence.
18. **Stop conditions** — production/demo DB or RLS change, new
    role/capability/permission, migration, D6/D7/D8/D11/D11-C1 policy change,
    `CONSULTANT` origin, billing redesign, a browser test that can only pass by
    weakening a security assertion, missing real Auth/RLS, an absent PE route
    requiring product scope, overwriting unrelated work, or evidence requiring
    guesswork → document the blocker and stop.
19. **Final verdict rule** — exactly COMPLETE / PARTIALLY COMPLETE / BLOCKED; the
    words VERIFIED, INDEPENDENTLY VERIFIED, CLOSED and PHASE 6 COMPLETE are
    forbidden.
20. **Completion criteria + response format** — verdict A only if all 18 criteria
    hold; otherwise B or C, with the prescribed response sections.

## 2. Response record

**Already complete and reused (no-repeat confirmation).** The isolated
`carbontally_e2e` stack (`:55325`/`:55326`), migrations, the E2E-only
`grant_service_role.sql` + `grant_authenticated.sql` privilege baseline and the
RLS-preservation proof, the synthetic orgs/firms/PEs/personas, consultant grants,
PE test data, `seed_e2e.py`, `run_acceptance.py` (16/16), the FastAPI + CRA
bring-up and isolation from production, the IV-N6 deep link, and the existing
route-protection implementation were **all reused as-is**. No environment was
rebuilt or reseeded from scratch.

**Files inspected.** `tests/e2e/carbontally/security-denies.spec.ts`,
`consultant-lifecycle.spec.ts`, `tests/e2e/personas.ts`, `playwright.config.ts`,
`e2e/environment/scripts/{seed_e2e.py,teardown.sh,bootstrap.sh,reset.sh}`,
`backend/services/billing.py` (the entitlement gate at lines 167-190),
`backend/data/billing.py` (`get_active_for_org` → `public.customer_subscriptions`),
`frontend/src/v3/api.js` + `frontend/src/v3/consultant/ConsultantItemPage.jsx`
(status gating: `SUBMITTABLE_STATUS='consultant_reviewed'`), the v3 router
inventory (`v3_processing_workflow.py`, `v3_qc.py`, `v3_billing.py`,
`v3_commercial.py`, `v3_messaging.py`, `v3_pe.py`, `v3_consultants.py`), the
`customer_subscriptions` CHECK constraints, and prompt-history 004.

**Files changed (005).**

* `tests/e2e/personas.ts` — added `apiBase()`, `sessionToken(page)`, `apiCall()`
  and lifecycle item ids (`itemA2/A3/A6/A7`).
* `tests/e2e/carbontally/security-denies.spec.ts` — the three DENY specs now hit
  the real API origin with real JWTs and assert the response is not the SPA.
* `tests/e2e/carbontally/consultant-lifecycle.spec.ts` — submit-to-QC uses the
  `consultant_reviewed` fixture item.
* `e2e/environment/scripts/seed_lifecycle_fixtures.py` (**new**) — lifecycle-state
  items + active client-org `customer_subscriptions` + a real `consultant-submit`
  so D11 emits the notification through the workflow.
* `e2e/environment/scripts/run_p6f_acceptance.py` (**new**) — D6/D8/D11/PE matrix.
* `e2e/environment/scripts/bootstrap.sh`, `reset.sh` — run the new fixture script.
* `docs/architecture/CARBONTALLY_P6_2F_E2E_ENVIRONMENT_IMPLEMENTATION_REPORT.md`
  — §8 addendum.
* `docs/cline/prompt-history/CT-P6-2F-ENV-IMPL-20260911-005.md` (this file).

**Commands executed.** `git branch/rev-parse/diff --cached`; stack health curls
(`:8051`, `:3000`, `:55325`); `information_schema` / `pg_policies` /
`pg_constraint` queries on `:55326`; `seed_lifecycle_fixtures.py`;
`run_acceptance.py`; `run_p6f_acceptance.py`; `npx playwright test` with
`tests/e2e/.env.personas`; `pytest backend/tests/unit backend/tests/e2e`
(background, incomplete).

**Test results.** `run_acceptance.py` **16/16**; `run_p6f_acceptance.py`
**34/37**; `npx playwright test` **16 total = 13 passed, 0 failed, 3 skipped,
exit 1, 26.1s**; pytest regression launched but **not completed** — no counts
claimed.

**Findings.** Largely the same as report §8.5: the three browser mismatches were
caused by specs resolving against the SPA origin (fixed, not weakened); the
D6/D11/browser gaps share one root cause — no credit/allowance fixture, so the
canonical approval gate returns 402; the consultant-surface fixture items are not
consultant-firm-scoped, so their action buttons do not render; PE-A item
workspace returns 403 while every PE DENY path passes.

**Decisions.** Adjudicated the browser mismatches as *spec defects* (the SPA 200
was masking the API) and corrected the assertions to the real boundary rather
than loosening them; added E2E-only fixtures (never product code); left the two
unresolved items (credit/allowance fixture, PE-A ALLOW) documented rather than
guessed.

**Risks / limitations.** R1–R5 in report §8.5. The environment remains running
for the next session; `teardown.sh` was not executed.

**Final verdict.**
`P6-2F E2E ENVIRONMENT IMPLEMENTATION — PARTIALLY COMPLETE / BLOCKERS REMAIN`

**Stop-condition confirmation.** No stop condition was triggered and none was
worked around: no production/demo/investor modification, no RLS or schema change,
no new role/capability/permission, no migration, no D6/D7/D8/D11/D11-C1 change,
no `CONSULTANT` origin, no billing redesign, no security assertion weakened. No
commit, push, reset, clean or staging was performed; unrelated working-tree
changes (e.g. `.agents/skills/**`) were preserved untouched.
