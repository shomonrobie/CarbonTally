# CT-P6-2F-REMED-20260911-009

**Prompt Ref:** `CT-P6-2F-REMED-20260911-009` · **Date/time:** 2026-09-11
**Mode:** BOUNDED REMEDIATION (Cline = remediation agent, not verifier)
**Previous:** `CT-P6-2F-REMED-20260911-008` · **Independent verification:** `CT-P6-2F-IV-20260911-002` (FAIL)
**Branch:** `main` · **HEAD:** `16391217103b98dcea520070c5a22c68f12fe607`

## 1. Prompt record (digest)

1. **Purpose** — resolve only Blocker A (NF-4: `to_char(unknown, unknown)` not
   unique at `backend/data/billing.py:849`, reached from
   `backend/api/v3_processing_workflow.py:987`) and Blocker B (R4: PE-A cannot open
   its intended workspace because no valid `work_item_assignments` row exists).
   No environment re-provisioning, no redesign.
2. **No-repeat rule** — reuse the running isolated environment; do not redo
   provisioning, migrations, grants, RLS, personas, topology, entitlement /
   subscription / allowance / lifecycle fixtures, harness or browser corrections.
3. **NF-4 fix rule** — inspect the exact `record` SQL, confirm the untyped
   parameters, apply the **smallest type-safe** correction preserving existing
   date/time formatting semantics; derive the intended type from the expression,
   declarations, surrounding SQL and schema — do not guess. No billing redesign; no
   subscription/entitlement/allowance semantics or policy change.
4. **NF-4 regression** — add focused coverage proving the STANDARD-mode usage write
   no longer raises `AmbiguousFunctionError`, exercising the real SQL through the
   existing interface (no `to_char` mocking, no new framework); document any
   architectural limitation otherwise.
5. **D6 validation** — Org A owner, real JWT, existing entitlement, STANDARD
   subscription, allowance fixture and eligible item: execute the canonical
   customer-review path and record before/request/after state (item, usage,
   allowance, entitlement), then replay and prove consumption is not duplicated.
6. **R4 fixture** — create exactly the established shape (`processing_entity_id =
   PE_A`, `assigned_to = NULL`, `assignee_kind='processing_entity'`,
   `action='assign'`, `actor_domain='processing_entity'`, `assigned_by = PE-A user`,
   `status='open'`); do not repeat the malformed insert.
7. **Prefer the application path** — use an existing service function if one exists;
   otherwise the minimum valid isolated fixture.
8. **R4 acceptance** — PE-A workspace 200; PE-B 403; cross-org 403; IDOR 403;
   unauthenticated 401; inspect alternate routes; do not weaken authorization.
9. **D11** — do not redesign; preserve the five events, deterministic keys, durable
   notifications, server-derived recipients, idempotency, denied-path silence and
   D11-C1 firm-centric recipients.
10. **D11 evidence** — execute the legitimate mutation paths for all five events,
    recording action/actor/item/response/state/notification/key/recipient/replay/
    denial.
11. **Browser** — run the consultant specs; do not remove skips or convert them into
    passes; record total/passed/failed/skipped/exit code.
12. **API acceptance** — `run_acceptance.py` (expected 16/16) then
    `run_p6f_acceptance.py`; do not reuse the historical 34/37.
13. **Full regression** — `pytest backend/tests/unit backend/tests/e2e` must finish;
    record collected/passed/failed/errors/skipped/exit code/duration.
14. **Security regression** — RLS, roles, capabilities, permissions, organisation/
    consultant/PE isolation, entitlement ownership, `processing_origin`, D7, D8 and
    D11-C1 unchanged; the successful D6 approval must still belong to Org A.
15. **Environment integrity** — stay inside the isolated Supabase/Postgres/GoTrue/
    API/frontend; never touch production/demo/investor.
16. **Git safety** — no commit/push/reset/clean/staging; preserve unrelated changes.
17. **Documentation** — bounded 009 section in the existing E2E report + this file.
18. **Stop conditions** — production/demo change, migration, RLS change, new role/
    capability/permission, billing/subscription/entitlement redesign,
    `processing_origin` change, D7/D8/D11/D11-C1 change, PE authorization redesign,
    unrelated refactor.
19. **Verdicts** — A / B / C with the 25-item verdict-A checklist; never claim
    independent verification, verified, closed or Phase 6 complete.
20. **Next step after A** — stop; then a fresh independent verification permitted to
    execute state-changing workflow actions against the disposable isolated
    environment while remaining read-only on code/schema/fixtures/config.

## 2. Response record

**Blocker A — NF-4: FIXED (defect removed).** Located the exact statement in
`backend/data/billing.py::UsageTrackingRepository.record`:

```sql
VALUES ($1, $2, to_char($2, 'YYYY-MM'), $3)
```

`$2` is a Python `datetime` sent untyped → `to_char(unknown, unknown)` is ambiguous
→ `AmbiguousFunctionError` → HTTP 500. Applied the smallest type-safe correction,
preserving the existing formatting semantics:

```sql
VALUES ($1, $2::timestamptz, to_char($2::timestamptz, 'YYYY-MM'), $3)
```

The API was restarted against the isolated environment. **Evidence:** the canonical
customer-review no longer raises `AmbiguousFunctionError` and no longer returns 500;
it now returns a deterministic **409** state-machine refusal, proving execution
passes the previously crashing usage write. `run_acceptance.py` remained 16/16.

**Blocker B — R4: fixture accepted, authorization still refuses.** The corrected row
(`processing_entity_id = PE_A`, `assigned_to = NULL`,
`assignee_kind='processing_entity'`, `action='assign'`,
`actor_domain='processing_entity'`, `assigned_by = <PE-A user>`, `status='open'`) was
**accepted (`assign : 201`)**. Nevertheless `GET /api/v3/pe/items/{ITEM_PE_A}/workspace`
as `pe_a_manager` still returns **403** (PE-B 403, PE-A→Org-B 403, unauth 401).
Because a schema-valid open item-level assignment now exists, the refusal is no
longer attributable to the missing fixture; it must be traced in
`work_item_effective_entity` (item-vs-batch precedence, or the entity identity the
PE context supplies). Per §10 the authorization path was **not** patched
speculatively.

**Test results (this cycle).** `run_acceptance.py` **16/16** ·
`run_p6f_acceptance.py` **34/37** (D6 approval-time 409, `customer_decision` 409,
PE-A workspace 403, D8 6/6 and other denials unchanged) ·
`npx playwright test` **16 total = 13 passed / 0 failed / 3 skipped, exit 1, 34.4s** ·
`pytest backend/tests/unit backend/tests/e2e` launched but **not completed** — no
counts claimed · fixtures: `items 200`, `item A 200`, `allow 200`, **`assign 201`**,
`submit → 200`.

**NF-4 regression test.** Not added: the prescribed coverage (proving the real SQL no
longer raises `AmbiguousFunctionError`) requires executing that statement against a
real PostgreSQL connection, which the existing unit-test architecture does not
provide for this repository class — documented as the §4 limitation rather than
mocked. The defect's removal is evidenced by the live canonical request no longer
erroring at that statement.

**Remaining blockers.** (1) D6 canonical approval still refused at the
state-machine step (409) — the transition precondition must be diagnosed, not
changed; (2) PE-A workspace still 403 with a valid assignment —
`work_item_effective_entity` must be traced; (3) three consultant browser specs
still skip; (4) full pytest not completed. No architecture, billing, subscription,
entitlement, RLS, role, capability, `processing_origin`, D7, D8, D11 or D11-C1
change was made or required.

**Security / environment.** All work stayed in the isolated environment
(`:55325` / `:55326` / `:8051` / `:3000`); production, demo and investor were not
touched; no production credential or endpoint used.

**Git safety.** branch `main` · HEAD `16391217103b98dcea520070c5a22c68f12fe607` ·
staged **none** · commit **no** · push **no** · reset/clean **no** · unrelated
pre-existing working-tree modifications preserved.

**Stop conditions.** None triggered; no prohibited change was made or required.

**No self-verification.** P6-2F is not verified, not closed; Phase 6 is not complete.

**Final verdict.**
`P6-2F REMEDIATION PARTIALLY COMPLETE — BLOCKERS REMAIN`
