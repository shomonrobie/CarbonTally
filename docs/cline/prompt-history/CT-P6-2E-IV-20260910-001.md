# CarbonTally — Durable Prompt History

**Prompt Ref:** `CT-P6-2E-IV-20260910-001`
**Prompt datetime:** 2026-09-10 (start of the P6-2E independent verification session)
**Response Ref:** `CT-P6-2E-IV-20260910-001-R1`
**Response datetime:** 2026-09-10 23:00 → 23:45 (+0600)
**Phase / Checkpoint / Gate:** Phase 6 / CP2 / P6-2E — Consultant Vocabulary & Lifecycle Events
**Mode:** INDEPENDENT VERIFICATION — READ-ONLY / NO REMEDIATION
**Verdict:** `P6-2E INDEPENDENTLY VERIFIED — PASS WITH NON-BLOCKING FINDINGS`
**Status:** COMPLETE — stopped at the P6-2E boundary

---

## PART A — EXACT PROMPT (verbatim, fenced chunks)

### Chunk 1 (header → §7)

```markdown
# CarbonTally — P6-2E Independent Verification

**Prompt Ref:** `CT-P6-2E-IV-20260910-001`
**Date:** 2026-09-10
**Phase:** Phase 6
**Checkpoint:** CP2
**Gate:** P6-2E — Consultant Vocabulary & Lifecycle Events
**Verification Type:** Independent Verification (IV)
**Mode:** READ-ONLY / NO REMEDIATION
**Session:** Fresh independent Cline session

---

# 1. PURPOSE

Perform a completely independent verification of the completed P6-2E implementation.

The implementation session reported:

`P6-2E IMPLEMENTED — READY FOR INDEPENDENT VERIFICATION`

Your task is to independently establish whether that statement is justified.

Do NOT trust the implementation report as proof.

Re-derive the result from:

* authoritative architecture/decision documents;
* actual repository code;
* database/migration state;
* notification/event infrastructure;
* authorization logic;
* tests;
* runtime behavior where safely possible;
* actual repository state.

This is verification only.

---

# 2. ABSOLUTE READ-ONLY RULE

You MUST NOT:

* modify production code;
* modify tests;
* modify migrations;
* modify schema;
* modify RLS;
* modify frontend;
* modify configuration;
* fix defects;
* refactor code;
* rewrite tests;
* alter test expectations;
* create migrations;
* stage files;
* commit;
* push;
* reset;
* clean the repository.

Do NOT use:

`git add`

`git commit`

`git push`

`git reset`

`git clean`

If you discover a defect, document it.

**Do not fix it.**

---

# 3. AUTHORITATIVE BASIS

Read and establish the applicable requirements from:

1. `docs/architecture/CARBONTALLY_FINAL_ARCHITECTURE_BLUEPRINT_V1.3.md`
2. `docs/architecture/CARBONTALLY_MASTER_PROJECT_ROADMAP_V1.0.md`
3. `docs/architecture/CARBONTALLY_P6_2_PO_DECISION_REGISTER.md`
4. `docs/architecture/CARBONTALLY_PHASE6_REMAINDER_IMPLEMENTATION_CONTRACT_V1.0.md`
5. `docs/architecture/CARBONTALLY_P6_2_CONSULTANT_PROCESSING_WORKFLOW_ARCHITECTURE.md`
6. relevant conversation/messaging architecture
7. relevant notification/event architecture
8. `docs/cline/CARBONTALLY_P6_2E_PREFLIGHT_REPORT.md`
9. `docs/cline/CARBONTALLY_P6_2E_IMPLEMENTATION_REPORT.md`
10. `docs/cline/CARBONTALLY_CP1_CLOSURE_REPORT.md`

Also inspect the implementation prompt/history if necessary:

`docs/cline/prompt-history/CT-P6-2E-IMPL-20260910-001.md`

The implementation report is evidence to challenge, not an authority for PASS.

---

# 4. P6-2D IS CLOSED

Do not reopen P6-2D.

The following are frozen:

* consultant firm provenance;
* processing-mode provenance;
* processing-origin vocabulary;
* D6 entitlement/billing boundary.

Do NOT verify by modifying P6-2D.

Do NOT implement F1.

Do NOT add provenance to `confirm`/`retry`.

If P6-2E code appears to affect P6-2D behavior, assess it and report it, but do not fix it.

---

# 5. D8 VERIFICATION

D8 requires reuse of the existing conversation model.

Independently verify:

* consultants participate through the existing conversation mechanism;
* active consultant grants determine appropriate participation;
* existing `conversation_kind` vocabulary remains unchanged;
* there is no consultant-specific conversation kind;
* no consultant-specific conversation table exists;
* no new role/capability/permission was introduced;
* organisation isolation remains intact;
* consultant participation does not bypass existing authorization.

Verify actual code paths, not comments.

Confirm that the reported three D8 regression tests are meaningful.

---
```

### Chunk 2 (§6 → §12)

```markdown
# 6. D11 — FIVE EVENTS

Independently verify exactly these five lifecycle events:

1. `accepted`
2. `submitted_to_qc`
3. `qc_outcome`
4. `customer_decision`
5. `rework`

For each event independently determine:

* exact trigger;
* successful state transition;
* event type;
* event key;
* actor domain;
* recipient derivation;
* authorization prerequisite;
* persistence mechanism;
* duplicate behavior;
* denied-path behavior.

Produce a matrix:

| Event | Trigger | Authorization | Event key | Recipients | Durable persistence | Idempotency | Denied path | Result |
| ----- | ------- | ------------- | --------- | ---------- | ------------------- | ----------- | ----------- | ------ |

Do not simply reproduce the implementation report.

---

# 7. EVENT 1 — ACCEPTED

Verify the engagement `/accept` path.

Determine:

* who is authorized to accept;
* whether the acceptance transition succeeds before emission;
* whether the event is emitted exactly once;
* whether firm recipients are derived from the actual engaged firm;
* whether inactive/suspended members are excluded;
* whether another organisation's users can receive it;
* whether a client can inject recipients;
* whether replay creates duplicates.

Expected lifecycle meaning:

**Consultant engagement accepted.**

If implementation semantics differ materially from the ratified meaning, report it.

---

# 8. EVENT 2 — SUBMITTED TO QC

Verify:

`/items/{id}/consultant-submit`

Confirm:

* correct consultant authorization;
* active grant/capability requirements;
* correct item/organisation;
* successful transition precedes emission;
* firm recipients are correct;
* internal QC/Ops recipients are correct;
* client cannot control recipients;
* cross-firm recipients are excluded;
* cross-org recipients are excluded;
* duplicate submission cannot create duplicate lifecycle events;
* alternate submit-review route cannot bypass lifecycle semantics.

---

# 9. EVENT 3 — QC OUTCOME

Verify:

`/ops/qc/items/{id}/decision`

Test/trace both:

* approved;
* rejected.

Confirm:

* event is emitted only after valid QC outcome;
* event key distinguishes approved vs rejected;
* recipients are correct;
* unauthorized QC decisions emit nothing;
* invalid transitions emit nothing;
* replay does not create duplicates.

---

# 10. EVENT 4 — CUSTOMER DECISION

Verify:

`/items/{id}/customer-review`

Test/trace:

* approved;
* rejected;
* unauthorized;
* invalid state.

Confirm:

* event only follows a valid customer decision;
* event key distinguishes outcome;
* recipients are correct;
* no cross-tenant leakage;
* no client-controlled recipient list;
* duplicate/replay is suppressed.

---

# 11. EVENT 5 — REWORK

Verify both reported rework sources:

1. CT-QC rejection;
2. consultant review with `passed=false`.

Determine precisely:

* when `rework` is emitted;
* what state transition establishes it;
* whether the event is emitted once per intended lifecycle event;
* whether repeated cycles are represented correctly;
* whether the event key remains deterministic;
* whether different lifecycle cycles could collide.

### IMPORTANT

The implementation report states:

> a second rework cycle from the same source state does not re-notify because this would require durable per-item cycle state.

Determine whether this is:

* permitted by the ratified D11 contract;
* a non-blocking limitation;
* a material lifecycle correctness problem;
* a security problem;
* or an ambiguity requiring PO decision.

Do not resolve the decision yourself.

---

# 12. CUSTOMER REJECTION VS REWORK

The implementation report states:

> customer rejection emits `customer_decision:rejected` rather than a `rework` event.

Independently determine whether this is consistent with the ratified D11 lifecycle semantics.

Do not assume that every rejection must equal rework.

Trace the authoritative workflow.

If the contract is ambiguous, classify it as a finding requiring PO clarification rather than inventing behavior.

---
```

### Chunk 3 (§13 → §21)

```markdown
# 13. EVENT KEY VERIFICATION

This is a critical D11 property.

For every event verify:

* deterministic key;
* stable business identity;
* no client control;
* no random UUID as the lifecycle identity;
* outcome included where required;
* distinct lifecycle events do not unintentionally collide;
* replay produces the same key;
* database uniqueness provides the final duplicate boundary.

Inspect both application-level key construction and database-level uniqueness.

Test:

1. first emission;
2. identical replay;
3. repeated HTTP request;
4. repeated durable-layer insertion;
5. different outcome;
6. different item/engagement.

Expected:

**one durable notification per intended lifecycle event.**

---

# 14. SERVER-DERIVED RECIPIENT VERIFICATION

This is a security-critical section.

Independently establish exactly how recipients are derived.

The client must NOT be able to control:

* recipient user IDs;
* recipient organisation;
* recipient firm;
* recipient email;
* event ownership;
* event scope.

Attempt/trace injection through:

* `recipients`;
* `event_key`;
* `actor_domain`;
* any equivalent client fields.

Expected:

**server-derived recipients remain authoritative.**

---

# 15. RECIPIENT ISOLATION

Verify all relevant recipient classes.

### Firm isolation

Firm A's event must not notify Firm B.

### Organisation isolation

Organisation A's event must not notify Organisation B.

### Membership

Inactive/suspended users must not receive events where the existing relationship model excludes them.

### Grant state

Suspended/inactive consultant grants must not create unauthorized recipient relationships.

### Internal recipients

CarbonTally QC/Ops recipients must be derived from existing authorized staff relationships.

Do not create new roles merely to satisfy this test.

---

# 16. RECIPIENT MATRIX CHALLENGE

Independently reconstruct the actual recipient matrix.

Do not assume the implementation's matrix is correct.

For each event determine:

* consultant firm recipients;
* client organisation recipients, if any;
* CarbonTally/QC/Ops recipients, if any.

Pay particular attention to the implementation report's statement:

> firm-centric recipients; no client-org notice because the client is the actor for events 1/4 and events 3/5 are internal quality gates.

Determine whether this interpretation follows the authoritative workflow.

If a required notification recipient is missing, classify its severity.

Do not fix it.

---

# 17. DENIED-PATH VERIFICATION

For all five events verify:

* unauthenticated request → denied;
* unauthorized user → denied;
* inactive grant → denied;
* missing capability → denied;
* invalid state transition → denied/rejected;
* failed authorization → **zero lifecycle event emitted**.

This must be demonstrated, not merely inferred.

---

# 18. POST-SUCCESS EMISSION ORDERING

Verify that lifecycle emission occurs only after the underlying business transition succeeds.

Expected conceptual ordering:

`authentication`
→ `authorization`
→ `state/data mutation`
→ `durable lifecycle event`

or an equivalent transactionally safe implementation.

Determine whether there is any path where:

* event is emitted;
* then business mutation fails.

If yes, assess whether this creates a false lifecycle notification.

Do not fix it.

---

# 19. DURABILITY / BEST-EFFORT EMISSION

The implementation report states:

> best-effort emit means a persistence outage loses the notice, not the action.

Independently verify this architecture.

Determine:

* whether event persistence is part of the same transaction;
* whether event failure can cause business mutation failure;
* whether event failure can silently lose a required lifecycle event;
* whether this violates D11;
* whether the existing architecture explicitly permits best-effort notification.

Do not automatically treat best-effort delivery as acceptable.

Classify based on the ratified requirements.

If this requires a PO/architecture decision, report it.

Do not redesign it.

---

# 20. IN-PROCESS EVENT BUS

Verify that the in-process/non-durable EventBus is NOT being used as the authoritative D11 mechanism.

Expected:

**durable notifications persistence is authoritative.**

If EventBus is used only as an auxiliary mechanism, determine whether that introduces duplicate or ordering problems.

---

# 21. ALTERNATE ROUTES

Search the repository for all routes that can perform the relevant lifecycle transitions.

Particularly verify:

`/ops/items/{id}/submit-review`

and any equivalent:

* submit;
* QC decision;
* customer decision;
* engagement acceptance;
* rework;
* retry/confirm.

Determine whether any alternate route can perform the business mutation without producing the required D11 event.

Expected:

**no material alternate-route lifecycle bypass.**

Do not modify routes.

---
```

### Chunk 4 (§22 → §31)

```markdown
# 22. AUTHORIZATION

Verify that D11 does not introduce a parallel authorization system.

Confirm existing:

* authentication;
* organisation access;
* consultant grants;
* capabilities;
* staff/QC authorization;
* canonical permission resolution

remain authoritative.

Do not introduce new permission semantics.

---

# 23. DATABASE / IDEMPOTENCY

Inspect the existing notification schema and uniqueness mechanism.

Verify:

* `event_key`;
* `actor_domain`;
* `notification_type`;
* `uq_notifications_event_key`;
* `create_idempotent`.

Confirm D11 uses the existing durable mechanism correctly.

There should be no migration if the existing schema is sufficient.

---

# 24. RLS

Verify P6-2E introduced no RLS change.

Do not create or alter policies.

If live Supabase/RLS verification is unavailable, state the limitation explicitly.

Do not treat lack of live RLS testing as evidence that RLS is correct.

---

# 25. BILLING

Verify no billing changes were introduced.

Confirm D6/P6-2D billing behavior remains unchanged.

Do not modify billing.

---

# 26. PROVENANCE / ORIGIN

Verify P6-2E did not modify P6-2D's:

* consultant firm provenance;
* processing-mode provenance;
* processing-origin vocabulary.

Specifically verify that:

* `CONSULTANT` is still not an origin;
* no provenance expansion to `confirm/retry` occurred.

---

# 27. TEST VERIFICATION

Independently run:

### Focused

`backend/tests/unit/api/test_p6_2e_consultant_lifecycle.py`

### Relevant regression

Relevant:

* messaging tests;
* notification tests;
* processing workflow tests;
* operations/QC tests;
* P6-2C tests;
* P6-2D tests;
* services/infra tests as appropriate.

### Full unit suite

Run the complete unit suite.

Record actual:

* collected;
* passed;
* failed;
* skipped;
* errors;
* exit code.

Do not rely on the implementation report's test numbers.

Do not alter tests.

Do not change pytest configuration to manufacture a result.

---

# 28. BASELINE

The implementation session independently measured:

**1,627 tests before implementation**

and:

**1,654 after implementation**

with 27 focused tests added.

Independently establish the current count.

Distinguish:

* observed count;
* historical evidence;
* inferred count.

---

# 29. TEST DEFECTS

The implementation report says three focused-test iterations were required and all were test defects.

Do not automatically accept that characterization.

If you inspect the history:

* determine what failed;
* determine why;
* determine whether production behavior was actually correct;
* determine whether tests were weakened.

If any test was improperly weakened or skipped, report it.

Do not change it.

---

# 30. REPOSITORY INTEGRITY

Establish:

* branch;
* HEAD;
* working-tree status;
* tracked modifications;
* untracked files;
* exact P6-2E implementation files;
* pre-existing modifications.

The implementation report states:

* HEAD `1639121`;
* tracked modifications remained at 284;
* four pre-existing files received six P6-2E hunks;
* new lifecycle service/test/report/history files were created.

Independently verify this.

Do not use mtime alone as the sole proof if a stronger method is available.

Do not modify the repository.

---

# 31. SCOPE-CREEP VERIFICATION

Confirm P6-2E did NOT introduce:

* new roles;
* new capabilities;
* new permissions;
* RLS changes;
* migrations;
* consultant conversation kind;
* consultant table;
* P6-2F UI;
* browser E2E;
* Auditor/Assurance;
* Analytics;
* Phase 7;
* Phase 8;
* billing redesign;
* P6-2D remediation;
* `confirm/retry` provenance.

Any attributable scope creep must be reported.

---
```

### Chunk 5a (§32 → §35)

```markdown
# 32. DOCUMENTATION VERIFICATION

Verify the implementation created:

`docs/cline/CARBONTALLY_P6_2E_IMPLEMENTATION_REPORT.md`

and:

`docs/cline/prompt-history/CT-P6-2E-IMPL-20260910-001.md`

Verify that the history contains:

* exact Prompt Ref;
* complete prompt;
* response;
* Response Ref;
* datetime;
* commands;
* tests;
* findings;
* verdict;
* stop condition.

Do not modify historical records.

---

# 33. EVIDENCE STANDARD

Classify every material conclusion as:

### VERIFIED

Directly observed.

### INFERRED

Reasonable conclusion not directly demonstrated.

### NOT VERIFIED

Could not establish.

### LIMITATION

Environment/access prevented verification.

Never turn:

* implementation-agent claims;
* comments;
* assumptions;
* test intent

into VERIFIED evidence without independent support.

---

# 34. DEFECT SEVERITY

Classify findings as:

### BLOCKING

A violation of a ratified security, tenancy, authorization, lifecycle, idempotency, or architecture invariant.

### NON-BLOCKING

A real issue that does not prevent P6-2E acceptance.

### PO CLARIFICATION

The implementation may be technically sound but the authoritative requirements do not determine the correct behavior.

### ENVIRONMENTAL

Verification limitation caused by tooling/environment.

Do not silently make a PO decision.

---

# 35. IMPORTANT QUESTIONS TO ANSWER

Your final report MUST explicitly answer:

1. Are all five D11 lifecycle events correctly implemented?
2. Are their trigger points correct?
3. Are event keys deterministic and collision-safe?
4. Are recipients fully server-derived?
5. Can client input manipulate recipients or event identity?
6. Are cross-org and cross-firm notifications prevented?
7. Do denied operations emit zero events?
8. Can replay create duplicates?
9. Can alternate routes bypass lifecycle emission?
10. Is emission ordering safe?
11. Is best-effort event persistence acceptable under the authoritative contract?
12. Is the rework-cycle limitation acceptable or does it require clarification?
13. Is customer rejection correctly modeled without a rework event?
14. Did P6-2E preserve D8?
15. Did P6-2E preserve P6-2D?
16. Were any roles/capabilities/permissions/RLS/schema changes introduced?
17. Are the tests sufficient and genuinely passing?
18. Are there any blocking defects?

---
```

### Chunk 5b (§36 → §42 + final rule)

```markdown
# 36. STOP CONDITIONS

STOP and report rather than fixing if:

* client-controlled recipients are possible;
* cross-org notification leakage is possible;
* cross-firm notification leakage is possible;
* unauthorized event emission is possible;
* duplicate lifecycle events can materially occur;
* alternate route bypass exists;
* event identity is not deterministic;
* event can be emitted before failed business mutation;
* a migration/RLS/new permission is actually required;
* P6-2E conflicts with the Blueprint or PO register;
* P6-2D must be reopened;
* implementation scope has materially expanded.

Do not remediate.

---

# 37. FINAL VERDICT

Use exactly one:

### PASS

`P6-2E INDEPENDENTLY VERIFIED — PASS`

### PASS WITH NON-BLOCKING FINDINGS

`P6-2E INDEPENDENTLY VERIFIED — PASS WITH NON-BLOCKING FINDINGS`

### BLOCKED / FAIL

`P6-2E INDEPENDENT VERIFICATION — BLOCKED / FAIL`

### INCONCLUSIVE

`P6-2E INDEPENDENT VERIFICATION — INCONCLUSIVE`

Do not use PASS if a material requirement remains unverified.

---

# 38. REQUIRED VERIFICATION REPORT

Create:

`docs/cline/CARBONTALLY_P6_2E_INDEPENDENT_VERIFICATION_REPORT.md`

The report must include:

1. Prompt Ref
2. Response Ref
3. verification date/time
4. verifier role
5. authority documents inspected
6. repository baseline
7. D8 verification
8. D11 five-event verification
9. trigger matrix
10. event-key analysis
11. recipient matrix
12. recipient injection tests
13. cross-org isolation
14. cross-firm isolation
15. denied-path tests
16. replay/idempotency tests
17. alternate-route analysis
18. authorization analysis
19. emission ordering
20. durability/best-effort analysis
21. EventBus analysis
22. database/idempotency analysis
23. RLS analysis
24. billing preservation
25. P6-2D preservation
26. focused tests
27. regression tests
28. full unit results
29. test-defect analysis
30. scope-creep analysis
31. documentation/history verification
32. findings
33. limitations
34. final verdict
35. explicit stop-condition confirmation

---

# 39. REQUIRED DURABLE HISTORY

Create:

`docs/cline/prompt-history/CT-P6-2E-IV-20260910-001.md`

Preserve:

* exact Prompt Ref;
* complete prompt;
* prompt datetime;
* complete verification response;
* Response Ref;
* response datetime;
* commands;
* files inspected;
* tests;
* evidence;
* findings;
* verdict;
* stop-condition confirmation.

Do not modify the implementation history.

---

# 40. NO REMEDIATION

Even if a defect is discovered:

**DO NOT FIX IT.**

Document it.

The project owner will decide whether remediation is authorized.

Do not create a follow-up implementation in this session.

---

# 41. NO PHASE ADVANCEMENT

Do not start:

* P6-2F;
* Phase 7;
* Phase 8.

Even if P6-2E passes, simply state that it is eligible for CP2 closure and the next authorized gate.

---

# 42. FINAL RESPONSE FORMAT

Start with:

`P6-2E INDEPENDENT VERIFICATION — [VERDICT]`

Then provide:

## Executive Result

* verdict;
* CP2 status;
* remediation required/not required.

## D8

Explain the independent result.

## D11 Event Matrix

Show all five events with:

* trigger;
* key;
* recipients;
* authorization;
* persistence;
* idempotency;
* result.

## Security

Summarize:

* recipient injection;
* cross-org;
* cross-firm;
* denied paths;
* replay;
* alternate routes.

## Reliability

Address:

* emission ordering;
* durable persistence;
* best-effort behavior;
* EventBus.

## Findings

List all:

* blocking;
* non-blocking;
* PO clarification;
* environmental limitations.

## Tests

Give actual independently observed results.

## Repository Integrity

Give:

* branch;
* HEAD;
* changed files;
* pre-existing changes;
* unexpected changes;
* commit/push status.

## Scope

Confirm no P6-2F/Phase 7/8 work.

## Stop Condition

Explicitly state that verification stopped at P6-2E.

---

# ABSOLUTE FINAL RULE

**This is an independent verification session.**

**Do not trust the implementation report as proof.**

**Do not modify anything.**

**Do not fix anything.**

**Do not commit.**

**Do not push.**

**Do not advance the roadmap.**

Independently establish the truth, document the evidence, issue one final verdict, create the durable verification record, and STOP.
```

## PART B — COMPLETE VERIFICATION RESPONSE (verdict-first, per prompt §42)

### B.0 Header

`P6-2E INDEPENDENT VERIFICATION — PASS WITH NON-BLOCKING FINDINGS`

### B.1 Executive Result

* **Verdict:** `P6-2E INDEPENDENTLY VERIFIED — PASS WITH NON-BLOCKING FINDINGS`.
* **CP2 status:** P6-2E is **eligible for CP2 closure** (subject to the project owner's acceptance).
* **Remediation:** **not required** for acceptance. One **PO clarification** (IV-C1, client-org recipient
  audience) requires a PO decision — not a code fix — before any recipient change. Eight non-blocking
  findings and three environmental limitations are recorded.

### B.2 D8

D8 is **PRESERVED** with **zero production change**. Consultants participate through the *existing*
conversation model: `api/v3_messaging.py::_authorize_org_actor` admits a caller via
`ensure_consultant_org_access(...)`, which requires an **active** `consultant_clients` grant (D15), and
returns participant role `"consultant"`. The `conversation_kind` vocabulary remains exactly
`org`/`entity`; there is no consultant conversation kind, no `consultant_conversations` table, and no new
role/capability/permission. Organisation isolation is intact (no-grant consultant → 403). The three D8
tests are meaningful, with the caveat that the vocabulary test scans **migrations**, not Python (IV-N7c).

### B.3 D11 Event Matrix

| # | Event | Trigger | Key | Recipients | Authorization | Persistence | Idempotency | Result |
| - | ----- | ------- | --- | ---------- | ------------- | ----------- | ----------- | ------ |
| 1 | `accepted` | `/organizations/{org}/consultant-engagements/{id}/accept` after `pending→active` + audit | `consultant.lifecycle.accepted:engagement:<id>` | active members of the engaged firm | `require_org_admin` + `ensure_org_access` + transition rule | `create_idempotent` → `public.notifications` | `uq_notifications_event_key` + 409 replay | PASS |
| 2 | `submitted_to_qc` | `/processing/items/{id}/consultant-submit` after provenance + `→reviewed` + audit | `consultant.lifecycle.submitted_to_qc:item:<id>` | active-grant firm members + ops (`can_manage_staff`) | `require_auth` + submission gate + D6 | same | same | PASS |
| 3 | `qc_outcome` | `/ops/qc/items/{id}/decision` after guarded `ct_qc_decision` + audit | `…qc_outcome:item:<id>:approved\|rejected` | active-grant firm members | `require_staff` + `require_internal_staff` + `can_qc` | same | same | PASS |
| 4 | `customer_decision` | `/processing/items/{id}/customer-review` after D37 charge + `customer_review` + issue closure | `…customer_decision:item:<id>:approved\|rejected` | active-grant firm members | `require_org_admin` + prerequisites | same | same | PASS |
| 5 | `rework` | (a) CT-QC rejection; (b) `/processing/items/{id}/consultant-review` `passed=false` → `mapping` | `…rework:item:<id>:ct_qc_rejected` / `:consultant_review_rejected` | active-grant firm members | as 3 / review gate | same | same | PASS |

Recipients are identical (firm-only, + ops for event 2) across events; **no client-org recipient** for
any event (IV-C1).

### B.4 Security

* **Recipient injection:** none possible. No emit route accepts `recipients`, `event_key`, `actor_domain`,
  org or firm. The focused injection test posts `recipients`/`event_key`/`actor_domain` and they are
  ignored; recipients are derived server-side. **VERIFIED.**
* **Cross-org:** prevented — recipients come from the item's/engagement's own organisation; a foreign
  organisation's users never receive the event. **VERIFIED.**
* **Cross-firm:** an unrelated firm (no active grant) is never a recipient. Two firms *both* holding
  active grants for the same org are both notified (IV-N2) — not leakage under the ratified org-grant
  model, but broader than item-firm provenance. **VERIFIED with IV-N2 caveat.**
* **Denied paths:** unauthenticated (401), unauthorised (403), missing capability (403), suspended/inactive
  grant (403), invalid state (409), missing reason (422), concurrent QC (409) — all precede the emit and
  produce **zero** rows. **VERIFIED.**
* **Replay:** deterministic keys + `uq_notifications_event_key` + state-machine 409 ⇒ no duplicates. **VERIFIED.**
* **Alternate routes:** `POST /ops/items/{id}/submit-review` correctly emits nothing (not a consultant
  event). Two out-of-set gaps (IV-N4) — automatic-processing job review; consultant `reactivate` — neither
  crosses an authorization boundary. **No material bypass within the declared trigger set. VERIFIED.**

### B.5 Reliability

* **Emission ordering:** mutate → audit → emit (last statement). No path emits before a failed mutation. **VERIFIED.**
* **Durable persistence:** events persist through the existing `public.notifications` table via
  `create_idempotent`; not the in-process bus. **VERIFIED.**
* **Best-effort behaviour:** `_emit` swallows per-recipient failures and logs; a persistence outage loses
  the notice, not the action; the emit is not in the same transaction as the mutation. Consistent with the
  existing emitter convention and not prohibited by the contract (IV-N5 family). **VERIFIED (acceptable).**
* **EventBus:** not used by D11; durable notifications are authoritative. **VERIFIED.**

### B.6 Findings

**BLOCKING:** none.

**PO CLARIFICATION — IV-C1** — Recipient-matrix divergence (client-org recipients): Contract §7.2 lists
client org owner/admin for events 1/3/4 and "the responsible processor" for event 5; the implementation
notifies no client-org member. The same contract/register declare the matrix an explicit implementation
non-decision; no leakage, no privilege. Requires PO adjudication of the intended audience. Do not
remediate without a PO decision.

**NON-BLOCKING:**
* **IV-N1** — Event 2 internal recipients use `can_manage_staff` (staff-admins), not `can_qc`; a QC-only
  operator gets no intake notice.
* **IV-N2** — Recipient derivation is org-grant-scoped, not item-firm-scoped (two active-grant firms both
  notified).
* **IV-N3** — A deactivated firm profile with a retained active grant still yields (inert) recipients.
* **IV-N4** — Out-of-set alternate routes (automatic-processing job review; consultant `reactivate`) emit
  no lifecycle event; no authorization boundary crossed.
* **IV-N5** — Per-cycle idempotency limitation (repeat occurrences with the same identity+outcome are
  collapsed; would need a forbidden migration).
* **IV-N6** — Notification deep-link `/consultant/items/{item_id}` does not match the frontend route
  `/consultant/items/:clientId/:itemId` (`frontend/src/App.js:2169`); presentation-only, owned by P6-2F.
* **IV-N7** — Test-quality weaknesses (vacuous injection assertion; `in (403,404)`; uncovered cases; no DB
  integration idempotency test).
* **IV-N8** — Implementation-report bookkeeping (1627 baseline and hunk accounting not re-derivable;
  porcelain has advanced to 675 because the verification docs are untracked).

**ENVIRONMENTAL:** IV-E1 (`-q`/`-qq` summary suppression via `addopts`); IV-E2 (no live database — RLS/DB
idempotency verified by inspection + fake); IV-E3 (no live browser/E2E, correctly out of scope).

### B.7 Tests (independently observed)

* **Focused:** `python -m pytest tests/unit/api/test_p6_2e_consultant_lifecycle.py -p no:cacheprovider --tb=short`
  → `27 passed in 7.57s`, `EXIT=0`.
* **Full unit suite:** `python -m pytest tests/unit -p no:cacheprovider --tb=line`
  → `1654 passed, 1 warning in 264.39s (0:04:24)`, `EXITCODE=0`.
* **Collected:** `python -m pytest tests/unit --collect-only -q -p no:cacheprovider` → **1654**.
* Collected 1654 · passed 1654 · failed 0 · errors 0 · skipped 0 · exit 0.
* Baseline: 1654 = observed; 1627 = historical (implementation session / P6-2D closure); 1627+27 = 1654 =
  inferred and consistent.

### B.8 Repository Integrity

* **Branch:** `main`. **HEAD:** `1639121` (`16391217103b98dcea520070c5a22c68f12fe607`) — unchanged.
* **Working tree:** 675 porcelain entries = 284 ` M` + 152 ` D` + 239 `??` (pre-existing). No staging,
  commit, push, reset or clean.
* **P6-2E change set:** created `backend/services/consultant_lifecycle.py` and
  `backend/tests/unit/api/test_p6_2e_consultant_lifecycle.py`; 6 marked hunks across
  `v3_organizations.py`, `v3_operations.py`, `v3_processing_workflow.py` (3), `fakes.py`; plus 2 docs.
* **Pre-existing changes:** the ~284 tracked modifications and 152 deletions pre-date P6-2E.
* **Unexpected changes attributable to P6-2E:** none beyond the 6-file set.
* **Changes by this verification session:** the two mandated verification docs only.
* **Commit / push status:** none (as required).

### B.9 Scope

No P6-2F UI, no browser E2E, no Auditor/Assurance, no Analytics, no Phase 7, no Phase 8, no billing
redesign, no P6-2D remediation, no `confirm`/`retry` provenance, no new roles/capabilities/permissions,
no RLS change, no migration. **Confirmed.**

### B.10 Stop Condition

Verification is **STOPPED at the P6-2E boundary**. No stop condition was triggered. No remediation was
performed. P6-2E is eligible for CP2 closure and the next authorized gate; the roadmap was not advanced.

## PART C — COMMANDS, FILES, TESTS, EVIDENCE

### C.1 Commands executed (read-only)

```bash
git rev-parse --abbrev-ref HEAD ; git rev-parse HEAD            # main / 16391217103b98dcea520070c5a22c68f12fe607
git status --porcelain=v1                                       # 675 entries (284 ' M', 152 ' D', 239 '??')
git status --porcelain | awk '{print substr($0,1,2)}' | sort | uniq -c
git status --porcelain -- <p6-2e files>                         # all '??' untracked
git --no-pager diff --stat -- backend/api/v3_organizations.py backend/api/v3_processing_workflow.py backend/api/v3_operations.py backend/tests/unit/api/fakes.py
grep -rn 'P6-2E' backend/ --include='*.py'                      # 6 markers
find backend frontend supabase tests -newermt '2026-09-10 20:20' -type f   # 6 files
ls -t supabase/migrations/ | head -5                            # newest = 20260910120000_p6_2d_consultant_provenance.sql
grep -rn 'conversation_kind' supabase/migrations/*.sql          # org / entity only
grep -rn 'consultant_conversations' supabase backend --include=*.sql --include=*.py
grep -rn 'event_bus\|EventBus' backend --include=*.py
grep -n 'router.post\|router.get' backend/api/v3_processing_workflow.py | grep -i 'consultant\|customer'
grep -n 'consultant_review(' backend/api/v3_automatic_processing.py   # alternate route (IV-N4)
grep -n 'reactivate' backend/api/v3_consultants.py                   # alternate route (IV-N4)
cat backend/domain/processing_origin.py                              # CONSULTANT not an origin
cd backend && python -m pytest tests/unit/api/test_p6_2e_consultant_lifecycle.py -p no:cacheprovider --tb=short
cd backend && python -m pytest tests/unit --collect-only -q -p no:cacheprovider
cd backend && python -m pytest tests/unit -p no:cacheprovider --tb=line
```

No write command was run against the repository except `rm` of the two pre-existing verification
artefacts (untracked) prior to re-creating them.

### C.2 Files inspected

Authorities: Blueprint V1.3; Master Roadmap V1.0; P6-2 PO Decision Register (D8/D11/L580–582);
Phase-6 Remainder Implementation Contract V1.0 (§7.1/§7.2/§8/§10/§19); P6-2 Consultant Processing
Workflow Architecture; P6-2B Consultant Review/QC Architecture; P6-2E Preflight Report; P6-2E
Implementation Report; CP1 Closure Report; implementation prompt history; P6-2D implementation + IV
reports; PO ratification report.

Source: `services/consultant_lifecycle.py`; `data/notifications.py`; `data/consultants.py`;
`domain/processing_origin.py`; `api/v3_organizations.py`; `api/v3_processing_workflow.py`;
`api/v3_operations.py`; `api/v3_automatic_processing.py`; `api/v3_messaging.py`; `api/consultant_auth.py`;
`api/v3_consultants.py`; `infra/event_bus.py`;
`supabase/migrations/20260902050000_phase5_notification_event_key.sql`;
`supabase/migrations/20260902040000_phase5_pe_operational_messaging.sql`;
`tests/unit/api/test_p6_2e_consultant_lifecycle.py`; `tests/unit/api/fakes.py`; `frontend/src/App.js`;
`backend/pyproject.toml`.

### C.3 Tests

See B.7. Focused 27/27 (exit 0); full suite 1654 passed/0 failed (exit 0); collected 1654.

### C.4 Evidence summary

* Deterministic key builder (no uuid/random); DB partial unique index `uq_notifications_event_key`;
  `create_idempotent` `ON CONFLICT … DO NOTHING`.
* Emit sites are the final statement after mutation + audit in all five handlers.
* No emit route exposes recipient/identity fields; injection ignored.
* `consultant_lifecycle.py` is a pure consumer of existing relationships — no new role/capability/permission.
* `CONSULTANT` is still not a processing origin; no P6-2D artefact modified; no migration; no RLS change.
* Pre-existing verification artefacts (IV report + history) were found already present and were replaced
  by this session's independently-derived output.

### C.5 Answers to prompt §35 (compact)

1 Yes · 2 Yes · 3 Yes · 4 Yes · 5 No · 6 Cross-org yes / cross-firm yes except IV-N2 over-breadth ·
7 Yes · 8 No · 9 Not within declared triggers (IV-N4 gaps) · 10 Yes (non-transactional, no false event) ·
11 Acceptable under the contract (PO visibility) · 12 Acceptable as designed (IV-N5) · 13 Yes — see
report §36 discussion · 14 Yes · 15 Yes · 16 None · 17 Yes (27/27 + 1654/0) · 18 None.

## PART D — INTEGRITY CONFIRMATION, VERDICT, STOP CONDITION

### D.1 Integrity confirmation

* No production code, test, migration, schema, RLS, frontend or configuration was modified.
* No defect was fixed; no refactor; no test rewritten; no test expectations altered.
* No `git add`, `git commit`, `git push`, `git reset` or `git clean` was used.
* Branch `main`, HEAD `1639121` unchanged.
* The only files created by this session are
  `docs/cline/CARBONTALLY_P6_2E_INDEPENDENT_VERIFICATION_REPORT.md` and this history file.
* The implementation report and the implementation history were read only, not modified.
* Pre-existing verification artefacts with the same target paths (untracked, dated 21:20, verdict
  `PASS WITH NON-BLOCKING FINDINGS`) were replaced by this session's independently-derived output; no
  git-tracked file or history was altered.

### D.2 Findings (see PART B.6 / report §32)

* BLOCKING: none.
* PO CLARIFICATION: IV-C1 (client-org recipient audience).
* NON-BLOCKING: IV-N1 … IV-N8.
* ENVIRONMENTAL: IV-E1 … IV-E3.

### D.3 Verdict

> **`P6-2E INDEPENDENTLY VERIFIED — PASS WITH NON-BLOCKING FINDINGS`**

### D.4 Stop-condition confirmation

Verification **STOPPED at the P6-2E boundary**. No stop condition was triggered; no remediation was
performed; no P6-2F / Phase 7 / Phase 8 work was started. P6-2E is eligible for CP2 closure and the next
authorized gate.










