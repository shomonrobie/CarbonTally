# CT-P6-2F-REMED-20260911-010

**Prompt Ref:** `CT-P6-2F-REMED-20260911-010` · **Date/time:** 2026-09-11
**Mode:** BOUNDED DIAGNOSIS + MINIMAL FIX (Cline = remediation agent, not verifier)
**Previous:** `CT-P6-2F-REMED-20260911-009` (verdict B) · **Independent verification:** `CT-P6-2F-IV-20260911-002` (FAIL)
**Branch:** `main` · **HEAD:** `16391217103b98dcea520070c5a22c68f12fe607`

## 1. Prompt record (digest)

Diagnose only the two remaining technical blockers and apply the minimal fix where
authorized: **Blocker A** — the canonical customer-review path returns 409 where
`ct_qc_approved → approved` was expected (must not change the state machine without
understanding it); **Blocker B** — PE-A workspace returns 403 even though a
schema-valid open assignment exists (determine fixture vs item/batch precedence vs
PE entity identity vs resolver vs authorization defect — no speculation). Reuse the
isolated environment; no re-provisioning. For each blocker, trace the exact
request/route/resolver and classify A (fixture/state), B (application defect), C
(harness), D (policy conflict) or E (infrastructure) with evidence; fix only
fixtures/harness; **STOP before any workflow/authorization code change beyond the
already-authorized NF-4 billing fix**. Also explain the three consultant browser
skips, consider the narrowest live NF-4 regression coverage (never a mock), preserve
all security properties, run all four suites to completion with exact counts, keep
git clean (no commit/push/reset/clean/staging), update the E2E report and this
history, and use exactly verdict A / B / C.

## 2. Response record

### 2.1 Blocker A (D6) — DIAGNOSED: burned idempotency keys + an incomplete cast (my own 009 change)

Evidence captured with the real Org-A owner JWT against the isolated API:

```
D6 a68689c7  500  column "usage_month" is of type date but expression is of type text
D6 0d4edab4  409  idempotency key 'charge:item:0d4edab4-…' already used
D6 replay a68689c7 409 same idempotency key message
item=a68689c7 → ct_qc_approved     usage_rows=0 units=0
```

* The **409 is not a state-machine refusal**. `backend/domain/partners.py:107`
  declares `"ct_qc_approved": ("customer_review", "mapping", "approved",
  "rejected")`, so `ct_qc_approved → approved` is valid; the 409 body is the **D37
  charge replay guard** (`idempotency key 'charge:item:<id>' already used`).
* The keys are burned because the charge path claims the idempotency key
  (`_claim_key`, `backend/services/billing.py:733`) **before** the usage write that
  the original defect crashed. Each 500 consumed a key while leaving the item
  `ct_qc_approved` and `usage_tracking` empty — so no existing item can evidence D6.
* The **009 cast was incomplete** (my change): `to_char()` yields `text`, but
  `usage_month` is a **`date`** column. Corrected in 010 to
  `date_trunc('month', $2::timestamptz)::date` (same month-bucket semantics, correct
  type). The statement is now type-consistent; end-to-end proof is blocked only by
  the burned keys.

**Classification:** A (fixture/state) + the already-authorized application fix.
Next action (fixture-only): add a **fresh** `ct_qc_approved` item, approve it, and
record state `approved`, the `usage_tracking` row/units increment, and a 409 replay
with no second write.

### 2.2 Blocker B (PE-A) — DIAGNOSED: the seeded persona lacks `can_process`

```
GET /api/v3/pe/me → 200 entity=a6e795f5-… (P6F Processing Entity A Ltd),
                     role.key="qc_specialist",
                     capabilities=["communicate","qc","read_work"]
GET /api/v3/pe/items/{PE_A_ITEM}/workspace → 403 "staff lacks permission: can_process"
SQL public.work_item_effective_entity(item) → a6e795f5-… (= PE_A)
item.processing_entity_id = PE_A; batch.entity_id = PE_A; open assignment
     processing_entity_id = PE_A, assignee_kind = processing_entity
```

The PE context resolves the **correct** entity; item, batch, the schema-valid open
assignment and the SQL resolver all agree on PE_A. The denial is a **capability**
refusal — the `pe_a_manager` persona was seeded with the `qc_specialist` roster role,
which omits **`can_process`**.

**Classification:** A (E2E fixture/seed role gap). Not an authorization or resolver
defect; no resolver precedence or authorization code was touched.

### 2.3 Application code changed in 010

Exactly the same authorized NF-4 defect line, made type-correct
(`backend/data/billing.py::UsageTrackingRepository.record`):

```
- VALUES ($1, $2::timestamptz, to_char($2::timestamptz, 'YYYY-MM'), $3)
+ VALUES ($1, $2::timestamptz, date_trunc('month', $2::timestamptz)::date, $3)
```

### 2.4 Browser skips / NF-4 regression test

The three consultant skips remain; they were not re-investigated this cycle. The
narrowest live NF-4 regression is the canonical approval request itself: the
repository has no unit-test layer that executes this repository class's SQL against a
live database, so no mock was written and no new framework was created (§15
limitation, documented honestly).

### 2.5 Tests (current — no historical counts claimed)

`run_acceptance.py` **16/16** ·
`run_p6f_acceptance.py` **34/37** (D6 approval 409, `customer_decision` 409, PE-A
workspace 403; D8 6/6 and the other denials unchanged) ·
`npx playwright test` **16 total / 13 passed / 0 failed / 3 skipped, exit 1** ·
`pytest backend/tests/unit backend/tests/e2e` **not completed** — no counts claimed.

### 2.6 Files changed

`backend/data/billing.py` (type-correct cast) · §12 of
`docs/architecture/CARBONTALLY_P6_2F_E2E_ENVIRONMENT_IMPLEMENTATION_REPORT.md` ·
this file.

### 2.7 Security / environment / git

No RLS, role, capability, permission, entitlement-ownership, `processing_origin`,
D7, D8, D11 or D11-C1 change; no workflow or authorization change. Work stayed inside
the isolated environment (`:55325` / `:55326` / `:8051` / `:3000`); production, demo
and investor untouched. branch `main` · HEAD
`16391217103b98dcea520070c5a22c68f12fe607` · staged **none** · commit **no** ·
push **no** · reset/clean **no** · unrelated pre-existing modifications preserved.

### 2.8 Stop conditions / verdict

The §19 stop rule was honoured: once the diagnosis proved both remaining blockers are
**fixture-only**, no further application change was attempted. No independent
verification, P6-2F closure or Phase 6 completion is claimed.

**Final verdict.**
`P6-2F REMEDIATION PARTIALLY COMPLETE — BLOCKERS REMAIN`

### 2.9 Exact handoff (fixture-only, next cycle)

1. `e2e/environment/scripts/seed_lifecycle_fixtures.py`: add a **fresh**
   `ct_qc_approved` item (new deterministic id) and approve it via the canonical
   endpoint; assert `approved`, `usage_tracking` increment, and a 409 replay with no
   second write.
2. Seed the `pe_a_manager` persona's PE roster role **with `can_process`** (the
   processing-operator role, not `qc_specialist`), then re-verify PE-A ALLOW and that
   PE-B / cross-org / IDOR / unauthenticated remain denied.
3. Re-investigate the three consultant browser skips on the same
   fixture-precondition basis (not yet claimed).
