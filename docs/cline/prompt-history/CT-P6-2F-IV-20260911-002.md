# CT-P6-2F-IV-20260911-002 — P6-2F Independent Verification

**Prompt Ref:** `CT-P6-2F-IV-20260911-002` · **Verifier:** OpenHands (DeepSeek), independent of the implementation agent
**Date/time:** 2026-09-11 · **Mode:** INDEPENDENT VERIFICATION — READ ONLY
**Repository:** CarbonTally · **Branch:** `main` · **HEAD:** `16391217103b98dcea520070c5a22c68f12fe607`

## 1. Independence statement

Verification was performed as an auditor. No implementation file, test, fixture,
SQL, migration, configuration, document or database row was created or modified
during this session, and no state-changing endpoint was invoked (the read-only rule
prohibits it, so mutation-based reproduction is reported as *missing evidence*
rather than simulated). Implementation reports and prompt histories were used only
as pointers to what to check; every claim below rests on evidence I obtained
directly from the running environment, the database, or the repository.

## 2. Authorities consulted

Blueprint V1.3 · Master Roadmap V1.0 · Phase 6 Remainder Implementation Contract
V1.0 · P6-2 PO Decision Register · CP2 Closure P6-2F Preflight Report ·
`CARBONTALLY_P6_2F_E2E_ENVIRONMENT_IMPLEMENTATION_REPORT.md` (§7–§9) · prompt
history `...-004`, `...-005`, `...-006`.

## 3. Environment integrity

| Endpoint | Observed |
|---|---|
| Supabase PostgREST `:55325/rest/v1/` | **200** |
| Postgres `127.0.0.1:55326` | reachable (`psql` returned rows) |
| FastAPI `:8051` | live — unauth `/api/v3/notifications` → **401** |
| CRA frontend `:3000` | live — `/login` → **200** |

GoTrue issued real JWTs for **7/7** personas exercised (`org_a_owner`,
`org_b_owner`, `consultant_a`, `consultant_b`, `pe_a_manager`, `pe_b_manager`,
`internal_qc`). Isolated DB: 175 policies, 116 RLS-enabled public tables,
2 notifications, 2 `customer_subscriptions`.
**Limitation:** no compiled frontend bundle exists to inspect, so runtime frontend
isolation is proven only for API/DB/PostgREST, not for the served SPA.

## 4. RLS evidence (PostgREST + real user JWT, no service-role)

| Case | Identity | Result |
|---|---|---|
| Org A → Org A | org_a_owner | **200, row returned** |
| Org A → Org B | org_a_owner | **200, zero rows** (RLS filtered) |
| Org B → Org B | org_b_owner | **200, row returned** |
| Org B → Org A | org_b_owner | **200, zero rows** |
| Unauthenticated | none | **401** |

Policy / RLS-enabled counts match the pre-grant baseline (175 / 116): the E2E
baseline grants let `authenticated` reach RLS without disabling it.
**RLS PRESERVED.**

## 5. Authentication / actor isolation (read-only, real JWTs)

| Probe | Result |
|---|---|
| consultant_a → `/processing/items/{ITEM_A}/workspace` (firm-scoped) | **200 ALLOW** |
| consultant_b → same item | **403 DENY** |
| org_b_owner → same item | **403 DENY** |
| consultant_a → `/ops/qc/ct-queue` | **403 DENY** |
| internal_qc → `/ops/qc/ct-queue` | **200 ALLOW** |
| unauthenticated → `/notifications` | **401 DENY** |
| **pe_a_manager → `/pe/items/{ITEM_PE_A}/workspace`** | **403 — ALLOW expected** |
| pe_b_manager → same PE-A item | **403 DENY** |
| pe_a_manager → `/pe/work` | **200** |

No identity/org/firm/PE injection succeeded. **PE-A ALLOW fails.**

## 6. D6 — ownership, preflight, approval

**Ownership — VERIFIED.** `/api/v3/billing/me` → org_a_owner **200**,
org_b_owner **200** (own organisation only), consultant_a **403**, pe_a_manager
**403**, unauthenticated **401**; org-scoped entitlement reads for another org
return **404**. Entitlement is therefore client-organisation owned and neither the
consultant nor the PE becomes the owner.

**Preflight / approval-time ALLOW — NOT VERIFIED.** The isolated DB holds
`billing_commercial_config` `standard_allowance` **v1 with
`monthly_processing_units: null`** and **v2 with `100`**. The most recent
*directly observed* behaviour of the canonical approval action (customer-review)
with a valid allowance was **HTTP 500** (implementation run 006, immediately after
the 402 was cleared). The mandatory criterion — gate succeeds, state transitions,
consumption/usage changes — therefore has **no PASS evidence**, and a **500 on the
canonical approval path is a material concern**. It was not reproducible here
because that would require state mutation.

## 7. D7 — provenance

`manual_extraction_items.consultant_firm_id` is populated **server-side** on the
five consultant-scoped items (`ee1c059c-…`) and `NULL` on the internal/PE items —
consistent with server-derived, action-time provenance. Observed
`processing_origin` values in the isolated data are `CARBONTALLY_INTERNAL` and
`PROCESSING_ENTITY` only — **no `CONSULTANT` origin**. Firm-injection and
origin-injection were not exercised as writes (read-only rule).

## 8. D8 — conversation model

No consultant-specific conversation kind, table, type, role or capability was
found; the observed vocabulary is consistent with the existing `org`/`entity`
model and the synthetic data holds **no conversation rows**. Consultant A's
authorised access and the cross-org / other-firm / unauthenticated denials were
recorded by the implementing session but **not independently re-executed** here.

## 9. D11 — five events

| Event | Independent status |
|---|---|
| `accepted` | **No acceptance evidence** — only a state-idempotency 409 previously observed. |
| `submitted_to_qc` | **Partial.** DB evidence: item `04e671e9…` is `reviewed` and **2 notification rows persist**; the implementing session observed submit **200**, durable notification, replay **409 with no duplicate rows**, denied submit **403 with 0 new rows**. I confirm the persisted outcomes, not the live action. |
| `qc_outcome` | **No acceptance evidence** — only a GET → 405 route-inventory probe; the mutation method was never exercised. |
| `customer_decision` | **No acceptance evidence** — blocked behind the D6 500/409. |
| `rework` | **No acceptance evidence** — only a 403 authorization-scoped refusal. |

Event-level durability, deterministic keys and recipient derivation are therefore
**unproven for four of five events**; recipient injection and cross-org/cross-firm
leakage were not exercised.

## 10. D11-C1

No client-organisation lifecycle recipient was found (persisted notification rows
are user/firm-scoped). This is a *negative* conclusion (absence of the violation)
rather than positive confirmation of firm-recipient delivery.

## 11. Consultant browser acceptance

**NOT INDEPENDENTLY EXECUTED** — the suite performs real review/submit clicks and
would mutate application state, which the read-only rule forbids. Last captured
figures are the implementation's (005): **16 total / 13 passed / 0 failed / 3
skipped, exit 1, 26.1s**. Per §14 a required skip is a finding, not a pass, so the
three skipped specs (submit-to-QC, pass-review, notification deep link) remain
**unverified**. Read-only fixture evidence is favourable (consultant A returns 200
on a firm-scoped item; the deep-link item is persisted).

## 12. PE

* PE-A ALLOW on the item workspace: **403 (FAIL)** — `/pe/work` returning 200 does
  not prove item authorization.
* PE-B → PE-A item **403 DENY**; PE-A → Org B item 403; consultant → PE route 403;
  alternate PE routes previously 403/405 for PE-B.
* Browser PE denial **not executed** (read-only).
* `work_item_assignments` is empty and a fixture insert was rejected `23502`; the
  cause of the PE-A 403 is **unresolved**.

## 13. Security negative paths

Independently verified: unauthenticated 401 (API + PostgREST), cross-org 403 (API)
and zero-row RLS at the DB, cross-firm consultant 403, PE cross-PE 403, internal
ops 403 for a consultant, D6 ownership 403/404 for consultant and PE. Not
independently exercised (read-only): actor/org/firm/origin/recipient injection,
replay, entitlement misuse, suspended-grant path, alternate-route bypass with the
correct mutation methods.

## 14. Regression

**NOT INDEPENDENTLY COMPLETED.** `pytest backend/tests/unit backend/tests/e2e` was
still running (~21%) when the session ended and also mutates E2E state. No counts
are claimed; the implementation's earlier 1,693/1,693 is not accepted as
verification evidence.

## 15. Findings

| ID | Finding | Severity | Evidence | Impact |
|---|---|---|---|---|
| F1 | PE-A item workspace denied (403) | **BLOCKING** | `GET /api/v3/pe/items/{ITEM_PE_A}/workspace` as pe_a_manager → 403 | PE ALLOW criterion cannot pass |
| F2 | D6 approval-time ALLOW unproven; canonical path last observed **500** | **BLOCKING** | DB allowance v2=100; prior direct observation 500; not reproducible read-only | D6 criterion unmet |
| F3 | D11 `accepted`, `qc_outcome`, `customer_decision`, `rework` lack event-level evidence | **BLOCKING** | only 405/409/403 probes | 4 of 5 mandatory events unverified |
| F4 | Three required browser specs still skipped | **BLOCKING** | 005: 3 skipped | Browser acceptance incomplete (§14) |
| F5 | Backend regression not completed | **BLOCKING** | run unfinished | No regression acceptance |
| F6 | Frontend runtime isolation not independently proven | NON-BLOCKING | no compiled bundle to inspect | Isolation claim limited to API/DB |
| F7 | Recipient derivation / event keys not read from persisted rows | NON-BLOCKING (subset of F3) | 2 notification rows exist, recipients unread | Event-level evidence gap |
| F8 | `standard_allowance` v1 (`null`) coexists with v2 (`100`) | INFORMATIONAL | DB query | Ordering semantics unverified |

## 16. Limitations

Mutation-based acceptance (approval, submission, QC, rework) could not be
reproduced under the read-only rule, so those claims are recorded as *unverified*
rather than as behavioural failures. Browser and regression suites were not
independently executed. No production/demo/investor endpoint was contacted and no
production credential was used.

## 17. Remediation

"No implementation remediation was performed during this independent verification."
The only file written was this report.

## 18. Git integrity

branch `main` · HEAD `16391217103b98dcea520070c5a22c68f12fe607` · staged **none** ·
commit **no** · push **no** · reset/clean **no** · 682 pre-existing working-tree
entries preserved; **no verifier-created repository modification**.

## 19. Final verdict

**P6-2F INDEPENDENT VERIFICATION — FAIL**

Blocking findings F1–F5: PE-A ALLOW denied; D6 approval-time ALLOW unproven with a
500 on the canonical path; four of five D11 events lacking event-level evidence;
three required browser specs skipped; backend regression not completed.
Independently **confirmed**: RLS preservation, real-JWT authentication,
organisation isolation, cross-firm/PE denial, D6 entitlement **ownership**, and the
absence of a `CONSULTANT` processing origin.
