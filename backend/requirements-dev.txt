# Test/development dependencies (the runtime surface stays in requirements.txt).
# The pytest configuration in pyproject.toml ([tool.pytest.ini_options]) relies
# on these plugins:
#   asyncio_mode = "auto"  -> pytest-asyncio
pytest>=8.0
pytest-asyncio>=0.24
