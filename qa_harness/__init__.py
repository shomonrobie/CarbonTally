"""CarbonTally QA Harness v1.

A reusable, read-only QA harness for independently verifying CarbonTally V3.

BUILD-ONLY: this package must not be run against the CarbonTally application
until a stable implementation checkpoint is reached and a run is explicitly
authorized.
"""

__version__ = "1.0.0"
